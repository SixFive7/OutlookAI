{
  "_readme": "The test-VM parameter set, in one machine-readable place. Testbed/README.md is the prose entry point; this file is what scripts and .github/scripts/check-testbed-references.ps1 read. NEVER put a credential here - this repository is public. See Testbed/README.md section 'Credentials'.",

  "schemaVersion": 1,

  "corpus": {
    "_note": "These four values deterministically reproduce the corpus every published sweep and frame measurement rests on. They are NOT an example: they were read out of the manifest header of the corpus that is on the VM, and re-derived on the host with 'corpus-plan', which reproduced the recorded per-folder and per-window counts exactly.",
    "corpusId": "vm2",
    "seed": 7777,
    "anchor": "2026-08-19",
    "anchorUtc": "2026-08-19T00:00:00Z",
    "itemCount": 20000,
    "shape": "default",
    "shapeKey": "v1|vm2|7777|2026-08-19T00:00:00Z|s:short:45:200:1200|s:normal:33:1200:6000|s:long:15:6000:24000|s:quoted-thread:6:24000:96000|s:huge-thread:1:96000:640000|d:last-24h:2:0:1|d:1d-7d:6:1:7|d:7d-60d:17:7:60|d:60d-1y:30:60:365|d:1y-4y:45:365:1460|f:6:55|f:5:25|f:3:12|f:23:8",
    "storeDisplayName": "Outlook Data File",
    "storeFilePath": "C:\\Users\\vmadmin\\Documents\\Outlook Files\\Outlook Data File - OutlookAITest.pst",
    "placementMethod": "DraftsThenMoveWithSentFlag",
    "dateWriteMethod": "PropertyAccessorDates",
    "manifestFileName": "corpus-vm2.jsonl",
    "provenance": "VERIFIED 2026-08-24. Header line of corpus-vm2.jsonl recovered from the guest; 'corpus-plan --corpus-id vm2 --seed 7777 --anchor 2026-08-19 --count 20000' on the host reproduced Inbox=10,912 / Sent Items=4,964 / Deleted Items=2,461 / Junk Email=1,663 and 7d=1,612, which are the counts Docs/magic-numbers.md and Docs/autonomous-session-log.md quote."
  },

  "corpusIdConvention": {
    "_whatThisIsNot": "NOT MEASUREMENT AND NOT PROVENANCE. The 'corpus' block above records one corpus that exists and was measured, and .github/scripts/check-testbed-references.ps1 pins its four values across three files. THIS block is a naming rule plus the ids reserved under it, most of which describe corpora that DO NOT EXIST YET. It is separated rather than folded into 'corpus' precisely so the two can never be confused: nothing here may be read as a figure anybody took off a machine, and adding a row here is not a measurement.",
    "_why": "A manifest is named after the CORPUS, not after the guest, and Testbed/host/Copy-FromGuest.ps1 lands every guest's pull in one shared directory. One guest, harmless. Two guests sharing a corpus id, and the second pull silently replaces the first's manifest - which is the EntryID allowlist corpus-teardown requires and the file corpus-verify reads for freshness, so losing it removes the only safe way to remove that corpus from a real store. The fix is not a per-guest subdirectory: the two corpora genuinely ARE different populations, on different machines with different index state, and they already have to be told apart in each machine's settings file and in every measurement. Giving them one id and disambiguating by directory would paper over that.",
    "manifestFileName": "corpus-<corpusId>.jsonl",
    "manifestFileNameNote": "This is what makes Copy-FromGuest.ps1's default 'corpus-*.jsonl' include pattern work, and it is the whole of the collision fix: distinct ids give distinct file names, so the shared destination stops being a shared file. Copy-FromGuest.ps1 also REFUSES to replace a manifest whose content differs unless -Force is passed, as a backstop for the day somebody reuses an id anyway.",
    "idCharacterRule": "ASCII letters, digits, '-' or '_' - enforced by CorpusPlan's constructor, because the id lands inside the subject tag that teardown matches ordinally. Prefer '-': '_' is DASL's single-character wildcard, which turns CorpusPlan.DaslSubjectFragment from an exact match into a superset.",
    "namingRule": "Name the corpus after the PROPERTY THAT DIFFERS, not after a sequence number. 'vm2' is opaque and only works because there is one of it; with two guests the id has to carry which population it is, since it is what appears in a measurement's comparison scope, in every corpus subject, and in the manifest file name a human reads in a shared directory.",
    "assigned": [
      {
        "corpusId": "vm2",
        "guest": "OutlookAI-TestVM",
        "status": "BUILT AND MEASURED",
        "_source": "Restated here only so the id space is readable in one place. The 'corpus' block above is authoritative for this one and is what the CI check pins; do not maintain a second copy of its seed, anchor or count here."
      },
      {
        "corpusId": "vm-indexed",
        "guest": "OutlookAI-Indexed",
        "indexed": true,
        "status": "NOT BUILT - reserved name only",
        "seed": null,
        "anchor": null,
        "itemCount": null,
        "minimumItemCount": 160000,
        "_minimumItemCount": "A REQUIREMENT, not a measurement - proposed 2026-09-24 by the Q69/Q70 work under 'do not compromise on the tests', and KEPT by the maintainer's decision the same day (question E, option (a)), so the index tier's latency assertions are tests on this guest rather than formalities. Those assertions are timed against the LARGEST indexed store since the same day (question B, option (c)) - which on this guest is this corpus. Every index query in the live tier is held to MaxQueryMs = 2000, and against one 20,000-item store on an idle VM that is met by construction; the maintainer's profile, which those numbers were set against, holds ~160,000 items across five stores (Docs/vm-coverage-analysis.md section 3.3; its largest folder alone is 108,144). So Corpus A on the indexed guest is built at production scale. Testbed/host/New-LiveTestSettings.ps1 refuses to render this guest's settings while the recorded itemCount is below it. The build is deterministic and resumable; at the recorded 24.8 items/s it is roughly 1 h 50 min, plus the indexer's own crawl.",
        "_note": "Corpus A in the layout of Docs/live-tier-on-the-vm.md section 1.3: the store the index tier reads. Fill the nulls in, and add the expected plan, only when a build has actually produced it."
      },
      {
        "corpusId": "vm-unindexed",
        "guest": "OutlookAI-Unindexed",
        "indexed": false,
        "status": "NOT BUILT - reserved name only",
        "seed": null,
        "anchor": null,
        "itemCount": null,
        "_note": "Corpus B: the degraded path, no index frontier, the seven-day fallback window and the sweep and frame measurements."
      }
    ],
    "populations": {
      "_what": "The ids reserved for the curated FIXTURE populations the generator builds into each guest's hub, bystander and identity stores (corpus-build --population hub|bystander|identity; Q70, 2026-09-24; Docs/live-tier-on-the-vm.md section 3b). Same id rules and same reason as above: a population's manifest is corpus-<id>.jsonl, it lands in the same shared pull directory, and it is the only allowlist corpus-teardown will delete from - and the hub is not a store anybody can simply delete and recreate, because it is the dummy account's delivery store. Nothing here is a measurement. The three OutlookAI-Unindexed populations were built ONCE, 2026-09-24, by generator v1, and NOT cleanly (Docs/live-tier-on-the-vm.md section 4.1, step 6): that state survives only in checkpoint CP-11-POPULATIONS-BUILT-WITH-FAULTS, as evidence, and the guest is rolled back before any v2 build - so every status below is what a live run can rely on, which is nothing yet.",
      "_seedAndAnchor": "A population's seed is fixed per id below, so a rebuild reproduces it byte-for-byte. Its anchor is NOT fixed: the hub population is rebuilt against the moment a run starts, because its newest item is what makes the index frontier one minute old, and a frontier younger than the guest's UTC offset is the only kind LiveIndexSearchTests.Staleness_SelfReportsPlausibleFrontier can catch a local-time misreading with. The anchor goes in the manifest header, which is where a rebuild reads it from. The rebuild is a script step, decided 2026-09-24: Testbed/guest/Reset-HubPopulation.ps1, first in every run, and the frontier test fails on a hub it did not rebuild.",
      "_version": "Generator v2 (2026-09-24): besides its dated mail, the hub carries 12 UNDATED items - 4 appointments, 4 contacts and 4 tasks, none with a delivery time - and the bystander 42 (14 of each), so the three LiveOrderKeyCollationTests measure undated rows instead of running weak. No unsent drafts, although the maintainer's decision (question A, option (a)) named them: measured on OutlookAI-Unindexed the same day, a new unsent mail item's first save is filed in the profile's DEFAULT store's Drafts, and a population is never built into the default store, so a draft cannot be made in one without writing into another store first. Dated items are byte-identical to v1's. A v1 manifest's shape key does not match a v2 generator.",
      "assigned": [
        { "corpusId": "hub-indexed", "guest": "OutlookAI-Indexed", "population": "hub", "seed": 8181, "store": "the hub - testHubStoreDisplayName", "status": "NOT BUILT" },
        { "corpusId": "bystander-indexed", "guest": "OutlookAI-Indexed", "population": "bystander", "seed": 8282, "store": "the plain bystander", "status": "NOT BUILT" },
        { "corpusId": "identity-indexed", "guest": "OutlookAI-Indexed", "population": "identity", "seed": 8383, "store": "the identity account's delivery store, once section 2.8b has built it", "status": "NOT BUILT" },
        { "corpusId": "hub-unindexed", "guest": "OutlookAI-Unindexed", "population": "hub", "seed": 8181, "store": "the hub - testHubStoreDisplayName", "status": "NOT BUILT as v2. Built once by v1 with faults, 2026-09-24 - 56 items, every received one without a resolved owner recipient; kept only in CP-11 (section 4.1 step 6)" },
        { "corpusId": "bystander-unindexed", "guest": "OutlookAI-Unindexed", "population": "bystander", "seed": 8282, "store": "the plain bystander", "status": "NOT BUILT as v2. Built once by v1 with faults, 2026-09-24 - 300 items, its 'Inbox' items in the PST's hidden root; kept only in CP-11 (section 4.1 step 6)" },
        { "corpusId": "identity-unindexed", "guest": "OutlookAI-Unindexed", "population": "identity", "seed": 8383, "store": "the identity account's delivery store", "status": "NOT BUILT as v2. Built once by v1 with faults, 2026-09-24 - 8 items, its 'Inbox' items in the PST's hidden root; kept only in CP-11 (section 4.1 step 6). Waits on the identity store's Inbox (section 2.8b, the note of 2026-09-24)" }
      ]
    },
    "open": "Whether vm-indexed and vm-unindexed should share a seed and anchor is STILL NOT SETTLED - it is item 16 of Docs/live-tier-on-the-vm.md section 8, and nothing here decides it. Sharing them makes the two stores directly comparable, which is probably what is wanted; that is a reason, not a decision. The ids are distinct either way, which is the point: two corpora may share every generator parameter and still must not share an id, because the id is what names the POPULATION - one machine's store - and those are two different stores whichever way the seed question goes."
  },

  "expectedPlan": {
    "_note": "What 'corpus-plan' prints for the four parameters in the 'corpus' block - NOT for anything in 'corpusIdConvention', whose corpora do not exist and have no plan. Committed so a rebuilder can tell a correct rebuild from a subtly different one without having the old store to compare against. check-testbed-references.ps1 does not recompute these - running the tool needs a toolchain - but Testbed/guest/Build-Corpus.ps1 prints them beside the live plan.",
    "bodyBytesTotal": 225282619,
    "bodyBytesMean": 11264,
    "bodiesOver24KB": 1428,
    "bodiesOver96KB": 201,
    "bodiesOverSweepCap": 61,
    "receivedRangeUtc": [ "2022-08-20T08:14:56Z", "2026-08-18T23:48:36Z" ],
    "perFolder": { "Inbox": 10912, "Sent Items": 4964, "Deleted Items": 2461, "Junk Email": 1663 },
    "perSizeClass": { "short": 9007, "normal": 6515, "long": 3050, "quoted-thread": 1227, "huge-thread": 201 },
    "perDateBand": { "last-24h": 423, "1d-7d": 1189, "7d-60d": 3417, "60d-1y": 6012, "1y-4y": 8959 },
    "unread": 2761,
    "selectedByWindowDays": { "1": 423, "7": 1612, "30": 3093, "60": 5029, "90": 5647, "365": 11041 }
  },

  "buildCost": {
    "_note": "What building this corpus actually cost on the VM, from the build log recovered with the manifest. The plan is deterministic, so a rebuild should land here again.",
    "itemsPerSecond": 24.8,
    "elapsed": "00:13:25",
    "bodyBytes": 225282619,
    "created": 20000,
    "failed": 0,
    "measuredUtc": "2026-08-19",
    "note": "24.8 items/s is the WITH-MOVE figure: DraftsThenMoveWithSentFlag writes every item twice. The 50.9 items/s quoted elsewhere is the no-move rung."
  },

  "guest": {
    "_note": "Layout of the working directory on the guest. None of this was written down anywhere before 2026-08-24; it was read off the guest itself.",
    "workRoot": "C:\\OutlookAI-Q5",
    "serverExe": "C:\\OutlookAI-Q5\\server\\OutlookAI.McpServer.exe",
    "toolsExe": "C:\\OutlookAI-Q5\\tools\\OutlookAI.RemediationTools.exe",
    "manifestPath": "C:\\OutlookAI-Q5\\corpus-vm2.jsonl",
    "measureOutput": "C:\\OutlookAI-Q5\\measure.jsonl",
    "stagedPayloads": [ "C:\\OutlookAI-Q5\\McpServer.zip", "C:\\OutlookAI-Q5\\Tools.zip" ],
    "scheduledTasks": { "measure": "OutlookAI-Measure" },
    "autoLogonUser": "vmadmin",
    "hasDotnetSdk": false,
    "dotnetRuntimeVersion": "10.0.10",
    "shell": "Windows PowerShell 5.1",
    "provenance": "Reported by the coordinator on 2026-08-24 from a direct read of the guest. The .NET runtime version, the absence of an SDK and AutoAdminLogon=1 for 'vmadmin' were verified there; this agent could not inspect the VM and did not."
  },

  "_vmNameNote": "THE GUEST THIS WAS MEASURED ON, NOT THE GUEST TO BUILD. Everything in this file describes one machine's recorded state, so this name is provenance and stays as it is until the measurements themselves are retaken. Do not read it as a default: OutlookAI-Indexed and OutlookAI-Unindexed are the guests being built as of 2026-09-15, and no script in Testbed/host/ has a VM-name default any more - see Testbed/README.md section 4a. When a new guest's measurements replace these, the whole record changes together and this name changes with it.",
  "vmName": "OutlookAI-TestVM",

  "checkpoints": [
    "CP-01-WIN-CLEAN",
    "CP-02-INSTALLER-STAGED",
    "CP-03-OUTLOOKAI-INSTALLED",
    "CP-04-OFFICE-GOLD",
    "CP-05-ADDIN-TRUSTED",
    "CP-06-PRE-CORPUS"
  ],

  "outlookProfiles": {
    "_note": "Names only. Which is default, how they were created and how the switch is automated are all still unrecorded - see Testbed/README.md, 'What is still unknown'.",
    "known": [ "Outlook", "OutlookAITest" ],
    "provenance": "Docs/autonomous-session-log.md, 2026-08-24: 'the VM already has TWO Outlook profiles, Outlook and OutlookAITest'. The corpus PST's file name ends '- OutlookAITest.pst', which is Outlook's own naming for the profile that created it."
  },

  "liveTestSettings": {
    "_what": "The per-guest values for Testbed/live-test-settings.template.json, one section per guest, keyed by VM name. 'pwsh -File Testbed/host/New-LiveTestSettings.ps1 -VMName <name>' renders that guest's gitignored live-test-settings.json from them. Unlike the file it renders, this section IS committed - which is safe only because a test guest's stores are synthetic: created by this directory's own scripts and named after nothing real. NEVER put a store name from a working Outlook profile here. The maintainer's own settings file stays hand-written and gitignored, and the renderer refuses to write anywhere it could live.",
    "_fill": "Every value that begins '<FILL' is a placeholder, and the renderer REFUSES a guest while any remains, naming each one. They are store names and corpus build parameters that can only be READ OFF THE GUEST - over COM, from the TIER profile the live tier runs under - and nobody has read them yet. Do not type the name you expect to be there; copy the one Outlook reports. Declare only stores that profile mounts: a declared store the running profile does not have is censused, is not found, and refuses the tier. The renderer cannot check that from the host and says so every time it runs.",
    "_blocks": "subjectOnlyProbe, corpus and mailSink take either an object carrying every field, or null for 'this guest has none'. Never a partial object: the loader refuses one and so does the renderer. null leaves the block out of the rendered file, which the loader reads as absent. When this guest's corpus is not built, or its tier profile does not mount the corpus store, set corpus to null. When it IS declared, the renderer holds corpusId, seed, anchorUtc and itemCount against corpusIdConvention above - so a corpus that record still calls NOT BUILT has to be recorded there first.",
    "_decided": "Some values are filled in because they are recorded DECISIONS, not facts to read off a machine. machineProfile is Portable: a guest declares no Production population, and Portable is the profile whose missing populations announce PROVED NOTHING instead of refusing. expectedDelegateStoreDisplayNames is empty: DelegateStore is the one capability no test machine can be given (Docs/live-tier-on-the-vm.md section 5). mailSink is null: the guests get NO sink (section 1.4, decided 2026-09-15), and a sink block pointing at a listener that is not there refuses the whole tier at start. corpus.corpusId is the id corpusIdConvention assigns to this guest, and corpus.windowDays copies Testbed/live-test-settings.example.json. hubPopulationManifestPath is C:\\OutlookAI-Q5\\corpus-<id>.jsonl for the hub id corpusIdConvention.populations assigns this guest, by the same naming rule as every manifest: the hub is rebuilt from it before every run (Testbed/guest/Reset-HubPopulation.ps1, decided 2026-09-24), and the frontier test fails on a hub nobody rebuilt. On the INDEXED guest, probeTerm and three of subjectOnlyProbe's four fields are the population generator's own constants (CorpusPopulation.ProbeTerm, SubjectOnlyTerm, SubjectOnlySenderFragment and the hub's Notices folder) - T1/LiveTestSettingsTemplateTests holds them equal - and only the store, which is the hub, is read off the guest. On the UNINDEXED guest nothing is indexed, so indexedStoreDisplayNames is [], probeTerm is empty and subjectOnlyProbe is null: the index tests are deselected there, and the loader turns any that run anyway into a refusal. Change any of them only with the decision behind it.",
    "_indexedOrder": "indexedStoreDisplayNames on the indexed guest is [hub, bystander, corpus] in THAT order, and the renderer holds it: several index tests read entry 0, which must be the hub whose population carries senders and attachments; the exclude-subfolders measurement reads the first non-hub entry, which must be the bystander whose population has subfolders. Every entry but the corpus must be named as an .invalid address - the product finds a small store in the index only through mail addressed to it, and only for a name shaped like an address. The identity account's store is WATCHED and not in this list: it is written to by the identity tests and holds only its tiny population.",

    "OutlookAI-Indexed": {
      "machineProfile": "Portable",
      "testHubStoreDisplayName": "<FILL: the hub's Store.DisplayName - the dummy account's delivery store, named exactly as that account's SMTP address>",
      "hubPopulationManifestPath": "C:\\OutlookAI-Q5\\corpus-hub-indexed.jsonl",
      "expectedStoreDisplayNames": [ "<FILL: the Store.DisplayName of every store the tier profile mounts and the census must watch - the hub, the bystander, this guest's corpus store and, once it exists, the identity account's store>" ],
      "indexedStoreDisplayNames": [
        "<FILL: the hub's Store.DisplayName - FIRST>",
        "<FILL: the plain bystander's Store.DisplayName, an .invalid address - SECOND>",
        "<FILL: this guest's corpus store's Store.DisplayName - LAST>"
      ],
      "expectedDelegateStoreDisplayNames": [],
      "bystanderStoreDisplayNames": [ "<FILL: the stores no test may write to - the plain bystander and this guest's corpus store. Name each in expectedStoreDisplayNames as well; never the hub, never the identity account's store>" ],
      "probeTerm": "invoice",
      "subjectOnlyProbe": {
        "_note": "The hub population's SF-6 folder (corpus-plan --population hub prints all four). Only the store is read off the guest: it is the hub.",
        "storeDisplayName": "<FILL: the hub's Store.DisplayName>",
        "folderPath": "Inbox/OutlookAI-Corpus-Folder-Notices",
        "subjectTerm": "bulletin",
        "senderFragment": "noticebot"
      },
      "corpus": {
        "_note": "Corpus A in Docs/live-tier-on-the-vm.md section 1.3. Or null - see _blocks above.",
        "storeDisplayName": "<FILL: the corpus store's Store.DisplayName in the tier profile>",
        "manifestPath": "<FILL: where the build wrote the manifest on the guest - by the convention in corpusIdConvention, C:\\OutlookAI-Q5\\corpus-vm-indexed.jsonl>",
        "corpusId": "vm-indexed",
        "seed": "<FILL: an integer, from the manifest's header line>",
        "anchorUtc": "<FILL: yyyy-MM-ddTHH:mm:ssZ, from the manifest's header line>",
        "itemCount": "<FILL: an integer, from the manifest's header line>",
        "windowDays": [ 7, 30, 60 ]
      },
      "mailSink": null
    },

    "OutlookAI-Unindexed": {
      "_readOffTheGuest": "READ, NOT TYPED: every store name below is what Outlook reported over COM in the TIER profile on OAI-UNINDEXED, 2026-09-24 18:20, after the step-6 populations (Docs/live-tier-on-the-vm.md section 4.1): exactly three stores - tier@vm.invalid (C:\\OutlookAI-Tier\\Outlook.pst, the tier account's delivery store), identity@vm.invalid (identity.pst, the identity account's) and bystander@vm.invalid (bystander.pst, no account). The identity store is watched and deliberately NOT a bystander (section 2.8b). corpus is null because this tier profile does not mount Corpus B - it lives in CorpusProfile only - and corpusIdConvention still records vm-unindexed as NOT BUILT. mailSink is filled because Testbed/guest/Install-MailSink.ps1 reported SINK-READY on this guest the same day, loopback, on its default ports.",
      "machineProfile": "Portable",
      "testHubStoreDisplayName": "tier@vm.invalid",
      "hubPopulationManifestPath": "C:\\OutlookAI-Q5\\corpus-hub-unindexed.jsonl",
      "expectedStoreDisplayNames": [ "tier@vm.invalid", "bystander@vm.invalid", "identity@vm.invalid" ],
      "indexedStoreDisplayNames": [],
      "expectedDelegateStoreDisplayNames": [],
      "bystanderStoreDisplayNames": [ "bystander@vm.invalid" ],
      "probeTerm": "",
      "subjectOnlyProbe": null,
      "corpus": null,
      "mailSink": {
        "submitHost": "127.0.0.1",
        "submitPort": 25,
        "retrieveHost": "127.0.0.1",
        "retrievePort": 110,
        "connectTimeoutMs": 2000
      }
    }
  },

  "unrecorded": [
    "Hyper-V generation, Secure Boot, TPM, vCPU, RAM, disk size, checkpoint type",
    "Windows edition, build, ISO, licensing, computer name, Defender exclusions, power policy",
    "Office version, channel, bitness, install method, first-run-wizard suppression",
    "The second Windows account (the unindexed one) - whether it exists yet, and its name",
    "Which Outlook profile is default, and how the switch between them is automated",
    "Whether the three-store layout has been built at all, or whether the VM still has one PST",
    "The mail sink: whether it is installed, which build, and which ports",
    "How results leave the guest today (the recovery on 2026-08-24 was done by hand)"
  ]
}
