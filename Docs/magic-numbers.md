# Magic numbers, hardcoded timing and duplicated values

**What this file is for.** Every number, timeout, threshold and duplicated string in OutlookAI
that a reader could reasonably ask "why that value, and what happens if it changes?" about,
with an answer. It is a standing inventory, not a one-off report: when you add a literal that
someone might one day have to reason about, add a row.

**Why it exists.** Two full audits of the codebase were run for the values below - one over the
VSTO add-in and the build/release infrastructure, one over the MCP server. Together they close
the "**Audit the codebase for other timers and time-based behaviour**" item in `TODO.md`, which
had been open since the update poll was found by accident while something unrelated was being
added, and which explicitly asked for "one pass to find polls that should be events, intervals
that no longer match what they wait for, and anything that assumes wall-clock time moves
forward smoothly".

**How a value gets fixed**, in descending order of strength. A comment is the weakest possible
fix and is the last resort, because the audit found a comment in `Installer.iss` claiming the
Office versions it wrote "match versions checked elsewhere" that had already stopped being
true:

1. one named constant, used everywhere;
2. a value **derived at runtime** from the thing it mirrors - a line count, an array length, a
   sentence built from the number rather than restating it;
3. a **test or build step that fails** when the two sides drift;
4. only then, a comment.

**Status** is one of:

* **Fixed** - the value no longer has the problem the audit found. Says how.
* **Kept - defensible** - the value is right and stays. Says why.
* **Open - needs a decision** - somebody has to choose. Says what the choice is.

**Where the drift guards live.** Cross-file invariants that no compiler can see are checked by
`.github/scripts/check-pinned-constants.ps1`, which runs as the first step of both
`.github/workflows/build.yml` (every pull request) and `.github/workflows/release.yml` (every
release, and again against the certificate actually imported for signing). It is runnable by
hand: `pwsh -File .github/scripts/check-pinned-constants.ps1`. Cross-assembly invariants that a
compilation *can* see are pinned by the T1 test suite instead.

---

## Add-in and build

Scope: root `*.cs`, `Services\`, `TaskPane\`, `Properties\`, `OutlookAI.csproj`,
`Installer.iss`, `.github\workflows\*`.

### Cross-file invariants, and what enforces them

| Invariant | Enforced by | Status |
|---|---|---|
| `UpdateService.ExpectedCertThumbprint` == `OutlookAI.csproj` `ManifestCertificateThumbprint` | `check-pinned-constants.ps1` #1, on every build and release | Fixed |
| ...and both == the certificate the release actually signs with | `check-pinned-constants.ps1` #1b, called from release.yml's "Import signing certificate" step with `-ExpectedSigningThumbprint` | Fixed |
| `ThisAddIn.InstallerMutexName` == `Installer.iss` `SetupMutex` | `check-pinned-constants.ps1` #2 | Fixed |
| `UpdateService.MaxDownloadBytes` == release.yml's installer-size gate | `check-pinned-constants.ps1` #3 | Fixed |
| `OfficeVersions.Supported` == the Office majors `Installer.iss` writes resiliency exemptions for | `check-pinned-constants.ps1` #4 | Fixed |
| `McpRegistrationService.DotnetRuntimeDownloadUrl` == `Installer.iss` `NetRuntime10ManualUrl` | `check-pinned-constants.ps1` #5 | Fixed |
| `McpRegistrationService.Status*` == the add-in status codes `McpServer/README.md` publishes for `outlook_health`'s `registration.addInStatus` | `check-pinned-constants.ps1` #7 (set comparison, both directions) | Fixed |
| `McpRegistrationDecision.ParkX/ParkY` == `ComposeSurface.ParkX/ParkY` (net48 add-in vs .NET 10 server) | T1 `ParkCoordinateMirrorTests` - the one compilation that can see both | Fixed |
| `SectionLines(Preamble)` == line count of `PromptDefaults.Preamble` | derived at runtime; drift is now impossible | Fixed |
| `SectionCount` == `Sections.Length` | derived at runtime; drift is now impossible | Fixed |
| `PromptLint`'s trigger substrings remain present in the shipped `PromptDefaults` text | T1 `PromptLintTests` (pre-existing: asserts the shipped defaults lint clean, per section) | Kept - already enforced |
| Release tag `v<ver>` -> asset `OutlookAI-v<ver>.exe` -> the name `UpdateService` looks for | nothing, but `UpdateService` falls back to any `OutlookAI*.exe` | Kept - defensible |
| `McpRegistrationService`'s `.NET 10` probe (`StartsWith("10.")`) == `Installer.iss`'s (`Copy(...,1,3) = '10.'`) | `check-pinned-constants.ps1` #8 - the prefixes against each other, and the Pascal `Copy` length against the literal it compares to | Fixed |

### Identity, versions and protocol

| Value | Location | What it controls | Justification | Status |
|---|---|---|---|---|
| `2578F7B8…E995` | `Services\UpdateService.cs` `ExpectedCertThumbprint`; `OutlookAI.csproj` `ManifestCertificateThumbprint` | the certificate every downloaded installer must be signed by / the certificate MSBuild signs the ClickOnce manifests with | self-signed cert, so WinVerifyTrust always reports `CERT_E_UNTRUSTEDROOT`; the thumbprint pin is the real check | **Fixed** - the highest-consequence duplicate in the repo. The csproj half failed loudly on a rotation, the updater's half failed **closed and silently** (every future installer rejected, auto-update dead across the installed base, one line in an error label as the only symptom). Now compared by the drift script on every PR and every release, and checked against the imported certificate before the release builds. Both files carry a cross-reference naming the other. |
| `"claude-opus-4-6"` | `Services\ClaudeServices.cs` `DefaultModel` | the model every pane action and signature selection runs on | pinned so behaviour is reproducible; README documented it | **Fixed, and the default is now nothing at all.** It was pinned with no override and no fallback, so a retirement would stop every installed copy working until a release shipped. There is no model id left in `ClaudeServices`: with no stored preference `--model` is omitted entirely and Claude Code resolves it the documented way (its own `model` setting, then `ANTHROPIC_MODEL`, then the account default), so the product follows the user's configuration and future models without an edit here. The `Model` value under `HKCU\Software\OutlookAI` overrides it, written by the Model group on the Claude Code settings tab - a picked alias applies immediately, a typed id commits on Enter, on leaving the box, on Apply now and on close. |
| `16.0` in four registry paths | `Services\OutlookTuningService.cs` | which Outlook hive the search / caching / OST tuning is written into | nothing said it was deliberate | **Fixed** - it was a real defect, not untidiness: on Outlook 2013 (15.0) or a future 17.0 every write landed in a hive Outlook never reads, so the settings dialog showed every value as unset for ever and the user was told to restart Outlook indefinitely. New `Services\OfficeVersions.cs` holds the one list (`16.0, 17.0, 15.0`, newest first) and detects the version actually installed. `ThemeService`'s two probes - the startup read and the live registry watcher - now use the DETECTED version rather than the first `Common` key that opens, so the reader and the watcher can no longer subscribe to different hives, and when no Office is detected the theme comes from Windows rather than from the fallback major; `Installer.iss`'s three resiliency keys are checked against it by the drift script; and the MCP server, which had the same defect read-side, LINKS the same file rather than reimplementing it (see the server section's `16.0 everywhere in the server` row). On a 16.0 machine - the overwhelming majority - behaviour is byte-for-byte unchanged. |
| `"OutlookAISetup"` | `ThisAddIn.cs` `InstallerMutexName`; `Installer.iss` `SetupMutex` | the mutex that makes the add-in skip all startup work while the installer runs | documented on the code side only | **Fixed** - named on the C# side, cross-referenced from both, compared by the drift script. Renaming one side used to evaporate the guard with no error at all. |
| `_MailAutoSig`, `_MailOriginal` | `TaskPane\AITaskPane.cs` `BookmarkAutoSig` / `BookmarkOriginal` | the Word bookmarks that split a compose window into draft / signature / quoted thread - the anchor for every read and write the pane makes | Outlook's own names; not ours to choose | **Fixed** - were ten hand-typed string literals in the one file where a typo silently reads or replaces the wrong region of a real email. Now two `const string`s. |
| `%USERPROFILE%\.local\bin\claude.exe` | `Services\ClaudeServices.cs` `ClaudePath` | where the CLI is expected / whether Claude Code counts as installed | the CLI's own install location | **Fixed** - was spelled two different ways in two files. `McpRegistrationService` now uses `ClaudeService.ClaudePath`. Previously a move would have fixed the executing path (which fails loudly) and left the detecting one quietly reporting the wrong status for ever. |
| `irm https://claude.ai/install.ps1 \| iex` (+ sign-in and restart steps) | `Services\ClaudeServices.cs` `InstallInstructions` | the "how to install the CLI" block in two different error messages | the CLI's documented installer | **Fixed** - one constant, two call sites. |
| `https://dotnet.microsoft.com/download/dotnet/10.0` | `Services\McpRegistrationService.cs`; `Installer.iss` `NetRuntime10ManualUrl` | where a user with no .NET 10 runtime is sent | Microsoft's download page | **Fixed** - compared by the drift script; both sides cross-referenced. |
| `-32000` (`ParkX`/`ParkY`) | `Services\McpRegistrationDecision.cs`; `McpServer\OutlookAI.Core\Com\ComposeSurface.cs` | telling a *parked* Outlook window from a *minimised* one, which decides whether a human can be asked the MCP registration question | Windows' classic off-screen corner; net48 add-in cannot reference the .NET 10 server assembly | **Fixed** - the comment said "the two constants must stay in step" and nothing checked it. T1 `ParkCoordinateMirrorTests` now asserts equality plus the behaviour on both sides of the `/ 2` threshold. Drift meant either asking a question into a headless Outlook nobody can see, or never asking it at all. |
| `StartsWith("10.")` / `Copy(FindRec.Name, 1, 3) = '10.'` | `Services\McpRegistrationService.cs`; `Installer.iss` | .NET 10 runtime detection, exactly 10.x (roll-forward = Minor accepts 10.x, rejects 11.x) | reasoned identically on both sides | **Fixed** - option (b) taken, and it was the last unenforced cross-file invariant in the repo. `check-pinned-constants.ps1` #8 compares the two prefixes textually, which is the only thing that can relate them: Pascal cannot read a C# constant, and this pair's failure is quiet in BOTH directions - setup skipping a runtime install the machine needed, or Settings reporting a missing runtime that is sitting right there. Neither crashes; both just report. The Pascal side is checked a second time, against itself: it spells the prefix as a length plus a literal (`Copy(name, 1, 3) = '10.'`) and those two can disagree, at which point the probe is not wrong but DEAD - `Copy(name, 1, 2)` can never equal `'10.'` and `Copy(name, 1, 4)` never matches a real 10.x folder, so setup would decide the runtime is missing on every machine forever. Proved by breaking all three: C# to `"11."` gives "McpRegistrationService accepts a shared-framework folder starting '11.' but Installer.iss accepts one starting '10.'"; the `.iss` literal to `'11.'` gives the same sentence with the sides swapped; the `Copy` length to `2` gives "Installer.iss compares Copy(FindRec.Name, 1, 2) against '10.', which is 3 characters." Both files now carry a comment naming the other and the script. |
| `78AF2871-…-455552E37C91` | `Installer.iss` `AppId`; `Properties\AssemblyInfo.cs` typelib GUID | Inno's install identity / assembly typelib | deliberate reuse, unstated | Kept - defensible. Harmless coincidence of identity; neither reads the other. |
| `E205060633DD…D068` | `Installer.iss` x2 | the compromised certificate removed at uninstall and on every install | reasoned at length inline, including why by thumbprint and not by subject | Kept - defensible. Same value twice in one file, both occurrences justified in place. |
| `CFE1A40B…` + `41828424` | `.github\workflows\release.yml`; `Installer.iss` comment | the VSTO redistributable's identity | both sides carry it; CI is the enforcement point and fails closed | Kept - defensible. The `.iss` copy is a comment only, so drift there is cosmetic. |
| `Major == 99` / `99.99.99.0` | `Services\UpdateService.cs`; `Properties\AssemblyInfo.cs`; `release.yml` | "developer build - never auto-update" | named in the code with the CI stamp explained | Kept - defensible. |
| revision forced to `0` in the version compare | `Services\UpdateService.cs` | which releases count as "newer" | safe only because `release.yml` rejects `version_bump = 0.0.0`, so the base version always moves | Kept - defensible. The invariant is documented in `CLAUDE.md` rather than beside the code; the compare is correct as long as it holds. |
| `-p - --output-format json --max-turns 1` | `Services\ClaudeServices.cs` | the CLI protocol contract | README quotes it verbatim; `--max-turns 1` is what makes the `error_max_turns` branch reachable, and it is handled | Kept - defensible. |
| `"Microsoft.Outlook.Mail.Compose"`, `"Microsoft.Outlook.Explorer"` | `Ribbon.cs` | ribbon attachment points | fixed Office identifiers | Kept - defensible. |
| `release >= 528040` | `Installer.iss` | .NET Framework 4.8 detection | the canonical 4.8 release key | Kept - defensible. |
| `1,0,1,2` (search) / `0,0,1,1,1,0,0` (caching) | `Services\OutlookTuningService.cs` | the desired registry values | sourced to v3 plan D22/D24/D25 | Kept - defensible, with a caveat: the source (`v3.MD`) is gitignored, so the justification is unavailable to most readers. |
| `v4.8`, `ToolsVersion 18.0`, `VisualStudioVersion 17.0` | `OutlookAI.csproj` | target framework and toolchain | consistent with the installer's 4.8 check and the pinned runner | Kept - defensible. |
| `windows-2022` x3 vs `windows-latest` | workflows | CI runner images | the pin is justified in release.yml: the VS 2026 image dropped the VSTO signing task | Kept - defensible. Only the VSTO jobs need the pin, and only they have it. |
| `checkout@v7`, `dependency-review-action@v5.0.0`, … | workflows | action versions | none | Kept - defensible. One action pinned to an exact patch and the rest to majors is inconsistent, not risky. |

### Timing - timeouts, intervals, polls, delays

| Value | Location | What it controls | Justification | Status |
|---|---|---|---|---|
| `120_000` + the words "2 minutes" | `Services\ClaudeServices.cs` | hard cap on one CLI request; on expiry the process tree is killed and the user gets nothing | README states 2 minutes | **Fixed (configurable, default unchanged)** - one number and two spellings of it sat six lines apart. Now `DefaultRequestTimeoutSeconds = 120`, with the user-facing sentence built by `DescribeRequestTimeout()` from the value actually in force. Overridable per machine by the `RequestTimeoutSeconds` DWORD under `HKCU\Software\OutlookAI`, clamped to 10 s - 1 h so a typo cannot disable the cap. **The default did not change.** |
| `TimeSpan.FromMinutes(5)` as `HttpClient.Timeout` | `Services\UpdateService.cs` | shared by the GitHub API GET **and** the <=50 MB installer download | none | **Fixed** - it was a timeout that could be exceeded by the operation it guarded (50 MB in 5 min = ~170 KB/s sustained, or the update aborts), and the cap it guarded was declared 180 lines away with no reference between them. The ambient timeout is gone (`Timeout.InfiniteTimeSpan`); each request now carries its own `CancellationToken`. See the two rows below. |
| `ApiTimeout = 5 min` | `Services\UpdateService.cs` | the GitHub API GET only | generous for a JSON GET | Kept - defensible. Deliberately the same five minutes the ambient timeout used to give it, so the API half's behaviour is unchanged. The default completion option buffers the body inside `SendAsync`, so the whole API call really is inside this. |
| `DownloadTimeout` = `MaxDownloadBytes / MinDownloadBytesPerSecond` = 800 s | `Services\UpdateService.cs` | the installer download, end to end | derived | **Fixed** - derived from the cap it guards and a declared 64 KB/s floor, so it can no longer be exceeded by the operation it bounds. |
| the `ResponseHeadersRead` copy loop | `Services\UpdateService.cs` | reading the installer body by hand | the audit could not confirm it was covered by any timeout | **Fixed** - investigated and closed by construction rather than by deciding where net48 draws the line. The download token is passed to `GetAsync`, to every `ReadAsync` and to every `WriteAsync`, **and** registered to dispose the response when it fires - disposing the response tears down the content stream, which is what makes a stalled read actually fail. This matters because the one-at-a-time guard is released only in the `finally`: a hung copy loop used to leave both version indicators on "checking…" for the rest of the session. A dedicated `catch (OperationCanceledException)` reports "Update check timed out." instead of "The operation was canceled." |
| `TimeSpan.FromMinutes(10)` + "every 10 minutes" x4 in English | `Services\UpdateService.cs` `PollInterval` | the update poll | four prose copies, one of them a tooltip on screen | **Fixed** - `UpdateService.PollIntervalDescription` builds the sentence from the `TimeSpan`; the sidebar tooltip now asks for it. The two code comments and the doc comment name `PollInterval` instead of restating it. **`README.md:147` still says "every 10 minutes" and is outside this pass's ownership - see "What the docs sweep must say".** |
| `TimeSpan.Zero` due time + no backoff | `Services\UpdateService.cs` `Start()` | a network call fires during Outlook startup; an offline machine retries for ever | none | Open - needs a decision. Not a magic number, but found by the same audit. Options: (a) leave it - one conditional-GET per ten minutes is negligible; (b) delay the first check ~30 s so it is not competing with Outlook's own startup; (c) back off after N consecutive failures. |
| `1000` ms version tick, in two files | `Services\UpdateService.cs` `VersionLineTickMs`, used by `AITaskPane` and `SettingsDialog` | the "checked 4m ago" rollover, and picking up a check that finished in the *other* indicator | `TODO.md` said the rollover wants 30-60 s | **Fixed, and now `30000`.** The number lives on `UpdateService.VersionLineTickMs` so the two indicators cannot be tuned apart, and the reason it could not be slowed is gone: `UpdateService.StateChanged` now carries every state change to both indicators, including `IsChecking`, so a check started in one is visible in the other immediately rather than within a second. The timer's only remaining job is the "4m ago" to "5m ago" rollover, whose text has one-minute resolution, and it goes through the same geometry gate so a text-only change never causes a relayout. |
| `FirstLookDelayMs 5000`, `LaterLookDelayMs 60000`, `MaxLooks 6`, "~5 minutes" | `TaskPane\McpRegistrationPrompt.cs` | how long the MCP registration question stays available for a background Outlook | all three justified | **Fixed** - the "~5 minutes" was a product computed by hand from the other two, which changing either would have falsified silently. Inverted: `LookWindowMs = 5 minutes` is now the decision and `MaxLooks` is derived from it by ceiling division. Evaluates to 6, exactly as before. |
| `Get-CimInstance Win32_Process`, unbounded, at `ssInstall` | `Installer.iss` `StopRunningMcpServers` | stopping running MCP servers before any file is replaced | the *why* is documented at length; the unboundedness was not | **Fixed - UNVERIFIED, see below.** `-OperationTimeoutSec 30` added. Synchronous, at `ssInstall`, with `/NOCANCEL` in force on the auto-update path, so a wedged WMI/CIM service hung setup with no progress and no cancel. Bounded, the worst case is a stale server left running and `CloseApplications` taking over - the same best-effort outcome every other failure here already has. |
| `Invoke-WebRequest` with no `-TimeoutSec` | `Installer.iss` `DownloadFile` | fetching the .NET 10 runtime | none | **Fixed - UNVERIFIED, see below.** `-TimeoutSec 600` added. Ten minutes is far longer than the ~30 MB payload needs on any usable link, and a timeout is already reported gently (the runtime is needed only by the mail server, so setup carries on). |
| `Start-Sleep -Seconds 2` | `Services\UpdateService.cs` `InstallerGraceSeconds` | grace after Outlook exits, before the installer runs, so the add-in DLLs are released | none | **Fixed as a literal; value unchanged.** Named and explained. Still a guess, and still the one called out in `TODO.md`: too short and the silent install fails, with the next check retrying as the only recovery. |
| `WaitForExit(1500)` | `Services\ClaudeServices.cs` `WarmUpProbeMs` | probe for immediate exit = missing prerequisite, at warm-up | the comment justified the *cap*, not the value | **Fixed as a literal; value unchanged.** A cold machine where `claude.exe` takes longer than this to fail leaves the prerequisite undetected until first use - acceptable, because first use then reports it. |
| `WaitForExit(5000)` | `Services\ClaudeServices.cs` `TaskKillWaitMs` | `taskkill /T /F` before falling back to `proc.Kill()` | none | **Fixed as a literal; value unchanged.** A fallback path exists, so an overrun degrades rather than fails. |
| `EmptyReadAttempts = 3`, `EmptyReadPauseMs = 50` | `Services\McpRegistrationService.cs` | retry window for a config file the CLI is mid-truncate on | extensively, including the ~100 ms worst case | Kept - defensible. The model of how to do this: named, bounded, product stated, and the fact that it runs on the settings dialog's thread acknowledged. |
| `t.Join(2000)` | `Services\ThemeService.cs` | joining the OfficeThemeWatcher thread at shutdown | none | Kept - defensible. `stop.Set()` runs first, the thread is `IsBackground`, and a failed join deliberately leaks the event rather than racing a dispose. |
| `60` s / `60` min / `24` h + `DateTime.Now` | `Services\UpdateService.cs` `DescribeState` | the wording of "checked just now / 4m ago / 3h ago / 2d ago" | the negative clamp is justified; the thresholds are self-evident units | **Fixed - the thresholds stay, the clock changed.** The units were always fine; the `DateTime.Now` arithmetic was not, and the `if (ago < TimeSpan.Zero) ago = TimeSpan.Zero` clamp was the symptom rather than the fix - a backwards jump made "4m ago" read as an hour, as two days, or as a negative interval. `UpdateService` now measures from a process-lifetime `Stopwatch` (`_sinceStart`), so the line can only count forwards and the clamp is gone. |
| `7` clicks in `3` seconds | `TaskPane\AITaskPane.cs` `DebugClicksRequired` / `DebugClickWindowSeconds` | the debug-mode easter egg | the comment restated both | **Fixed as literals; values unchanged.** Both named; the comment now names the constants. Now measured with a `Stopwatch`, so a clock jump can no longer make the window enormous or negative. |
| `timeout-minutes: 30 / 20 / 20 / 30`, `cron: '23 5 * * 1'`, `Start-Sleep -Seconds 5` between timestamp servers, `$daysLeft -lt 30` | `.github\workflows\*` | CI job caps, the weekly CodeQL scan, the timestamp-server retry pause, the certificate-expiry warning | conventional; the last two are commented | Kept - defensible. |

### Limits, caps and truncation points

| Value | Location | What it controls | Justification | Status |
|---|---|---|---|---|
| `SectionCount = 4` beside a 4-element `Sections[]` | `TaskPane\SettingsDialog.Prompts.cs` | every loop over the prompt editors | none | **Fixed** - now `Sections.Length`, declared after the array (static initialisers run in textual order, so the other way round it reads 0). Two constants encoding one fact was a trap at both ends: a fifth section added without bumping the count vanished from the window, a bumped count without the array entry threw `IndexOutOfRangeException` on load. |
| `12` lines in the Preamble editor | `TaskPane\SettingsDialog.Prompts.cs` | how much of the preamble is visible before it scrolls | justified precisely - and the justification was the danger | **Fixed** - now `LineCount(PromptDefaults.Preamble)`. The comment said a 10-line box would hide the no-trace-of-AI rule, which is the last line; a hand-counted 12 would have re-created that exact fault, silently, the first time a 13th line was added. |
| `10` lines in the Signature-selection editor | `TaskPane\SettingsDialog.Prompts.cs` | same, for `PromptDefaults.SignatureSelection` (13 lines) | none | Kept - defensible, now with a reason attached. Deliberately fewer than the text's line count: it is the longest block, its head is its whole point, and a box tall enough for all of it would push the rest of the tab off a laptop screen. It scrolls. |
| `50 * 1024 * 1024` / `-gt 50MB` | `Services\UpdateService.cs` `MaxDownloadBytes`; `.github\workflows\release.yml` | largest installer the updater accepts / the release gate that refuses to ship a bigger one | yes, and cross-referenced by name | Kept - defensible, now also mechanical. This was already the house model for a duplicated limit; the drift script now compares the two numbers as well, so the gate cannot quietly stop matching the cap. The local `const` became a class-level `MaxDownloadBytes` so the workflow comment names something real. |
| `500` x3 and `300` x2 | `Services\ClaudeServices.cs` `RawOutputExcerptChars` / `ErrorTextExcerptChars` | how much raw CLI output or stderr an error message quotes | none | **Fixed as literals; values unchanged.** One concept, two budgets, five literals, no constant - now two named constants with the difference between them explained (raw JSON needs more room than a message). |
| `200` / `300` x2 / `60` | `TaskPane\AITaskPane.cs` `DebugBookmarkExcerptChars` / `DebugTextExcerptChars` / `AnswerExcerptChars` | debug-log bookmark text, debug-log draft and result, an unmatched signature name shown to the user | none | **Fixed as literals; values unchanged.** Four truncation points with four different values, each with its own hand-written `Substring` expression. Now three named constants and one `Excerpt()` helper, so the "..." that says a cut happened cannot be forgotten at one site and not another. |
| `i <= 20` recipients | `TaskPane\AITaskPane.cs` `MaxRecipientsInPrompt` | recipients included in the signature-selection prompt | the doc comment said "(capped)" without saying to what | **Fixed - the cap is gone.** It truncated silently, so the model reasoned about an audience it was told was complete. The cap's stated rationale was also wrong about Outlook: an unexpanded distribution list is ONE recipient, its own name, so the "a DL would swamp the prompt" case cannot arise without the user expanding it by hand. Every recipient is now sent, which needs no "(and N more)" marker because nothing is cut. |
| `102400` (100 GB) / `96256` (~94 GB) | `Services\OutlookTuningService.cs` `OstMaxDefaultMb` / `OstWarnDefaultMb` | the OST size cap and warn threshold, in the megabytes Outlook stores | sourced to v3 plan D25 | **Fixed** - named, and the settings dialog's `(100 GB max)` caption is now built by `OutlookTuningService.DescribeOstMaxSize()` from the **desired value actually in the registry**. The desired numbers are deliberately tunable, so a hand-written caption stated a limit the product might have stopped applying. |
| `MaxButtonNameLength = 64` | `Services\PromptDefaults.cs` | longest quick-button name | yes ("to keep a label readable in the pane"); enforced in `PromptStore.Validate` and as `_txtName.MaxLength` | Kept - defensible. The three comments in `AITaskPane.cs` that restated "64" in prose now name the constant instead. |
| `ExcerptMaxChars = 160` | `Services\SignatureStore.cs` | signature excerpt length sent to the model | XML doc | Kept - defensible. |
| `MaxDepth = 200` | `Services\McpConfigEditor.cs` | JSON nesting limit in the validator | yes ("so a hostile/broken file cannot blow the stack") | Kept - defensible. |
| `MaxJsonLength = int.MaxValue` | `Services\ClaudeServices.cs`, `Services\McpRegistrationService.cs` x2 | `JavaScriptSerializer` limit | partially | Kept - defensible. Deliberate: `~/.claude.json` is large, and these are local files only. |
| `pass < 4` | `TaskPane\AITaskPane.cs` `LayoutPane` | layout re-entrancy cap | thoroughly | Kept - defensible. Bounded because a scrollbar needed at one width and not the other would otherwise alternate for ever. The comment no longer states the scrollbar's width as "17px" - that was a 96-DPI system metric quoted as prose, which is ~25 px at 150%. |
| `ServerSearchParentLevels = 4` | `Services\McpRegistrationService.cs` | directory levels walked upward looking for the installed server | the comment said the real layout is 2 levels up and "is not worth hard-coding", then hard-coded 4 | **Fixed** - named, and the doc comment now says what it actually is: a search bound, not a statement about the layout. Running out of levels returns `null`, which every caller already handles. |
| `Stream.Size >= 1000000` | `Installer.iss` `IsWindowsExecutable` | the 1 MB floor in the MZ/PE check before executing a downloaded prerequisite | at length, reasoned from the real payload sizes, with the v3.0.1 incident recorded inline | Kept - defensible. |
| `new byte[81920]` | `Services\UpdateService.cs` | download copy buffer | the .NET default `CopyTo` size - now said so in a comment | Kept - defensible. |
| `suffix < 1000` | `TaskPane\SettingsDialog.Prompts.cs` | "New button", "New button 2", … before giving up | none | Kept - defensible. |
| `SchemaVersion = 1` | `Services\PromptStore.cs` | registry layout stamp | yes ("Nothing reads it yet") | Kept - defensible. |

### Layout geometry

Most pixel values in the add-in are correct by construction and are not listed individually.
Three mechanisms do the work: `UiScale.ScaledFor(font, designPixels)` with `DesignLineHeight`
as the single 96-DPI baseline (`ThemedTabControl`); eight **named** design constants scaled by a
`UiScale` probe read back off a Designer literal (`AITaskPane`); and dock/anchor plus
`TableLayoutPanel` with **measured** wrapped-label heights (`SettingsDialog`). Only these stood
out.

| Value | Location | What it controls | Justification | Status |
|---|---|---|---|---|
| `280`, four identical bare literals | `ThisAddIn.cs` -> `AITaskPane.PreferredHostWidth` | the width the host `CustomTaskPane` is given | none, and unscaled | **Fixed** - the only user-visible geometry in the add-in that ignored `UiScale`. At 150% display scaling the pane's *contents* grew by half while the pane stayed 280 units wide, which is exactly the clipping the whole `LayoutPane`/`UiScale` machinery exists to prevent. Now one `PreferredHostWidth`, derived as `Scaled(DesignWidth + TaskPaneChromeWidth)`. `DesignWidth = 260` is named and the Designer assigns `this.Size` from it, the same bargain `PaneMargin` and `QuickActionsDesignTop` already make. At 100% it is still 280. |
| `Blend(resting, Accent, 0.22)` | `TaskPane\ThemedTabControl.cs` | hover tint on an unselected tab | none - while its sibling `0.35` one method away carries measured colour distances | Open - needs a decision. Either measure and justify it the way `RestingFill`'s `0.35` is justified, or accept it as a taste value. Left alone: changing it changes what the window looks like. |
| `Blend(face, Border, 0.35)` | `TaskPane\ThemedTabControl.cs` | resting fill of an unselected tab | exemplary - one rule for both themes, with the measured numbers that forced it | Kept - defensible. The model for how a ratio should be justified. |
| Office UI Theme `4` = dark; `0`,`3`,`5` = light; `6`/other falls through | `Services\ThemeService.cs` | dark-mode detection | yes - all five values listed | Kept - defensible. |
| `Scaled(16)` / `Scaled(39)` window chrome | `TaskPane\SettingsDialog.cs` | keeping the window inside the work area before its handle exists | documented as a hand-measured Windows 11 estimate | Kept - defensible. |
| `lines * (line + 1) + Scaled(8)` | `TaskPane\SettingsDialog.Prompts.cs` | editor height from a line count | the `+ 1` per line is an unexplained inter-line fudge; the `Scaled(8)` is border + padding | Kept - defensible. Both scale correctly. |
| `for (i < 6)` rows / `RowStyles[3] = Percent(100F)` / `Controls.Add(_txtPrompt, 0, 3)` | `TaskPane\SettingsDialog.Prompts.cs` | three statements that must agree "row 3 is the prompt editor" | none | Open - needs a decision. Inserting a row above it breaks the fill silently. A named `PromptEditorRow` constant would fix it; not done here because it touches layout code another pass was working in. |
| `AutoScaleDimensions = (6F, 13F)` vs `DesignLineHeight = 15` | `AITaskPane.Designer.cs` vs `ThemedTabControl.cs` | two different 96-DPI baselines in one product | self-consistent, because `AITaskPane.UiScale` reads the *applied* factor back rather than assuming one | Kept - defensible. Worth knowing before anyone "unifies" them. |

---

## What is deliberately still open

Three rows above are marked **Open**. One is a behaviour decision nobody should take
unilaterally (the first-check-at-startup timing); two are tidiness with a visual consequence
(the `0.22` hover blend, the row-3 coupling).

This paragraph used to say six, and named three things that were already closed by the time it
was read: the silent recipient truncation had been removed outright, and `DateTime.Now` for
elapsed time had been replaced by a `Stopwatch` in both places the add-in used it
(`UpdateService.DescribeState` and the debug-click window) - both rows above said so while this
count still said otherwise. It is written out here rather than quietly corrected because a
summary that counts rows is itself a duplicated value, and this is the failure mode the whole
document is about. The `.NET 10` version prefix, the sixth, is the one this pass closed.

## Configuration escape hatches added by this pass

Both are registry-only, both default to exactly the previous behaviour, and neither has any UI.
They exist so that a value whose failure mode is "the product stops working entirely" can be
worked around without waiting for a release.

| Value | Type | Default | Effect |
|---|---|---|---|
| `HKCU\Software\OutlookAI\Model` | String | absent -> **no `--model` argument at all**, so Claude Code resolves the model itself | the model the CLI is asked for. Written by the Model group on the Claude Code settings tab: a picked alias applies immediately, a typed id commits on Enter, on leaving the box, on Apply now and on close. Accepted only if it is letters, digits, dots, dashes, underscores and square brackets (the last for `opus[1m]` / `sonnet[1m]`) - it goes inside a quoted `--model "..."` argument, and anything else is refused rather than passed on. No model id is hardcoded anywhere any more |
| `HKCU\Software\OutlookAI\RequestTimeoutSeconds` | DWORD | absent -> `120` | seconds one request may take; clamped to 10 - 3600, so a typo cannot disable the cap or make it useless |

---

## MCP server

Scope: `McpServer\OutlookAI.Core\`, `McpServer\OutlookAI.ComHost\`, `McpServer\OutlookAI.McpServer\`,
`McpServer\OutlookAI.RemediationTools\`, `McpServer\OutlookAI.McpServer.Tests\`,
`McpServer\Directory.Build.props`, `McpServer\README.md`.

**The one structural change this pass made.** The server is two assemblies with a one-way
dependency: `OutlookAI.ComHost` references `OutlookAI.Core`, never the reverse. `ComHostPolicy`
owns the OUTER budgets (the deadline after which a wedged COM host is killed); `MailService`
owns the INNER ones (how long a scan or a sweep may run inside it). Because Core cannot see
ComHost, every one of those relationships was previously carried by two literals in two
assemblies that happened to agree - which is precisely how an inner budget came to EQUAL its
outer one. `OutlookAI.Core\Com\ComOperationBudgets.cs` is now the single source, in Core so both
tiers can reach it, and both tiers derive from it. Nothing in it may be edited casually: each
value is either measured or derived from one that is.

### Cross-file invariants, and what enforces them

Almost all server-internal, so almost all enforced by the T1 suite (one compilation sees every
assembly) rather than by `check-pinned-constants.ps1`. The two exceptions are the vocabularies
`outlook_health` PUBLISHES: those have a second copy in Markdown, which no compilation can read,
so the drift script compares them instead.

| Invariant | Enforced by | Status |
|---|---|---|
| `HealthReporting.Registration*` == the `registration.status` values both READMEs publish | `check-pinned-constants.ps1` #6 - the root README as a SET (an undocumented ADDITION fails), `McpServer/README.md` by CONTAINMENT (a RENAME fails) | Fixed |
| add-in `McpRegistrationService.Status*` == the `registration.addInStatus` values `McpServer/README.md` publishes | `check-pinned-constants.ps1` #7 | Fixed |
| `MailService.ExhaustiveTimeBudgetMs` < `ComHostPolicy.DefaultOperationDeadlineMilliseconds`, by exactly `ResultReturnHeadroomMs` | T1 `BudgetCompositionTests.ExhaustiveScanBudget_LeavesHeadroomInsideTheOperationDeadline` | Fixed |
| the `search.exhaustive` tool description quotes the real exhaustive budget | T1 `BudgetCompositionTests.ExhaustiveBudget_IsQuotedTruthfullyInTheToolSurface` + T3 `SearchSchemaCiTests` (both derive the phrase from the constant) | Fixed |
| the `outlook_health` description quotes the real COM probe budget | T1 `BudgetCompositionTests.HealthProbeBudget_IsOneValueAndIsQuotedTruthfully` | Fixed |
| `MailService.HealthProbeBudgetMs` == `ComHostPolicy.HealthProbeDeadlineMilliseconds` | same constant; T1 asserts it too | Fixed |
| `ComHostSupervisor`'s handshake budget == `ComHost\Program.ConnectTimeoutMs` (the two ends of one pipe) | T1 `BudgetCompositionTests.HandshakeBudget_IsSharedByBothEndsOfThePipe` (reads the child's private const reflectively) | Fixed |
| `MailService.SearchBudgetMs` == index timeout + sweep budget, and fits the operation deadline | T1 `BudgetCompositionTests.SearchBudget_IsComposedFromItsPartsAndFitsTheOperationDeadline` | Fixed |
| `WsSqlBuilder.MaxTermLength` == `ExhaustiveDaslFilter.MaxTermLength` (index tier vs COM tier) | T1 `SearchTermLimitParityTests` - drives BOTH builders at the boundary and one past it | Fixed |
| `HealthReporting.ExpandEnvironmentReferences` == add-in `McpConfigEditor.ExpandEnvironmentReferences` | T1 `EnvironmentExpansionParityTests` - one 27-case corpus through both, character for character | Fixed |
| `MailService.MinRawEntryIdHexChars` == 2 x `EntryIdCodec.MessageEntryIdLength` | T1 `BudgetCompositionTests.RawEntryIdFloor_IsDerivedFromTheMapiEntryIdLength` | Fixed |
| `MailService.StoreDetailsCacheTtl` == `FolderPathCacheTtl` | same constant; T1 `BudgetCompositionTests.StoreDetailsAndFolderPathCaches_ShareOneTimeToLive` | Fixed |
| `SearchSchemaCiTests`' truncation assertion == `DescriptionBudgetCiTests.ClientTruncationBudget` | same constant (`internal`); both sides now count UTF-16 code units, so the `IndexOf` offset and the budget are directly comparable | Fixed |
| the MCP protocol version the T3 client SENDS == the one the server negotiates | T3 `McpStdioClient` compares them and throws naming both | Fixed |
| `ComposeSurface.ParkX/ParkY` == add-in `McpRegistrationDecision.ParkX/ParkY` | T1 `ParkCoordinateMirrorTests` (see the add-in half) | Fixed |
| `HealthReporting.TuningKeyPath` / `McpKeyPath` == the add-in services they mirror | one definition - `Services\AddInServerContract.cs`, linked into `OutlookAI.Core` | **Fixed.** Both key paths AND all fifteen value names the two sides exchange now have a single definition. Proved by renaming it: the net48 add-in and both Core targets fail to compile together. Must stay `internal` and must NOT be linked into `OutlookAI.McpServer` - that project has `InternalsVisibleTo` for the tests, so a copy there would be visible to the linked one and collide (CS0436, an error here) |
| the exception types the COM layer RAISES == the `case nameof(...)` list in `ComHostErrorMapper` | `check-pinned-constants.ps1` #10 - collects every `throw new` under `OutlookAI.Core\Com`, `OutlookAI.Core\Text` and `OutlookAI.ComHost\Host`, and fails naming any type the mapper has no case for | **Fixed.** The third invariant here that the drift script rather than T1 enforces, and for the same reason as the other two: the second copy is not a constant anywhere, it is spread across 84 `throw new` statements in 26 files, so no compilation can relate the two halves. A type the switch does not name is not lost but DEMOTED - it arrives as `ComHostRemoteException`, and every layer that branches on exception type stops recognising it (`OutlookTools.GuardAsync` picks its advice that way; `ComGateway` keys its disconnect rebuild on `COMException`). Proved by breaking it: adding a `throw new FileNotFoundException` to `ComErrorTokens.cs` gives "the COM host raises FileNotFoundException but ComHostErrorMapper has no case for it". `ComHost\Protocol` is deliberately out of scope - a framing failure never becomes a `ComHostError`, it takes the host down (see TODO) |
| every `IOutlookSession` operation delivers its failure across the process boundary unflattened | T1 `ComHostErrorFidelityTests` - the whole child side over an in-process pipe, both channels (thrown exception AND `out string? error`), enumerated from the interface by reflection with the count asserted | **Fixed.** The complement to the row above: that one keeps the TYPE list honest, this one keeps the TRANSPORT honest. Proved by reinstating the 2026-08-18 defect (routing-proxy unwrap removed and the server's peel reduced to one layer): 19 of 20 cases fail |
| the live-tier capability vocabulary in `T1/LiveTierInventoryTests.ProductionOnlyCapabilities` == the values `Docs/live-tier-on-the-vm.md` documents | `check-pinned-constants.ps1` #11 - parses the C# array and requires each value to appear backticked in the runbook | **Fixed.** The fourth invariant the drift script rather than T1 enforces, for the usual reason: the second copy is Markdown. It matters because the runbook is what somebody reads to decide whether a live test can move off the maintainer's machine, and a capability documented nowhere reads as one fewer reason a test is pinned there. The C# side is already self-enforcing (a `Requires` value outside the vocabulary fails `EveryRequiresValue_IsInTheVocabulary`), so only the Markdown half needed a mechanism. Proved by breaking it: unbackticking `SmallHubStore` in the runbook gives "LiveTierInventoryTests allows SmallHubStore but Docs/live-tier-on-the-vm.md never mentions it" |
| tool-description prose vs the caps it restates (`1-100`, `max 255`, `Max 20 files and 150 MB`, ...) | nothing; `DescriptionBudgetCiTests` measures description LENGTH (and, since 2026-08-18, that no shipped string carries the client's truncation marker), never the accuracy of the numbers inside | Open - see row below |

### Timing - budgets, deadlines, backoffs, TTLs

| Value | Location | What it controls | Justification | Status |
|---|---|---|---|---|
| `300_000` (was `120_000`) | `ComOperationBudgets.OperationDeadlineMs`, used by `ComHostPolicy.DefaultOperationDeadlineMilliseconds` | hard deadline for one COM contract call; on expiry the host is killed | "generous but finite, which is the entire point" - and now a measurement | **Fixed as the single source; RAISED 2026-08-19.** It is the HANG DETECTOR for every tool except the exhaustive scan, so it has to be unreachable by slow-but-working work and reachable by a wedge. Measured on the maintainer's real profile (5 stores, largest 108,144 items): a whole-store 7-day sweep is 36.6 s and an Inbox-with-subfolders exhaustive scan 66.5 s - so 120 s was under 2x the slowest HEALTHY operation observed, which is not a hang detector, it is a second work limit wearing one's clothes. 300 s is ~4.5x that and holds the composed search shape (`SearchBudgetMs` = 240 s) with room. **The cost, stated plainly:** a genuinely wedged Outlook now takes 5 min to be recognised and up to 10 min for the breaker to open. That cost is paid only by a wedge, because since the same date an expiring caller-declared WORK budget no longer counts toward the breaker at all (row below). |
| `600_000` (was `105_000`, was `120_000`) | `MailService.ExhaustiveTimeBudgetMs`, derived as `ExhaustiveScanDeadlineMs - ResultReturnHeadroomMs` | how long `exhaustive: true` scans before it stops and returns partial results | derived from its own deadline class, and the class is measured | **Fixed 2026-08-18, RAISED 2026-08-19 - and both changes change behaviour.** The fix: the inner soft budget EQUALLED the outer hard deadline, so the documented graceful outcome was unreachable whenever the scan actually ran long - the scan stops only once elapsed has *passed* its budget and then still has to serialize its result set over the pipe, while the watchdog fires at `>=`. The caller got a `Timeout`, the host was killed, and two such searches opened the breaker. The raise: 105 s was the BINDING constraint on completeness rather than a backstop - measured on the real profile, a 60-day whole-store scan reached 3 folders of 32 before it expired. It now derives from `ComOperationBudgets.ExhaustiveScanDeadlineMs` (row below) instead of from the shared operation deadline, so the scan gets ten minutes without every other tool getting a ten-minute hang detector. **Before:** a wide scan returned 3 of 32 folders. **After:** ~6x the scanning time, with the same graceful partial-results answer at the end of it. |
| `615_000` | `ComOperationBudgets.ExhaustiveScanDeadlineMs` -> `ComHostPolicy.ExhaustiveScanDeadlineMilliseconds`, dispatched through the new `ComHostOperationClass.ExhaustiveScan` | hard deadline for one exhaustive scan, separate from every other operation | derived: the scan budget asked for, plus the return trip | **New 2026-08-19.** The alternative was dragging `OperationDeadlineMs` to 615 s, which would have made `read`, `new_draft`, `list_folders` and `move_mail` each wait 10.25 min before a wedged Outlook is reclaimed, and over 20 min for the breaker to open. The per-class mechanism already existed for `Connect` and `HealthProbe`; this is the third member and the reason the mechanism was worth having. T1 pins that the class deadline EXCEEDS the ordinary one - equal or shorter and the class buys nothing and should not exist. The scan is also the one operation whose budget expiry is a documented ANSWER rather than an incident, which is what makes a long work limit correct here and nowhere else. |
| `15_000` | `ComOperationBudgets.ResultReturnHeadroomMs` | the reserve an inner budget leaves for the return trip; used by the exhaustive scan and, since 2026-08-19, by the freshness sweep | sized against up to `SearchTopCap` briefs plus one more folder's `Restrict` after the last budget check | Kept - defensible, and deliberately NOT scaled when the budgets around it were raised. What it covers is the size of one answer and the cost of one more unit of work, neither of which is a function of how long the work was allowed to run: the result set is still capped at `SearchTopCap`, and the sweep is still capped per folder and by `SweepBodyBytesBudget`. Scaling it "proportionally" would have reserved 77 s of a 615 s scan for a frame measured at 432 KB. |
| `180_000` (was `90_000`) | `ComOperationBudgets.ConnectDeadlineMs` | establishing the COM session, possibly cold-starting Outlook | measured 2026-08-15: attach + health 1.0 s, cold search 6.2 s; a large OST on a slow disk is far slower | Kept as the best-measured budget in the tree; RAISED 2026-08-19 to keep it at 60% of the operation deadline, which is the ratio it already had. The measurement did not change; the margin over it did. A cold start on a ~50 GB profile is the case nobody has timed, and paying two extra minutes once beats reporting a start failure that was really a slow disk. |
| `5_000` | `ComOperationBudgets.HealthProbeDeadlineMs` -> `ComHostPolicy.HealthProbeDeadlineMilliseconds` **and** `MailService.HealthProbeBudgetMs` | how long `outlook_health` waits on COM before degrading | "short on purpose: outlook_health must answer even when Outlook is wedged" | **Fixed as a triplicate.** Was three independent declarations - two constants plus the words "gives up after 5 s" in the tool description - with nothing relating them. Now one constant, and a T1 test derives the description phrase from it. |
| `30_000` | `ComOperationBudgets.HandshakeBudgetMs` -> `ComHostSupervisor` + `ComHost\Program.ConnectTimeoutMs` | the COM host pipe handshake, both ends | none, on either end | **Fixed as a duplicate AND as a defect.** The parent consumed it TWICE (once waiting for the pipe, once for the ready event), so a slow child start cost up to **60 s** before any deadline applied, outside the deadline system entirely and unshortenable by `OUTLOOKAI_COMHOST_DEADLINE_MS`. It is now one budget across both halves, shared with the child, and derived from the triggering operation's deadline by `ComHostPolicy.HandshakeBudgetFor`. **Before:** up to 60 s of unbudgeted start on a health call with an explicit 5 s probe budget. **After:** at most 30 s, and at most 10 s when the deadline override is short. |
| `10_000` | `ComHostPolicy.HandshakeFloorMilliseconds` | floor under the handshake when the operation deadline is shorter - unless the CALLER declared that shorter budget | the CI override case, plus (2026-08-19) the rule that an explicit budget outranks a floor | **Kept as a value; FIXED as a defect 2026-08-19.** `outlook_health`'s description promises "gives up after 5 s" and it asks the gateway for exactly 5 s - but `HandshakeBudgetFor(5000)` returned this floor, and the handshake runs BEFORE the deadline clock starts, twice over because health makes two gateway calls. So the one tool that must always answer had the longest cold-host preamble in the product. An explicit budget already outranks the cold-start CONNECT floor two lines away in `ComHostSupervisor.InvokeCoreAsync`; it now outranks this one on the same terms and with the same `allowConnectFloor` opt-out (the freshness sweep keeps paying for a cold start). The floor is untouched for the only caller that shortens a deadline WITHOUT declaring its own budget - the CI suite via `OUTLOOKAI_COMHOST_DEADLINE_MS`, which is testing the timeout path rather than the start path. |
| `1_000` | `ComHostPolicy.MinimumDispatchDeadlineMilliseconds` | below this remaining aggregate, refuse to dispatch rather than send a deadline that would kill a healthy host | new | Kept - defensible. T1 pins both sides of the boundary. |
| `180_000` (was `30_000`) | `MailService.SweepBudgetMs` | the freshness sweep round trip | **measured 2026-08-19** | **Fixed twice, and the second fix changes behaviour on every large profile.** The 2026-08-18 fix was about the cold-start floor: an explicit budget suppresses it, which is right for health and was wrong for the sweep, so the first search on a fresh host had to fit the COM attach *and* the whole sweep into 30 s; `IComGateway.Run(op, budget, allowConnectFloor)` now lets the caller say which it means. The 2026-08-19 change is the value, and it is measured rather than preferred. On a purpose-built corpus - one PST outside the local index, 20,000 items across the four arrival-path folders with real received dates, 1,612 of them inside the seven-day fallback window so the 200-per-folder cap engages - four sweeps of ONE store took **13.6 s, 11.8 s, 10.7 s and 11.9 s**, i.e. ~12 s per store with the cap engaged. The maintainer's profile mounts FIVE stores, so the extrapolation is **~60 s against a 30 s budget** - the direct, measured explanation for the sweep timeout observed on their real profile, where the supervisor then killed and replaced the COM host. 180 s is 3x that extrapolation, and the margin is headroom rather than luxury: the corpus is a fast LOCAL PST and the same per-item work against Exchange is slower per item. **Before:** an ordinary sweep of a five-store profile could not finish, and each attempt cost a host kill plus a strike toward the breaker. **After:** it finishes - and if it ever does not, it stops at a folder boundary and returns what it covered. |
| `165_000` | `MailService.SweepWorkBudgetMs`, derived as `SweepBudgetMs - ResultReturnHeadroomMs` and passed to `IOutlookSession.SweepFoldersNewerThan` | the sweep's own soft budget, checked at every store and folder boundary inside the COM child | derived, the same shape as the exhaustive scan's | **New 2026-08-19 (maintainer decision (d)).** The whole-profile sweep had no budget of its own - only the gateway deadline above it - so a sweep that ran long raised a `TimeoutException`, the supervisor concluded the host was wedged, killed and replaced it, and **every folder already swept was thrown away**. Observed on the maintainer's real profile. The walk now stops at the next store or folder boundary, returns what it covered, and reports `sweep_time_budget` in `sweep.coverageGaps` with `freshness: "partial"` and `degraded: true` - the discipline the exhaustive scan has had since 2026-08-18. Its own coverage code rather than the subtree walk's `time_budget`, because the remedies point in opposite directions: that one says a subtree is wide (scope narrower, or `include_subfolders: false`), this one says the profile is big (name a store). The clock starts inside the child after the session is connected, so a cold start is paid out of the outer budget's headroom rather than out of this one. |
| the breaker's evidence rule | `ComHostPolicy.TimeoutIndicatesUnresponsiveness`, read by `ComHostSupervisor.ArmDeadline` | which deadline expiries count toward `UnresponsiveTimeoutThreshold` | new | **New 2026-08-19 (maintainer decision (d), second half).** Every expiry used to be evidence of a wedge. The freshness sweep runs on an explicit budget on every single search, so **two ordinary slow searches on a large mailbox opened the breaker and every COM request then failed fast for 30 s** - a self-inflicted outage whose only cause was the size of the mailbox. A caller that hands the gateway its own budget is stating how much of ITS work it will pay for, and it always has somewhere to degrade to; reaching that budget says the work was big, not that the host is deaf. The unqualified class deadlines still count: the ordinary operation deadline, connect, the exhaustive class (whose INNER budget stops it gracefully long before, so reaching the outer one really is a wedge) and the health probe, which is dispatched for the express purpose of finding out. The kill and the restart happen either way - nothing else can reclaim a blocked COM call, and the child serves requests serially. Only the CONCLUSION changed. |
| the in-process gateway's budget | `ComGateway.Run(op, budgetMilliseconds, allowConnectFloor)` -> `BudgetedSessionProxy` | bounds a multi-call lambda inside the COM host child AND inside every T2 live fixture | new | **New 2026-08-19 - it was `{ return Run(operation); }`, i.e. it accepted a budget and discarded it.** Defensible inside the COM host child, where the parent's watchdog is the real bound. Not defensible for `MailService.CreateDefault()`, which uses the same gateway and on which EVERY T2 live fixture is built: the whole live tier ran with no budget, no aggregate, no breaker and no hang detector, so anything sized against those budgets was unverified there by construction and a hung live run had nothing to stop it. This is the likely cause of a live-tier hang previously misdiagnosed as an Outlook problem. What the in-process side can enforce is the AGGREGATE, never the single call - a blocked outbound COM call is not cancellable and the caller cannot kill itself - so the proxy refuses to START a call once the budget is spent, mirroring `RemoteSessionProxy`'s dispatch floor. Failures are re-thrown through `ExceptionDispatchInfo`, because a reflective layer that flattened every deliberate error into "Exception has been thrown by the target of an invocation" is a defect this repository has already paid for once. |
| `250` ms | `ComHostSupervisor.CleanExitGraceMilliseconds` | how long a child gets to exit on its own after its pipe is closed, before it is terminated | new | **New 2026-08-19 (kill-safety option C).** The child already had a clean-exit path - `ComHostServer.ServeAsync` returns on EOF, "which is what makes a parent shutdown reliably take the child with it" - and `TearDownChild` already disposed the pipe before killing. But the kill followed the dispose in the same statement block, so the child was terminated microseconds after EOF and that path was dead code in practice. The gap makes it reachable: on an ORDERLY teardown the child now runs its own `finally` blocks and releases its COM references, which is exactly what a kill skips. It is free on the DEADLINE path, where `KillChild` has already terminated the process. |
| `60` (was `15`, was the client's `30`) | `MailService.SearchIndexTimeoutSeconds`, with `OleDbIndexClient.DefaultCommandTimeoutSeconds` raised to 60 so it stays the ceiling | per-query index timeout on the search path | measured healthy at 60-550 ms | **Fixed 2026-08-18, RAISED 2026-08-19.** The fix: `search`'s own description promises "sub-second and cheap", and one search is several index statements plus the sweep, each of which could independently take the client's 30 s default with no tool-level bound above any of them. The raise is a judgement about what to do on EXPIRY, not about the measurement, which is unchanged: on a ~50 GB profile a saturated or still-building index is an ordinary state rather than a fault, and a search that gives up after 15 s hands back a degraded answer the caller must then work around, while one that waits a minute hands back the real one. Headroom over the healthy measurement is now ~110x - this is a backstop against an indexer that has stopped answering, not a service-level target. `SearchBudgetMs` accordingly expresses the shape as 240 s (60 s index + 180 s sweep), still inside the 300 s operation deadline, and T1 pins that composition. |
| `4` s / `8_000` ms | `MailService.HealthIndexTimeoutSeconds` / `HealthPerStoreIndexBudgetMs` | per-query and overall index budget inside `outlook_health` | both justified in place | **Fixed - the budget now covers what it claimed to.** `IndexSearchService.DiscoverStoreScopes` **declared a `commandTimeoutSeconds` parameter and passed it to neither of its two statements** - a silently ignored argument, so health's catalog discovery ran on the 30 s default. `EnsureCatalogCoverageFromCom` (2 searches + 1 probe per '@'-named store) was unbounded on the index side for the same reason, and the 8 s clock did not start until both had finished. The clock now starts at the top of the whole index block and both steps run under it. **Before:** on a delegate-heavy profile with a busy indexer the "always answers" tool could spend minutes in the block it believed it bounded at 8 s. **After:** the whole index block is bounded at 8 s plus one in-flight 4 s query. |
| `240_000` (was `120_000`) | `MailService.MoveBatchBudgetMs`, with `MinimumItemBudgetMs = 1_000` | aggregate wall clock for one `move_mail` / `archive_mail` batch | derived: strictly inside the operation deadline | **Fixed 2026-08-18, and the fix was itself defective until 2026-08-19.** The original fix bounded a batch that had no bound at all - up to `MoveIdsCap = 50` ids at 2-3 gateway calls each, a theoretical 150 full deadlines. But it was written as `= OperationDeadlineMs`, and **an aggregate equal to its own unit of work is not an aggregate**: the check runs BEFORE each item and each item was a fresh gateway call carrying a full deadline of its own, so a batch one millisecond inside the budget could start one more item and run to ~240 s. **T1 pinned that equality, so the test enforced the flaw.** Now the value is strictly below the operation deadline AND each item is dispatched with what is LEFT of the batch budget (below `MinimumItemBudgetMs` the item is reported as not attempted, which is legible, rather than refused by the COM host's dispatch floor as a bare timeout). A per-item timeout is reported per item and says the move's outcome is UNKNOWN, because it is a mutation the host was killed in the middle of. T1 now asserts `<` rather than `==`. |
| the gateway aggregate | `ComHostRequestContext` + `ComHostPolicy.EffectiveDeadlineMilliseconds` | bounds a whole `IComGateway.Run` lambda, not just each round trip in it | new | **Fixed.** `RemoteComGateway.Run<T>(operation)` did not bound its lambda at all, so operations that make several contract calls - `HitLocator.Locate` (1 + up to 3 + N), the archive path's `foreach` over every store - each got a full deadline per call. The aggregate is the ordinary operation deadline measured from the start of the lambda, and each call is clamped to what is left. The explicit-budget overload does the same with its own budget, so health asking for 5 s now means 5 s of health rather than 5 s per round trip. Rounded up to a whole second before clamping, so the first call is never told "3999 ms" and the timeout message a human reads stays clean. |
| `1500` ms, `250` ms x6 | `OutlookComSession.RecipientResnapshotDelayMs`, `ExplorerFolderSettleDelayMs`/`Attempts` | waiting for Outlook to finish settling a fresh draft / a just-created Explorer | the delays were justified; the wait MECHANISM was not | **Fixed - defect, not tuning.** Both were plain `Thread.Sleep` inside a `_runner.Run` work item on the pumped STA thread. `PumpedStaRunner.PumpedWait`'s remarks state the rule they broke and record the measurement behind it (soak fix 21): an unpumped sleep blocks this thread's message queue, so Outlook cannot complete what it was asked and **the state being waited for cannot change during the wait**. Both waits are for exactly that class of settling, so both were structurally incapable of helping - 1.5 s of guaranteed-stale re-reads in each case. Now `PumpedStaRunner.PumpedWait`, like `AttachmentSizeSettleDelayMs` beside them. Values unchanged. Not observable without a real Outlook: the evidence is structural (same thread, same runner, same class of wait as the measured case), not a new measurement. |
| `1_500` ms | `MailService.StoreIndexProbeBudgetMs` | whole-pass bound on the "is this store in the index at all" probes that name `sweep.storesWithoutIndex`; the per-store verdict itself is cached for `StoreDetailsCacheTtl`, so this bounds the first search per five minutes rather than every search | justified in place, with a measurement: a delegate-subtree miss is 9-10 ms and a targeted '@' discovery miss 27-30 ms across a two-store catalog, so ~40 ms for one unknown store | Kept - defensible, and its REACH widened 2026-08-18 (gap A1 residue) without the number moving. The pass used to run only after a sweep; it now also runs on the three paths where the sweep does not happen at all (not needed, refused, failed), which is where an unindexed store was invisible. Same bound, same cache, and the same discipline on expiry: a store the budget could not settle is left unreported rather than guessed at, because "not established" must never become "not indexed". |
| `5 min` | `MailService.StoreDetailsCacheTtl`, used by `FolderPathCacheTtl` | how long the store list and the delegate folder-path list are reused | the named one said "Matches the store-details TTL"; the store-details one was a bare `TimeSpan.FromMinutes(5)` | **Fixed.** The comment asserted an equality nothing enforced. One constant now, plus a T1 test - the two caches describe the same profile, and mixing a fresh store list with a stale folder list shows a profile that never existed. |
| `20_000` autostart cooldown, `30_000` breaker cooldown, `30_000` start backoff, `2` timeouts to open, `3` failed starts to back off | `ComHostPolicy` | anti-churn and circuit-breaker behaviour | the autostart cooldown carries ~15 lines of root-cause narrative (2026-08-16); the breaker carries ~20 | Kept - defensible. The best-justified values in the tree, and already T1-pinned including boundaries. |
| `250` ms / `30_000` ms | `PumpedStaRunner.RetryAfterMs` / `GiveUpAfterMs` | COM `IMessageFilter` retry interval and give-up window | justified, including the Phase-3 live run that motivated it | Kept - defensible, with a caveat worth knowing: rejected COM calls can auto-retry for ~30 s, which is a quarter of an operation deadline. |
| `2_000` ms, `10 s`, `15 min`, `120 s` | `OutlookComSession.ScopedSweepTimeBudgetMs`, `SweepCache.DefaultTimeToLive`, `BodyCache.TimeToLive`, `SendConfirmation.DefaultTimeToLive` | per-folder sweep wall clock, sweep reuse window, paging snapshot age, send-confirm token life | all four documented and T1-pinned; the sweep budget carries measurements and a stated 5x headroom | Kept - defensible. |
| `10 min`, `32` | `DraftUpdateIntents.DefaultTimeToLive`, `DraftUpdateIntents.Capacity` | how long an interrupted `update_draft` stays resumable, and how many such records are held | **ADDED 2026-08-19** with the re-entrant update. The TTL bounds a real hazard rather than tidiness: the record holds a PRE-IMAGE of the draft, and the longer it sits the more likely the user has edited that draft in Outlook themselves, at which point completing the interrupted update would be reasoning from a state that no longer holds. 10 min is comfortably past the longest operation deadline (`ExhaustiveScanDeadlineMs`, 615 s) and comfortably short of a working session, so it covers "the agent retried the call that just failed" and nothing else. The capacity matches `SendConfirmationTokens.MaxPendingTokens` because it bounds the same kind of thing - unresolved per-draft state in one server process - and 32 concurrent half-finished draft edits is already far beyond any real session. T1-pinned (expiry, capacity eviction is not). | Kept - defensible. |
| `30 min`, `4` | `ExhaustiveScanCursors.DefaultTimeToLive`, `ExhaustiveScanCursors.Capacity` | how long a paged exhaustive scan stays resumable after its last page, and how many such chains one process holds | **ADDED 2026-08-19** with the resumable scan (gap F2). **The TTL is a RELATIONSHIP, not a preference, and T1 pins the relationship rather than the number** (`BudgetCompositionTests.AScanContinuationOutlivesTheScanPageThatIssuesIt`): it is roughly 2x `ComOperationBudgets.ExhaustiveScanDeadlineMs` (615 s), so a caller who lets one FULL-BUDGET page run and then spends as long again deciding what to do with the answer still has a whole page of slack. A TTL at or below one page length is unusable by construction - the handle can expire before the page that issued it has returned - and nothing would notice, because every refusal it then produces is a well-formed, correctly worded answer. `BodyCache`'s 15 min is the right KIND of precedent (a paging session rather than send friction) and is only about 1.5 page-lengths here. The capacity is lower than `SendConfirmationTokens.MaxPendingTokens` (32) because a scan session is far heavier - it carries a finished-folder EntryID set and a per-folder suppression set, not one hash - and paging four different exhaustive scans at once is already past any real use of a mode that costs minutes per page. Eviction is oldest-first and an evicted chain answers `Unknown`, which carries the restart advice. Both are `MonotonicClock`-based for the reason the row below states. | Kept - defensible, and the TTL is derived rather than chosen. |
| `10_000` | `OutlookComSession.ScanCursorEmittedIdCap` | most EntryIDs one folder's resume cursor may carry for duplicate suppression | **ADDED 2026-08-19** with the resumable scan (gap F2). It bounds the ONE structure in that design that would otherwise grow without limit: the restart rung has to remember everything it has already returned from the folder it keeps re-reading. An EntryID is around 140 characters, so 10 000 is roughly 3 MB held in the parent and crossing the COM pipe once per page - and 10 000 is 100 FULL PAGES spent inside a single folder, which is far past the point where narrowing the scan is the better answer. **Scoped per FOLDER rather than per session, deliberately:** once a folder is finished nothing can re-read it, so carrying its ids further would be pure wire cost. **What happens at the cap is the load-bearing part:** suppression STOPS and the payload says so (`dedup_capacity`, with its own advice sentence), rather than dropping rows to stay inside it. Duplicates are the failure mode chosen on purpose - losing mail in the mode picked BECAUSE completeness matters is the worse of the two. | Kept - defensible. |
| `10 min` | `MailService.SweepSafetyMargin` | how far back before the index frontier the freshness sweep starts, to cover mail the index has recorded but has not finished making searchable | none | **Kept - defensible, and now measured** (2026-08-18, dev profile: 5 stores - 3 accounts plus 2 delegate mailboxes - 222 folders, read-only through the shipped `search` / `outlook_health` tools). The margin has to cover INDEX LATENCY, and that is not what `staleness.ageMinutes` reports: age is the time since the newest indexed mail was RECEIVED (median 6.2 min, p90 29.8, max 35.9 over 177 probes at 20 s across 60 min), which on a quiet mailbox is the inter-arrival gap and says nothing about the indexer. Latency was bracketed instead by watching the frontier move: eight advances, each item searchable within `[1,22]`, `[0,15]`, `[7,27]`, `[0,7]`, `[0,4]`, `[73,93]`, `[0,6]` and `[0,12]` seconds of its own `DateReceived`. Seven of the eight are inside 30 s; the outlier at `[73,93]` is the number to size against. **Worst upper bound 93 s, so 10 min is 6.5x the worst case observed** - and the sample is one overnight hour on one profile, so it is a floor on the spread, not the whole of it. The direct check agrees: over 215 per-store sweeps (43 samples x 5 stores x the 4 default folders, every one `performed`) COM found exactly ONE item the index did not have, and it was 4 s NEWER than the frontier - the margin actually needed across the whole run was zero, and `duplicates` equalled `itemsSeen` in 214 of the 215. Two things to know before anyone trims it. **(1) The anchor WAS profile-wide, and that was fixed the same night this was measured.** The measurement above found `search` calling `GetStaleness()` with no scope, so `gapStartUtc` was the newest indexed `DateReceived` ANYWHERE in the profile even for a store-scoped search: five store-scoped searches in one sample all reported `2026-08-18T00:14:53Z` while `outlook_health`'s per-store probe reported three different values spanning 45.4 h, and `StalenessInfo.NewestIndexedUtc` documented itself as "across the searched scope" while the code did no such thing. A busy store pinned a quiet store's window narrow. Fixed in `c515565` - a search that names a store now gets that store's own frontier. The 10 min margin is unaffected; the numbers above were taken against the old anchor, which makes them if anything conservative. An unscoped search still opens one window from the profile-wide frontier, which is `QUESTIONS.md` Q4. **(2)** On roughly 12 days of every month the window is not applied as written at all - see the DASL caveat in the row below. |
| `7 days` | `MailService.EmptyIndexSweepWindow` | the window the sweep uses when the staleness probe returns no rows at all. The expression is `staleness.NewestIndexedReceivedUtc ?? UtcNow - EmptyIndexSweepWindow`. **This became reachable per store in `c515565`**: the probe used to be unscoped, so a null frontier meant the whole SystemIndex was empty, and now a search naming a store probes THAT store - so a single store with no indexed rows (an unindexed PST, a store still ingesting, a store Windows Search excludes) opens a 7-day window on every search naming it. Whether such a store exists on a real profile is unverified: the measurement below predates the change and was profile-wide, so it does not answer the per-store question | none | **Kept - defensible, and the cost is now measured** (same profile and run). Cost model fitted over the same 215 sweeps: **~19 ms per folder fixed and ~15 ms per item opened** (4 folders and 0 items = 76 ms median, n=172; 2 items = 90 ms, n=19; 4 items = 136 ms, n=24). Items really falling in a 7-day window, counted exactly through the index (hourly buckets wherever a day bucket hit the 100-row page cap): **1501 across the 20 arrival-path folders, 377 once the 200-per-folder cap applies** - and 1324 of the 1501 sit in ONE folder, the busiest account store's Deleted Items. Predicted 7-day empty-index sweep therefore **6.0 s capped against `SweepBudgetMs` of 30 s, and 22.9 s uncapped**. Seven days is affordable, but only because `SweepPerFolderCap` holds it there: the two are one decision and neither is safe alone. **What could not be measured, and why.** The sweep window cannot be widened through the shipped tools - `gapStart` is the scoped frontier minus `SweepSafetyMargin` (profile-wide at the time of measurement) and `after` can only narrow it, and no scope on this profile has a null frontier (40 folders of >=50 items probed, all non-null). The 6.0 s is a prediction from a measured cost model, not an observed 7-day sweep; a sweep that WAS forced to ~101 days (by the defect below) blew the 30 s budget and took the COM host with it, which is the direction the uncapped 22.9 s points. **The caveat that outranked all of it - FIXED 2026-08-18, and the number now means what it says.** Both DASL date filters - `ExhaustiveDaslFilter` and the sweep's own `GetTable` restriction in `OutlookComSession` - emitted `MM/dd/yyyy`, and Outlook parses a DASL date literal in the MACHINE locale. On this Dutch-locale box a day-of-month of 12 or less was read as the MONTH: `search exhaustive` asked for 1-5 August 2026 returned 48 items received in April and May 2026, and a sweep window starting `2026-09-05` was read as 2026-05-09 and timed out. A 7-day empty-index window opened on 18 August starts on 11 August, so `08/11/2026` was read as 8 November 2026 - a future date - and the sweep selected NOTHING while still reporting `foldersSwept: 4` and `freshness: "live"`. Both formatters now go through `DaslDateLiteral.FormatUtc`, which emits the year-first `yyyy-MM-dd HH:mm:ss` no locale can transpose. Re-measured read-only on one 368-item folder the same day, against ground truth from a full unfiltered table scan: the sweep shape at `since = 2026-08-11` returned **0 rows before and 55 after (ground truth 55)**, and at `since = 2026-09-05` **230 rows before, reaching back to 14 May, and 0 after (ground truth 0)**. Through the shipped `search` tool, an exhaustive 1-5 August window returned **158 hits, none inside the window, before** (a floor - one store hit the 100-hit cap, and the direct probe counted 121 matches in that folder alone) and **42 hits, every one inside it, after**, with the already-correct 13-15 August window unchanged at 67. So the 6.0 s prediction is now the prediction for the window this constant actually names. |
| `200` | `MailService.SweepPerFolderCap` | items read per folder in a sweep | none on the constant; the runtime advice explains the consequence | **Kept - defensible, and now measured** (same profile and run). It never fires in steady state and it always fires in the empty-index path, and both halves are the point. **Steady state: 0 of 215 sweeps hit it** - `itemCappedFolders` empty in every one, `coverageGaps` empty in every one, the largest `itemsSeen` for a whole 4-folder store sweep was 4 and for all 20 folders 5, against a window of `frontier age + 10 min` that ran to a median of ~19 min and a maximum of ~46 min. Expressed as the staleness it would take to reach the cap, from 169 exact hourly buckets on the busiest arrival-path folder (median 4 items/h, p90 16, peak **89**): the cap cannot truncate anything until the window exceeds **2.2 h at the peak rate**, 12.5 h at p90 and 50 h at the median - and the window only gets that wide once the index has stopped advancing, which needs Outlook closed (it only advances while Outlook runs) or WSearch stopped. **Empty-index path:** that same folder holds 1324 items in a 7-day window, so the cap drops the oldest 1124 of them, and **the newest 200 span only the last 19 hours - a nominal 7-day sweep of that folder really covers 19 h.** That is the trade, and it is the right one: without the cap the same sweep is 22.9 s against a 30 s budget (row above). One correction to this row's earlier text: the payload DOES flag it now - `sweep.itemCappedFolders` names the folders, `sweep.coverageGaps` carries `item_cap`, `degraded` and `freshness: "partial"` are set, and the advice quotes the number. What it still does not carry is HOW MANY items were dropped. **One correction to the correction, 2026-08-18 (audit gap H2):** "the cap drops the oldest" is a property of the folder TABLE, not of this constant. `Table.Sort` can fail - the property must exist as a column, and late-bound COM turns E_INVALIDARG into an ArgumentException - and the failure was swallowed with the note that an unsorted sweep still works. It does; the sentence stops working, because an arbitrary cut leaves an arbitrary hole. Such folders are now named in `sweep.itemCappedFoldersUnsorted` and raise `item_cap_unsorted` instead, with a sentence that says which mail is missing is unknown. The "19 h of a nominal 7-day sweep" figure above is the SORTED case and is unchanged; on an unsorted folder the 200 kept are simply 200 of the 1324, spanning nothing in particular. How often the sort actually fails on a real profile is unmeasured - it has never been observed here. |
| `400` ms x3 | `HitLocator.DelegateLeafWalkRetryMs` / `DelegateLeafWalkAttempts` | backoff between delegate leaf-walk attempts | the attempt count is justified ("live-bitten"); the backoff is not | Kept - defensible, now that the enclosing `Run` has an aggregate bound. Before this pass the 1.2 s plus three tree walks sat inside a caller with no bound at all. |
| `15` ms, `3 min`, `10 s`/`2 s` | `AuditLog` retry gap; `RemediationTools\ComMailbox` STA join; `RemediationTools\Program` sweep convergence | audit append retry, one-shot operator tooling | none | Kept - defensible. The audit retry is trivial; the remediation console is a one-shot operator tool that is not shipped, is run by hand and is watched while it runs. |
| `180` s / `3` s, `120` s / `3` s, and the T2 recovery polls | `T2\LiveInboxArrival`, `LiveFreshModeTests`, `LiveDisconnectRecoveryTests`, `LiveAttachmentKindRecallTests` | live-tier waits on real mail and on Outlook restarting | mostly none | **Fixed as a duplicate (D20 round trip); the rest kept.** `WaitForInboxArrival` was five verbatim copies across five T2 files, each with its own 180 s deadline and 3 s poll: raising one would have left four wrong and the suite flaky in whichever file was forgotten. Now `T2\LiveInboxArrival`, named constants, one copy. The disconnect-recovery and index-catch-up polls stay as they are - they are live-tier only, and several carry measurements. |
| `180` s x3, `120` s, `10` s | `T3\Phase5LiveMcpToolShapeTests.ArrivalSeconds`/`ReadableHitSeconds`, `T3\Phase4LiveMcpToolShapeTests.SeedVisibleSeconds`, `T3\MoveArchiveLiveMcpToolTests.SeedVisibleSeconds`, `T2\LiveShowMeTests.ExplorerSettleSeconds` | how long a live test waits for a real mail round trip, for a just-sent mail to become searchable over stdio, and for a second COM client to see an explorer move | none - four bare literals, and three of them were ALSO written out in the `TimeoutException` message beside them | **Fixed - named, and the message is built from the constant.** This is the same defect `LiveSweepScopeTests` was already bitten by: its failure said "within 120 s" while it waited 240, so the only fact the failure carried was wrong. Raising a wait now cannot leave the message lying. |
| `500` per folder, `3_000` per store, `4x` growth headroom | `T2\CensusIdentityPlan` | how much of each mailbox the store tripwire walks item by item rather than merely counting | new | **Kept - defensible, and instrumented rather than guessed.** Identity is what lets the guard say WHICH items left and tell a filing from a deletion, but a real profile holds a 6,153-item Archive, a 4,866-item Sent Items and a 19,525-item Deleted Items, and walking all of that twice per run would cost more than the run. 500 covers the folders a person actually works in - the 2026-08-18 false alarm was an Inbox of 168, a Postvak IN of 52 and a junk folder of 1 - and everything above it is still guarded by the count rule, which says in its own failure text that it cannot name the items. The real cost has never been measured on this profile, so every run now PRINTS it (`[tripwire] baseline: ... identified N folder(s)/M item(s), T ms`); read it on the first live run and move the budget rather than arguing about it. |
| every `DateTime.UtcNow` in the server | `Core\Services\MonotonicClock.cs` and its six call sites | which clock answers "how long ago?" | none - the server half had never been swept for this at all | **Fixed - six sites converted, five left alone deliberately, and the difference is now written down.** The add-in half of this defect was closed first (`UpdateService.DescribeState`, the debug-click window); this is the same sweep over `McpServer\`. The rule: a wall clock is right for RECORDING OR COMPARING AN ABSOLUTE INSTANT and wrong for MEASURING AN ELAPSED DURATION, because it can jump - a backwards jump (NTP correction, a user setting the clock, a VM resuming from a snapshot) extends every live TTL by the size of the jump, and a forwards jump expires them all at once. **Converted, all of them TTLs whose two ends are readings this process took:** `SendConfirmationTokens` (a send confirm-token whose whole purpose is to expire, held open by a backwards jump), `BodyCache` (15 min paging snapshot), the sweep cache clock in `MailService.RunGapSweep`, `MailService`'s folder-path and store-details caches (5 min each), and the `RemediationTools` purge-convergence loop (an 8 min cap and a 10 s stability window - a forwards jump there ends the purge with tagged items still in the mailbox). **Left alone, because both sides are real calendar time:** `AuditLog`'s line timestamp, `SignatureManager`'s backup filename, `IndexSearchService`'s staleness "as of", `MailService`'s `baseGapStart` (it becomes a DASL date compared against mail Outlook received), and `OutlookComSession`'s temp-file purge (compared against `File.GetLastWriteTimeUtc`). Converting any of those would compare a drifting clock against a real one. **The COM-host supervision and breaker paths needed no change and are worth recording as already correct**: `ComHostPolicy`, `ComHostSupervisor` and `ComHostRequestContext` contain no `DateTime` at all and were already `Stopwatch`-only - which matters more there than anywhere else, since a backwards jump would wedge the breaker open and a forwards one would release it early. `MonotonicClock` is a wall-clock anchor taken once plus `Stopwatch` elapsed, so it stays a `DateTime` and every injectable-clock seam and every T1 clock-injection test is untouched. `Stopwatch` and not `Environment.TickCount64` for the reason `PumpedStaRunner` already records: Core also targets net48. **One case is worth knowing before anyone tidies it**: `RunGapSweep` deliberately reads TWO clocks eight lines apart - the real one for the DASL window, the monotonic one for the cache age - and says so in place. **Completed 2026-08-18 with the third half, the LIVE TEST TIER**, which this row had never covered: thirteen polling loops in `T2\`/`T3\` measured their own timeout as `DateTime deadline = DateTime.UtcNow + timeout; while (DateTime.UtcNow < deadline)`, with waits of 10-240 s each - every wait for real mail to come back, and the cleanup loop that decides a mailbox is free of test artifacts. A backwards jump lengthens all of them by the size of the jump, which on this suite is indistinguishable from the hang that already cost a night; a forwards jump ends the artifact-cleanup loop with tagged items still in a real mailbox. All thirteen now use `T2\LiveWaitBudget` (`Stopwatch.GetTimestamp`). Left on the wall clock on purpose and commented in place: the send instants that become DASL sweep-window bases compared against Outlook's own `DateReceived`, the `ReceivedOnOrAfterUtc` filters, and the screenshot filenames. |

### Limits, caps and thresholds

| Value | Location | What it controls | Justification | Status |
|---|---|---|---|---|
| `48` hex chars | `MailService.MinRawEntryIdHexChars`, derived as `2 x EntryIdCodec.MessageEntryIdLength` | the floor for accepting a bare string as a raw EntryID | the comment said "real Outlook EntryIDs are 70+ bytes = 140+ hex chars" while the code said 48 - three times looser than its own stated truth, with no constant and no test | **Fixed as a derivation; value unchanged.** The 48 was never arbitrary: it is exactly the shortest structurally valid MAPI entry id (4 flag bytes + 16-byte store UID + 4-byte node id = 24 bytes), which `EntryIdCodec` already declared in BYTES a few files away. The two assumptions are now one. The comment is corrected to say what the check is for: acceptance, not validation - Outlook is the authority on whether an id opens, and the floor only has to exclude a hit id like `h7`. |
| `128` | `WsSqlBuilder.MaxTermLength`, used by `ExhaustiveDaslFilter.MaxTermLength` | longest accepted search term, both tiers | none, on either | **Fixed.** Two `private const int MaxTermLength = 128`, one per tier, both undocumented. Raising either alone would have made indexed and exhaustive search disagree about which terms are valid, for the same query, with nothing to notice. One public constant now, plus a T1 test that drives BOTH builders at the boundary and one past it. |
| `2048` | `DescriptionBudgetCiTests.ClientTruncationBudget` (`internal`), the only budget left | the wire-length guardrail: the MEASURED client cap on the initialize `instructions` and each tool `description`. Every `inputSchema.properties[*].description` is measured and reported against no budget at all | **Measured 2026-08-18 against Claude Code `2.1.234` on Windows 11**, by intercepting the client's own outbound `POST /v1/messages` and reading the `tools` array the model actually receives, byte for byte (reproduced against two models; the cut is client-side). Previously it rested on the documentation's ambiguous "2KB each" plus an extrapolation | **Kept - defensible, and the unit is corrected.** The measurement answers the three things the documentation left open. **(1) Per STRING, not per serialized tool** - there is no per-tool bucket at all: entries of 17,411 and 20,172 bytes arrived intact, and no total budget exists across `tools/list` (202 tools, 348,314 bytes of entries, nothing dropped). So the 2026-08-17 trim that moved `search` detail from the description onto its arguments was VALID; the dangerous reading, in which it moved text from one capped bucket into the same bucket, is disproved. **(2) UTF-16 code units, never bytes** - a 2,048-character description weighing 6,004 UTF-8 bytes arrives whole, and two strings of very different byte lengths were cut at the same character offset. The guard measured `max(chars, UTF-8 bytes)` and failed on the larger, because the unit was undocumented; it now measures `string.Length` alone. That only ever produced FALSE failures, and on today's surface **no measured size changed at all** - all 132 wire strings are pure ASCII, so chars == bytes everywhere, and the largest strings are unmoved (`search` 1791, `update_draft` 1593). **Re-measured 2026-08-20**, after the atomicity pass appended one shared 190-unit `outcome` clause to the thirteen tools that can change something: `search` unchanged at 1791 (read-only, deliberately not given the clause), `update_draft` 1774, `discard_draft` 1430, everything else below 1200. Thirteen copies of one clause cost nothing against any single tool because the cut is PER STRING with no per-tool bucket - which is the measurement above being relied on rather than restated. The guard is now right rather than accidentally harmless. **(3) Parameter descriptions are NOT capped** - 20,000 characters through intact - so the 2048 applied to them is GONE. It was briefly relabelled `HouseParameterBudget` and kept, on the reasoning that it floats with a client version we do not control and that `BodyHtmlHint` is one constant reused across five drafting tools - so one over-long shared parameter description would be five silent truncations the day a release starts cutting schemas. The maintainer overruled that on 2026-08-18: a limit that rejects text the client delivers whole is a false failure whatever the future risk. Parameter sizes are still measured and printed, so the re-measure is a document lookup rather than a build failure. The 75% warn tier went with it, for the reason a warning that fires on three unchanging strings every run is wallpaper by the time it matters. Boundary unchanged: cut when `length > 2048`, so exactly 2,048 passes. A cut string reaches the model as its prefix plus a literal marker - U+2026 HORIZONTAL ELLIPSIS, space, `[truncated]` - of 13 units, so 2,061 in total, recorded as `ClientTruncationMarker` - the suffix is added after our response has left, so **a server cannot detect its own truncation** and no test here ever will; the constant exists for the human grepping a transcript. **One client at one version, with nothing watching it**: no version header, no notification, no server-side signal. Re-measure at client-bump time; the change worth re-measuring for is a release that introduces a per-tool bucket. |
| `100` / `25`, `1000` / `200`, `200` / `50`, `500_000` / `20_000`, `100_000`, `65_536` / `8_192` / `256`, `100` x2, `20`, `1000`, `10_000`, `12`, `50` | `MailService` payload caps | every list a tool can return | section-12 payload-discipline block, and all T1-pinned | Kept - defensible. The model of how to do this: every cap named, every cap reported to the caller, every cap pinned. **One correction, 2026-08-18:** "every cap reported to the caller" was not true of the `20` in this list - see the row below. The claim is true again now, and the lesson is that a row summarising thirteen values cannot be trusted to hold for each of them. |
| `20` | `MailService.UnresolvedRecipientsCap` | addresses in `unresolvedRecipients` on `new_draft` / the three derived draft tools / `update_draft` | the constant was named and documented, and it was the ONE cap in the payload-discipline set with **no has-more indicator**: two `Take(20)` sites, no flag, no total, two lines below a resolved recipient list carrying exactly that pair | **Fixed as an omission; value unchanged.** The cap itself is fine - twenty bad addresses is already a broken recipient line - and what was wrong is that it was invisible. It also mattered more than its inherited "tangential to search" label: the RESOLVED list can survive being short, because the draft holds the recipients whatever the payload lists and every operation (send hash, identity checks, transport) reads the full COM-side list, whereas an UNRESOLVED address is a defect the agent is expected to act on by asking the user about it. So a cut list read as complete is one whose remainder is never mentioned to anybody, and the draft goes out - or fails to - carrying a fault its own report declared absent. One pure `CapUnresolvedRecipients` cuts the list for both mapping methods and one pure `DescribeUnresolvedRecipientCap` renders the sentence from the same `total`/`truncated` pair, so the number in the prose and the number in the field cannot disagree; the sentence quotes the constant rather than restating 20. The tool hints name the flag pair but deliberately NOT the number: an attribute argument must be a compile-time constant, so a digit written there could not be derived from the constant and would be a second copy free to drift. T1 `UnresolvedRecipientReportingTests` pins the boundary (20 of 20 is not truncated), the empty case, both payloads and the sentence. |
| `12` (= `SweptFolderListCap`) | `MailService.UnindexedStoreListCap` | names in `sweep.storesWithoutIndex`, in the payload AND in the `no_index_frontier` advice sentence that joins them into prose | none existed: this was the one list in the server with **no cap at all** (Q7b, answered 2026-08-18) | **Fixed as an omission, and DERIVED rather than a second 12.** Both bound a name list inside the same sweep block for the same compact-payload reason, so they are one number - the shape `ExhaustiveDaslFilter.MaxTermLength = WsSqlBuilder.MaxTermLength` already uses. Truncated rather than dropped, which is where it differs from the swept-folder list: a swept-folder list is a legibility aid and is worth nothing in part, whereas each unindexed store NAME is separately actionable, so the first twelve keep their whole value. Reported as `sweep.storesWithoutIndexTruncated` + `sweep.storesWithoutIndexTotal`, and the advice sentence names the cap and the remainder - a cap applied only to the payload would have left the whole list in the text an agent relays to the user. T1 `StalenessAndUnindexedStoreTests` pins the cap, both flags and both wordings of the sentence. |
| `10_000` + `64` | `MailService.FolderWalkAbsoluteCap` + `OutlookComSession.FolderWalkDepthGuard` | the two bounds of the full-tree folder walk (`list_folders`, and the delegate folder-name walk behind a delegate folder scope) | the cap carried a one-liner and was pinned; the depth guard was **private, unpinned, and restated as a literal `64` in two doc comments** | **Kept - values unchanged; what changed is that both are now REPORTED and both are pinned.** Neither bound was visible in any payload (gaps G3/G4, closed 2026-08-18): the walk stopped and `FoldersOutcome.truncated` was computed against the already-truncated list, so a cut-off tree paged out as a complete answer, and the delegate folder-NAME list built from the same walk narrowed a search with nothing saying so. Now `truncated` covers both causes, `walkCapReached` / `depthLimitReached` tell them apart, `nextOffset` is withheld where paging cannot help, and a delegate scope carries `scope.folderNamesTruncated` + `degraded`. The depth guard is public so the advice sentence quotes the constant instead of restating it, and `PayloadDisciplineTests` pins it beside the cap - a cap named in prose next to a constant nothing compares it with is exactly the drift this document was written to find. **The depth guard gained a THIRD walk on 2026-08-18** (audit gap F4): `OutlookComSession.ScanFolderTree`, the exhaustive scan's, which had no bound at all and was therefore an uncatchable `StackOverflowException` on a cyclic tree - it ends the COM host process, and the caller is told Outlook has gone away. Same constant, same comparison, and the stop is reported (`exhaustive.depthLimitReached` plus the sweep's own `depth_limit` code) rather than silently truncating the one mode chosen for completeness. All three walks now read one number. |
| `(unnamed store N)` | `Com/StoreNaming.UnnamedStorePrefix` | the label a store gets when its `DisplayName` COM read fails, keyed on its 1-based `Namespace.Stores` position | new with gap G2, 2026-08-18; there was no value before because there was no store - it was dropped | **Kept - and the shape is the decision.** Every scope, bucket, label and refusal in this server is keyed by a display name, so an unnameable store had nowhere to go and was dropped from `list_folders`, from `list_accounts` and from the freshness sweep. The alternatives were all guesses that LOOK like names - the root folder's `Name`, the PST file path - and every one of them shares the fatal property that it cannot round-trip, because a store scope is matched against the `DisplayName` that failed, while gaining a new one: a guessed name can collide with a real store's sweep bucket. So the label is bracketed, it is never treated as a name (`IsUnnamedStoreLabel` refuses it as a scope by what it is), and it is flagged everywhere it appears. Honest limit, documented at the constant: the position renumbers if the profile's store list changes, so the label identifies a store within one answer and not across a profile change. |
| `4` | `MailService.NonMailClassAdviceCap` | distinct item classes the "not ordinary mail" advice names before trailing off into `, ...` | new with gap B3's widening; sized as a heads-up rather than an inventory | Kept - defensible. The sentence exists because all three tiers now return bounce reports, receipts, meeting requests and posts alongside mail (B3), and an agent relaying a count must know which is which; the complete answer is the per-hit `itemClass` field beside it, so the prose only has to be enough to notice. Pinned in `PayloadDisciplineTests` with every other cap - a cap in prose is still a cap. |
| `1601-01-01 00:00:00` | `WsSqlBuilder.OrderKeyFloorUtc`, emitted by `WsSqlBuilder.BuildOrderKeyPresence` | lower bound of the "this row carries a received date" predicate the index tier's displacement refetch adds (`IndexOrderGuard`) | new with the B3 follow-up, 2026-08-18; **the collation it defends against is NOT measured** | Kept - defensible, and chosen to be a null test rather than a date filter. WS-SQL offers no `IS NULL`, so "has a value" has to be written as a comparison and three-valued logic does the rest: a row with no date fails it. The value is the FILETIME epoch, below every instant a MAPI date property can hold **including the zero dates a damaged item carries**, so it excludes exactly the rows with nothing there and nothing else - a floor of, say, 1900 would quietly turn a null test into a date filter, which is the narrowing this whole area keeps having to undo. What is unmeasured is the thing that makes the predicate necessary at all: **where the Windows Search provider sorts a NULL under `ORDER BY ... DESC`**. If NULLs sort last the refetch never fires and this constant is never emitted; if they sort first it fires on every truncated search and costs one extra index statement (~40-100 ms on the measured shapes). T2 `LiveOrderKeyCollationTests` measures both that and whether the provider accepts this literal; until it runs, the guarantee rests on construction rather than on measurement. |
| `5000`, `64`, `40`, `40`, `64`, `24`, `512`, `8`/`8_000_000`, `20`/`150 MB`/`50`, `160`, `128`, `512 KB`/`20`/`1024` | `WsSqlBuilder`, `FolderScopeResolver`, `OutlookComSession`, `HitLocator`, `ServerDraftRegistry`, `BodyCache`, `DraftAttachments`, `SignatureCatalog`, `SignatureManager`, `HtmlFragmentNormalizer` | statement, walk, cache and payload bounds | `WsSqlBuilder.MaxFolderPaths` is the best-measured cap in the repo (95 literals execute, 100 fails with 0x8000FFFF; cost curve 43->101 ms); the rest carry one-liners or measurements | Kept - defensible. |
| `64 MB` (`67_108_864`) | `ComHostProtocol.MaxFrameBytes` | the largest single message either end of the COM-host pipe may send, and the largest a reader will allocate for | the comment said "far above any real payload" and nobody had measured what a real payload weighs - so the headroom was an assumption, and the derivation in `TODO.md` puts an ordinary sweep of an UNINDEXED store within reach of it (4 folders x 200 items x every store, bodies uncapped at this layer, ~80 KB average body on one store or ~16 KB on five) | **Value unchanged; what changed is that breaching it is survivable and that the number is now measurable.** Breaching it used to end the child process - the refusal was thrown from a write the serve loop guarded with `catch (IOException)` only, so the caller was told the COM host had gone away and every later request died with it. The loop now answers with a `ComHostResponseTooLargeException` frame naming the operation, the size and the limit, and keeps serving. The measurement is `ComHostFrameMeter`: a per-server-process high-water mark of the largest payload seen in either direction, plus a refusal count, reported by `outlook_health` beside the limit itself (`comHost.largestFrameBytes` / `frameLimitBytes` / `framesRefusedTooLarge`), so the ratio between assumption and reality can be read off a running install. It survives COM host restarts on purpose - the child is restartable and the profiles that produce the biggest frames are the ones whose hosts restart most. **DECIDED 2026-08-18 by the maintainer: cap bodies at the COM layer, so a frame that cannot be sent cannot be built.** The number itself does not move and the sweep result is not chunked - those were the other two options and they were not chosen. The cap is the two rows below (`OutlookComSession.SweepBodyCharsCap` and `SweepBodyBytesBudget`), and the second of them is DERIVED from this constant: bodies may claim at most half a frame, pinned by T1 `SweepBodyCapTests` because Core cannot see this assembly. What that does NOT close is the non-body half - per-item EntryIDs, StoreIDs, subjects and folder names are ~1-2 KB each and the item count is unbounded in the store count - so a profile of roughly 40 mounted stores, each with 200 items in every one of its four arrival-path folders inside the freshness window, could still build an oversized frame carrying no body text at all. That case has never been seen; the typed refusal remains the backstop for it. Reachable in T1 because `ComHostServer` takes the ceiling as a constructor parameter defaulting to this constant - a 4 KB ceiling and a 64 KB answer run the same branch, the same serve loop and the same mapper as a 64 MB one, in milliseconds. |
| `500 000` chars | `OutlookComSession.SweepBodyCharsCap` | the longest mail body ONE swept item may carry across the COM-host pipe | new 2026-08-18 with the maintainer's decision above. There was no bound at all: `SnapshotBrief` took `item.Body` whole, and `MailService.BodyCharsDefault`/`BodyCharsCap` are applied on the FAR side of the pipe, so they bounded what an agent SEES and not what crosses | **New - and the value is anchored rather than picked.** It IS `MailService.BodyCharsCap`, the largest body window `read` will ever hand back in one call, pinned equal in T1 `PayloadDisciplineTests` rather than written as one expression so the reason stays visible and the COM layer keeps no compile-time dependency on the service layer. The evidence agrees with that anchor: `Docs/com-host.md` records a `read` legitimately returning ~0.5 MB, and `TODO.md` measures an ordinary long quoted thread at ~80 KB - so the cap sits at the top of the observed range and ~6x above the ordinary worst case. **What it costs, and it is a real cost.** These bodies are shown to nobody: they exist so `FreshMerge.MatchesTerms` can match terms over mail newer than the index frontier, so this is NOT a display truncation like `read`'s windowing, which loses nothing because it pages. A term appearing only past the cap in just-arrived mail will not be matched. Reported, never silent: `ComMailBrief.BodyTruncated` per item, `sweep.itemsBodyCapped` and `sweep.itemsBodyCappedUnmatched` in the payload, the `body_cap` coverage code raised on the intersection alone, and one advice sentence built from the same two numbers. **It is NOT what keeps the frame legal** and cannot be - the item count is 4 folders x 200 x every store, and the store count is unbounded - which is the row below. What it does is keep that budget fair: without it one enormous mail would spend the whole budget and blind the sweep to every item behind it. |
| `32 MiB` (`33_554_432`) = `MaxFrameBytes / 2` | `OutlookComSession.SweepBodyBytesBudget` | encoded body bytes ONE sweep answer may carry in total, across every folder and every store it visits | new 2026-08-18, same decision. The per-item cap above cannot bound a frame whose item count is itself unbounded: at 500 000 chars, 800 items is already ~400 MB of theoretical worst case on a single store | **New, and DERIVED from the frame limit rather than chosen.** Half, not a figure closer to the ceiling, because a sweep frame is not only bodies: per-item EntryIDs, StoreIDs, subjects, senders and folder names run to ~1-2 KB each, i.e. ~16 MB at the 8 000 items a folder-scoped sweep can reach. Bodies take half, everything else the other half, and what is left is margin. Pinned as `SweepBodyBytesBudget * 2 == ComHostProtocol.MaxFrameBytes` by T1 `SweepBodyCapTests`, the one compilation that sees both assemblies. **Counted in BYTES, not characters, and that is what makes it unreachable in normal use.** The pipe serializes with `JavaScriptEncoder.Default`, which escapes every non-ASCII character as a six-byte `\uXXXX`, so a character costs 1 to 6 bytes depending on the language of the mail. A CHARACTER budget would have to be sized for the 6-byte case and would then bite at ~5.5 M characters - which an ordinary unindexed-PST sweep (800 items, ~10 KB bodies = ~8 M characters) really can reach. In bytes it bites at ~18 M characters of ordinary Latin prose and correspondingly earlier for CJK or Cyrillic mail, which is right, because those frames genuinely are that much bigger. `OutlookComSession.EncodedBodyByteCeiling` is deliberately an OVER-estimate (1 byte for ASCII letters, digits and space; 6 for everything else, which is the ceiling for any UTF-16 code unit under that encoder, surrogate pairs included) and T1 checks it against what the real serializer emits - an estimate that is ever LOW is the one failure mode it may not have. **Steady state never touches it:** 0-5 items per whole sweep on the measured profile, ~50 KB of body. The measured empty-index case (377 items after the per-folder cap) is ~3.8 M characters, ~20-25% of the ordinary-prose bite point. **MEASURED 2026-08-19, and the measurement moves this row from insurance to load-bearing.** On a 20,000-item unindexed PST with the per-folder cap engaged, ONE store's sweep produced a frame high-water of **10,734,599 bytes** - 10.2 MB over 758 items, about 13.5 KB per item. Five such stores extrapolates to roughly **54 MB against the 64 MB frame limit**, so this budget bites BEFORE the frame limit does, which is exactly its design intent. It is COUPLED to `MailService.SweepBudgetMs`, which is why the two moved in the same pass: the 432 KB high-water previously measured on the real profile was bounded by the old 30 s sweep timeout, not by any item cap - the sweep was being stopped before it could gather enough to be big. Giving the sweep time to finish is what lets it build frames this size. The value does not change. |
| `2000` | `MailService.StoreDiscoverySampleSize` | item URLs sampled to discover store scopes | measured at 552 ms in the section-5 probes - but at the callee, not at the call site | **Fixed as a literal; value unchanged.** Named at the call site, with the measurement moved next to it. |
| `1000`, `500`, `256`, `255` x3, `30` minutes | `OutlookComSession.TimeOnlyProbeMaxFolderItems` / `GetTableProbeMaxRows`; `MailService.ShowSearchQueryCharsCap` / `SubjectCharsCap` / `StaleIndexNoticeMinutes` | subjectless-locate refusal, `GetTable` fallback scan bound, `show_search_results` query cap, subject caps, health staleness notice | none, on any of the seven | **Fixed** - all seven named, and the naming was the point rather than the value: not one of them changed. The subject cap went first (three code copies plus tool prose, now one constant and a T1 test deriving the phrase). The rest closed here. Two of them could not be read at all before: `total > 1000` counts ITEMS IN A FOLDER, because a hit with no subject has nothing to `Restrict` on and can only be found by walking the folder item by item - the cap is what keeps "we could not identify this hit" from turning into a full scan of an Inbox; `scanned < 500` counts TABLE ROWS, bounding the `GetTable` fallback when the DASL filter matches most of a large folder. The other two are restatements: `query.Length > 256` sat next to a message saying "max 256 chars", which now quotes the constant, and the 30-minute health threshold is now `StaleIndexNoticeMinutes` beside the `VeryStaleAdviceMinutes` (720) it must stay below - they select two different wordings about the same number and nothing said they were a pair. T1 pins all four, and pins the two staleness thresholds *in order*, because a notice threshold above the advice threshold would have `outlook_health` calling an index "current" that `search` was calling very stale in the same session. No tool description grew: `show_search_results` deliberately does not advertise its query cap. |
| `5` s / `120` s / `15` s / `120` s | `MailService.EmailLocateToleranceSeconds` / `AttachmentLocateToleranceSeconds` / `DedupeToleranceSeconds`; `HitLocator.Locate`'s default parameter | ReceivedTime tolerance when mapping an index hit to a real item | the first two justified (live-verified 25/25 within 5 s); the dedupe window and the default parameter not | Kept - defensible, with a caveat. The 5 s is machine-specific: a loaded box, or a delegate store with skewed `DateReceived`, silently fails to locate. `HitLocator`'s default `toleranceSeconds = 120` is a fourth copy of that number and is dead in production (the only caller passes explicitly). |

| the corpus mixture: `45/33/15/6/1` size weights over `200`-`640_000` bytes, `2/6/17/30/45` age weights over `0`-`1460` days, `55/25/12/8` folder weights, and `250` items between progress lines | `RemediationTools\CorpusPlanOptions.DefaultSizeClasses` / `DefaultDateBands` / `DefaultFolders`, `CorpusOptions.ProgressEvery` | what the synthetic measurement corpus contains, and therefore what the sweep and scan budgets get measured against (`Docs/corpus-measurement-plan.md`) | new, and every number is derived from a constant under measurement rather than picked | Kept - defensible, and deliberately tied to the constants it exists to measure. The age bands are cut at exactly 1, 7 and 60 days because those are the windows being timed, so the expected selection of each is a band count the tool prints BEFORE anything is built rather than an estimate made afterwards. The top size class runs to 640 000 bytes specifically to straddle `OutlookComSession.SweepBodyCharsCap` (500 000), so roughly a fifth of it trips the per-item body cap - a corpus that stopped below the cap could never show what capping costs, nor that `itemsBodyCapped` is wired at all. Overridable per run, and the whole shape is digested into a `ShapeKey` the manifest records, so a corpus cannot be extended with a different mixture than the one it started as. T1 `CorpusGeneratorTests` pins the weights against the realised distribution over 40 000 items, that the 7-day and 60-day windows select genuinely different volumes, and that the tail reaches past the sweep cap. |

### Duplication across the process boundary

| Concept | Copies | Status |
|---|---|---|
| `ExpandEnvironmentReferences`, the whole algorithm | add-in `Services\McpConfigEditor.cs`; server `Core\Services\HealthReporting.cs` | **Fixed.** The source said "both are pinned so they cannot drift apart unnoticed" and that was **false**: two suites, two sets of fixture literals, and no test ever fed one input to both - even though the tests project already compiles the add-in file. They are the write and read halves of one registration check, so a divergence would report a healthy registration as drifted or the reverse. T1 `EnvironmentExpansionParityTests` now runs one 27-case corpus through both and asserts character-for-character equality. The false claim is replaced by one naming the test. |
| `9375CFF0413111d3B88A00104B2A6676` (Outlook accounts subkey) + the Outlook root | `SignatureCatalog`, `SignatureManager`, `T2\LivePhase4Fixture` | **Fixed.** Three hand-typed copies of a 32-character GUID inside the server alone. A typo in one is invisible on inspection and produces "no accounts found" rather than an error. Now `Core\Services\OutlookProfileRegistry`, whose Outlook root is also no longer a hardcoded `16.0` - see the row below. |
| `HKCU\Software\OutlookAI\Tuning`, `HKCU\Software\OutlookAI\Mcp`, `...\Outlook\Search` + `DisableServerAssistedSearch`, `~/.claude.json` + the `"outlookai"` server key, the registration status strings | add-in `OutlookTuningService` / `McpRegistrationService`; server `HealthReporting` | **Open - needs a decision.** Five mirrors, each acknowledged by a comment ("mirrors OutlookTuningService.TuningKeyPath") and none enforced. They CANNOT be shared in code - the add-in is net48/VSTO and cannot reference the .NET 10 server, which is the same constraint that produced the `ExpandEnvironmentReferences` pair. Options: (a) extend `check-pinned-constants.ps1` to compare the literals textually across the two trees, the way it already does for the certificate thumbprint and the installer mutex; (b) accept them - each mirror is read-only on the server side and a drift degrades a health report rather than corrupting anything. Note one real asymmetry: the server also reads the POLICY hive (`Software\Policies\...\Outlook\Search`) which the add-in never writes or checks. **Largely superseded** - see the two `Fixed` rows in the invariants table above: the two HKCU key paths, the fifteen value names and the Claude Code names are now one definition (`Services\AddInServerContract.cs`, linked into Core), the registration status strings are compared by the drift script (#6/#7), and the Office major inside the Search key path is detected rather than assumed (`Services\OfficeVersions.cs`, linked the same way). The policy-hive asymmetry stands and is deliberate: only the server reads it, so only the server builds that path. |
| `16.0` everywhere in the server | `HealthReporting.OutlookSearchUserKeyPath` + `OutlookSearchPolicyKeyPath`, `OutlookProfileRegistry.OutlookRootKeyPath`, and through that last one `SignatureManager`, `SignatureCatalog` and `T2\LivePhase4Fixture` | **Fixed - one definition, shared, not a second implementation.** The add-in half of this pass introduced `Services\OfficeVersions.cs`; that file is now LINKED into `OutlookAI.Core` as well (`OutlookAI.Core.csproj`), exactly the way `AddInServerContract.cs` is, so the two halves probe the same majors in the same order (`16.0, 17.0, 15.0`, newest first) by construction rather than by comment. It stays `internal` for the CS0436 reason that file's header documents, and Core re-exports what outside callers need through `OutlookProfileRegistry` (`SupportedOfficeVersions`, `FallbackOfficeVersion`, `DetectedOfficeVersion`, `OfficeVersion`, `BuildOutlookRootKeyPath`, `DetectOfficeVersion`). Proved by renaming a shared member: the net48 add-in and both Core targets fail to compile together. (That proof originally renamed `Supported` and broke `ThemeService` twice; `ThemeService` now consumes `TryDetectOutlookVersion` / `HasOutlookKey` / `CommonKeyPath` instead, so the same proof runs through those members - the sharing holds, only the member that demonstrates it changed.) Every path above is now built from the detected major, `outlook_health` reports it as `outlook.officeVersion`, and a machine with NONE of the supported majors gets a `problems` line naming what was probed and which hive was used instead, rather than the empty accounts and empty signature defaults that used to be the only symptom. On a 16.0 machine - the overwhelming majority - behaviour is byte-for-byte unchanged. **The probe had to be strengthened to make any of this work** - see the row below. |
| "does `...\Office\<major>\Outlook` exist?" as evidence that Office major is installed | `Services\OfficeVersions.cs` (`DetectOutlookVersion`, used by `OutlookTuningService` and now by the whole server) vs `Installer.iss`'s three resiliency exemptions | whether version detection can ever answer anything but `16.0` | none - the key-exists probe was assumed sufficient | **Fixed, and it was a live defect in the detection shipped earlier in this pass.** `Installer.iss` writes `...\Office\{16.0,15.0,17.0}\Outlook\Resiliency\DoNotDisableAddinList` on every install, and writing that value creates the whole path above it - so on every machine this product is installed on, ALL THREE Outlook keys exist and a key-exists probe returns the FIRST version tried. Detection would have answered `16.0` on an Outlook 2013 or 17.0 machine, i.e. the fix would have been inert on exactly the machines it was written for, while looking correct in code and in the health payload. Measured on the dev machine 2026-08-17: `15.0` and `17.0` each held one subkey (`Resiliency`) and no values; the real `16.0` held 31 subkeys and 4 values. `OfficeVersions.IsOutlookHive` now requires at least one value or one non-`Resiliency` subkey, re-exported as `OutlookProfileRegistry.IsOutlookHive` and pinned by T1 `OfficeVersionDetectionTests` against those measured shapes. `Installer.iss` is unchanged and its exemptions stay deliberate; what changed is that the detector no longer mistakes our own footprint for an installation. |

### Claims of non-effect, and what makes one assertable

**Not a number, and it belongs here anyway.** This file exists for values a reader could ask
"why that, and what happens if it changes?" about. A sentence asserting that nothing happened is
exactly such a value, and it was the worst-behaved family in the repository: thirty-one of them,
each decided by hand at the site that wrote it, sixteen wrong. They are duplicated the way a
magic number is duplicated - the same claim restated in prose in three files - and they had the
one property that makes duplication dangerous, which is that no compilation and no test could
relate the copies.

| Value | Location | What it controls | Justification | Status |
|---|---|---|---|---|
| `"unchanged"` / `"applied"` / `"unknown"` | `Core\Com\MutationOutcome` | the machine-readable half of every failure: the `outcome` field on the error object and on `MoveItemView` | one vocabulary, derived from `ComSessionOperations.IsRetryable`, never restated | **Fixed - one definition, and the classification behind it already had a completeness guard.** Three values because the outcome really has three states and `MoveItemView.Ok` is a boolean: a move that succeeded and could not be audited returns `Ok=false` over an item that MOVED, and no wording repairs a field. `ForInterrupted` and `ForCompleted` are the only two ways a value is produced, both keyed on the same set that decides the disconnect retry, so a contract operation added without a classification fails T1 `SessionRetryClassificationTests` before it can produce a wrong claim here. Fail-closed in the same direction as its source: an unclassified name reads as mutating. |
| The OPENING sentence of an interrupted-operation message | `MutationOutcome.DescribeInterrupted` / `DescribeAnswerLost` | what a caller is told about effect by the COM-host timeout arm, the bare-`COMException` arm, the too-large-answer arm and the child-side framing refusal | the shape `ComHostSupervisor.DescribeInterruption` proved, lifted out so three more sites share it | **Fixed - shared opening, per-site remedy.** Only the opening is shared, deliberately: the remedy differs sharply per path (update means repeat the call, send means check the Outbox, create means check Drafts, discard means check Deleted Items) and a shared sentence that tried to carry it would be worse than the per-site prose it replaced. `DescribeAnswerLost` is a second sentence rather than a parameter because it says the opposite thing - the work SUCCEEDED and a repeat would do it twice - and folding the two into one function is how that distinction would eventually be lost. |
| `"com_failure"` | `MailService.ComFailureRefusal`, read by `OutlookTools.DraftRefusalAdvice` | which draft refusals may claim the draft was left alone | it was `internal` and the tool layer branched on nothing | **Fixed.** The tool layer attached ONE advice string to every refusal reason, so an interrupted `update_draft` returned a payload whose `message` said the outcome was UNKNOWN and whose `advice` said nothing had changed - undoing `db34923` one layer up, in a field no test asserted on. Now public and shared, with T1 `AtomicityClaimsTests` asserting on the advice as well as the message. |
| "Nothing was changed", "Nothing was sent", "nothing was moved for this item" and their siblings | `MailService` refusal builders, `MailModels.MoveItemView`, the `discard_draft` tool description, XML docs on `BuildDraftRefusal` and `DraftRefusedException` | what an agent believes about the mailbox after a failure | audited claim by claim on 2026-08-19; fourteen were true and stayed | **Fixed where false, kept where earned.** The fourteen true ones are true for a reason worth stating: each sits before every write. That is also the test for a new one - if the sentence is not written before the first mutating call, it may not assert the negative. |

### Protocol and environment

| Value | Location | What it controls | Justification | Status |
|---|---|---|---|---|
| `"2025-06-18"` | `T3\McpStdioClient.ProtocolVersion` | the MCP revision every T3 assertion is written against | none | **Fixed.** The client sent it and then did `_ = init.GetProperty("result").GetProperty("protocolVersion")` - asserting the field EXISTS and never comparing it, so a server that negotiated a different revision passed every T3 test silently while a real client would have disconnected. Now compared, with a message naming both. Worth knowing: the SDK in use advertises `2024-11-05, 2025-03-26, 2025-06-18, 2025-11-25` and rejects an unsupported request outright, so this client is deliberately one revision behind the newest the server can speak. |
| `Provider=Search.CollatorDSO;...`, the SystemIndex column names, `mapi16://`, `System.Kind` values, `OlFolderArchive = 39`, `olMail = 43`, `rctrl_renwnd32`, the MAPI proptags, `EncodingOffset = 0xAC00` | `IndexClients`, `WsSqlBuilder`, `IndexRowFilter`, `ArchiveFolderResolution`, `OutlookComSession`, `OutlookLiveness`, `EntryIdCodec` | every external contract the server depends on | the strongest documentation in the repo - forbidden columns explained per column, the 22.6%-of-attachment-rows measurement driving the kind list, `OlFolderArchive` feature-detected per store because the value is undocumented | Kept - defensible. |
| `99.99.99.0` | `McpServer\Directory.Build.props` | the developer-build marker the add-in's `UpdateService` skips self-updating | documented as a cross-component contract | Kept - defensible; the add-in half owns the mirror. |
| `Assert.Equal(21, names.Length)` | `T3\SoakToolSurfaceCiTests` | the advertised tool count | - | Kept - defensible. An intentional pin: adding a tool is a reviewed decision. |
| `net48;net10.0-windows` | `OutlookAI.Core.csproj` | Core's dual target | referenced from the code that depends on it (`PumpedStaRunner`, `HealthReporting`) | Kept - defensible. It is load-bearing: net48 is why Core uses `Stopwatch` rather than `Environment.TickCount64`, and why `ComOperationBudgets` holds only constants. |
