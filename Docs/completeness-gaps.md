# OutlookAI MCP - completeness gap map

**Why this file exists.** The product's worst possible failure is a search that quietly returns less
than it should - worse than being slow, worse than erroring, because nothing downstream can tell.
Several such defects were found on 2026-08-17/18 (date literals parsed in the wrong locale, a
freshness sweep anchored to the wrong account's frontier, coverage counters pooled across accounts),
so the whole surface was audited read-only for the same species.

**How to read the severity column.** **SILENT** means nothing in the payload says the answer is
partial - an agent cannot know. **PROSE** means only `advice` or the tool description mentions it, so
an agent that reads fields rather than sentences is not told. **REPORTED** means a field an agent can
branch on. The standing rule is that a partial answer must be reported in a form software can act on,
so PROSE is a defect too, merely a lesser one.

**Status.** This is the map as audited. Items get closed against it; a row is not removed when fixed,
it is marked, so the history of what was silent stays legible.

**How a closed row is marked.** The Gap cell keeps its original wording and gains a
`**CLOSED <date>** — <what the payload says now>` sentence; the severity cell becomes
`~~OLD~~ → **NEW**`. Nothing is rewritten, so the row still reads as the defect it was.

Closed so far: **A1**, **A2**, **A3**, **A4**, **B1**, **B3**, **B6**, **C1**, **C2**, **C3**, **E1**, **F1**, **F5**, **G1**,
**G6**, **H1**, **I1** (2026-08-18), and **C4** and **A5** downgraded from SILENT to REPORTED by C1's and
A4's work. **B6 was not in the original audit** - it came out of the COM-host boundary work,
and it is added as a row rather than fixed quietly, because the map is meant to record what
was silent and not only what was looked for.

**A2, A3 and E1 were closed by `79c1827` on 2026-08-18 and this map did not say so** for the
rest of that day, because that commit touched no row here. They were re-verified against the
code and against T1 before being marked, rather than taken on the commit message's word - and
the same verification is what turned up A1's residue, which the commit's own summary reads as
having covered. A map that lags the code invites exactly one wasted day per stale row, so the
lesson is recorded next to the rows: **a row is marked in the commit that closes it.**

**B3 closed 2026-08-18** by maintainer decision ("unify all three, and prefer returning EVERYTHING where
possible"): item class no longer excludes anything in any tier, and the two counters that were missing
now reach the payload. The earlier note here said an `index` block on `SearchOutcome` had been declined
to keep the `search` description inside its 2048-unit client budget; that turned out to be a cost that
does not exist - the budget is per STRING, a payload block needs no description text, and `search`
measures 1791 units before and after. `index.rowsDropped`, `index.rowsScanned` and
`index.candidatesExhausted` are the block.

Working tree was NOT clean at audit start (see notes at end). No file in the repo was modified by this audit.

Severity key:
- **SILENT** — nothing in the payload says the answer is partial.
- **PROSE** — only `advice` (or a tool description) mentions it.
- **REPORTED** — a field an agent can branch on.

All paths relative to `c:\Source\SixFive7\OutlookAI\McpServer\`.

## 1. Empty / partial index for a whole store (the flagged Hyper-V PST case)

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| A1 | No index rows in scope, so the sweep window silently becomes "last 7 days". Everything older in that store is reachable by neither tier. **CLOSED 2026-08-18**, in two commits, and the second half is the interesting one. `79c1827` gave the state a name: `sweep.indexFrontierMissing`, the `no_index_frontier` coverage code with its own advice sentence, `sweep.storesWithoutIndex` naming the stores, `freshness:"partial"` and `degraded:true` - plus `sweep.gapStartUtc`, which is the fallback boundary itself. That fired on the measured all-PST machine because the PROFILE-wide frontier probe came back empty there, setting the flag before anything else ran. **The residue it left, found and closed here:** on a MIXED profile - one indexed mailbox plus one unindexed data file, the ordinary archive-PST shape - the profile frontier measures fine and the per-store loop walks only the index's own CATALOG, which has never heard of that data file. The single thing that noticed it was a probe pass over the store names the SWEEP RESULT carried, which runs after the sweep, so all three paths where the sweep does not run stayed silent: the sweep was NOT NEEDED (a `before` bound older than the fallback span - which is how an agent searches for OLD mail, i.e. exactly what such a store holds - answering `freshness:"live"` with `degraded` absent, the very sentence `79c1827`'s own message claims to have retired); the sweep was REFUSED (`to` filter, attachment-only); or the sweep FAILED. Those three now source the profile's store list from Outlook instead of from the sweep they never got (`MailService.NoteProfileStoresWithoutIndex`), and the verdict is the pure `StoresMissingFromIndex` that closed A3, so "the index holds nothing for this store" is ONE rule in this server rather than two. Costs nothing on the store-scoped path (its own frontier probe already settled it) and nothing on the ordinary path (still the sweep's own counters); only a sweep that did not happen pays one 5-minute-cached COM store-list read. Reproduced first in T1 `UnindexedStoreReportingTests` - 4 of 8 failing before, all 8 after. | `OutlookAI.Core/Services/MailService.cs:880` (`?? DateTime.UtcNow - EmptyIndexSweepWindow`), constant at `:112`; now `MailService.NoteStoresWithoutIndex` / `NoteProfileStoresWithoutIndex` + `FreshMerge.GapNoIndexFrontier` | `freshness:"live"`, `degraded` **absent**, `sweep.performed:true`, `sweep.foldersSwept:4`, `sweep.coverageGaps` absent, `staleness.newestIndexedUtc` **absent** (nulls omitted, `OutlookTools.cs:54`), `staleness.ageMinutes` absent, no advice (`DescribeStaleIndex` returns null on a null age, `MailService.cs:627-631`) | ~~SILENT~~ -> **REPORTED** (`sweep.indexFrontierMissing`, `sweep.storesWithoutIndex`, `no_index_frontier`, `degraded`, `freshness`), on every path a sweep can take |
| A2 | `outlook_health` reports "Index is current; searches run at index speed." and `status:"ok"` when the index holds zero mail rows. **CLOSED 2026-08-18 by `79c1827`** - verified against the code and against T1 `IndexHonestyTests` on 2026-08-18, not re-fixed here. A null frontier and a null age were being read as "no lag", when they are also what an index holding no mail AND an index nobody could read both report. `MailService.ClassifyIndexCurrency` now tells the three apart in one pure place (`Measured` / `NoMailAtAll` / `Unavailable`): the "index is current" sentence needs `Measured` plus a real age before it may be said, and `NoMailAtAll` raises a PROBLEM naming the fallback window, the four folders it covers and the two things to check - so `status` reads `degraded`. Measured on the all-PST machine: `status:"degraded"` with two problem lines. | `MailService.cs:3768-3772` (fires when `advice.Count==0 && ageMinutes==null`); now `MailService.ClassifyIndexCurrency` + the `NoMailAtAll` problem in `Health()` | `index.newestIndexedUtc` absent, `index.perStore: []`, `status:"ok"`, advice says current | ~~SILENT (worse: actively wrong prose)~~ -> **REPORTED** (`status:"degraded"` + a problem sentence; the false claim is unreachable) |
| A3 | `index.perStore` is built from the index-derived catalog, so a mounted store with no index rows never appears; nothing compares it against `outlook.stores` (the COM list). **CLOSED 2026-08-18 by `79c1827`** - verified against the code and against T1 `IndexHonestyTests` on 2026-08-18, not re-fixed here. Both lists were already in hand and `AddStoresMissingFromIndex` now compares them: every store Outlook reports that the index holds nothing for gains an `index.perStore` row with `inLocalIndex:false`, and a problem names them all with the remedy. The comparison is by PROBE, never by name - absence from the 2000-row unordered catalog sample is not evidence, and a delegate mailbox is indexed under its owner's subtree - so a store the budget could not settle is reported neither way, and the comparison is skipped entirely when the per-store loop was cut short or Outlook gave no list. Measured on the all-PST machine: `index.perStore: [{"store":"Outlook Data File","inLocalIndex":false}]`. | `MailService.cs:3727-3747`, catalog at `:4403-4409`; now `MailService.StoresMissingFromIndex` + `AddStoresMissingFromIndex` | Two lists that disagree, with no field naming the disagreement | ~~SILENT~~ -> **REPORTED** (`index.perStore[].inLocalIndex:false` + a problem naming the stores) |
| A4 | Store-scoped search of a store absent from the index catalog throws instead of degrading to a COM-only answer. Also fires for a small-but-indexed store the unordered 2000-row discovery sample missed. **CLOSED 2026-08-18** - the scope is resolved against the profile OUTLOOK has, with the index used only to decide what the index can contribute. Reproduced first in T1 (`UnindexedStoreScopeTests`) against a stand-in index client whose store catalog is EMPTY, and a second that omits one store the profile has - the two fixtures no test had, which is why this shipped. A store the profile HAS now SEARCHES: the index tier is skipped rather than run unscoped, the freshness sweep is the only tier, and the answer reports that through machinery that already existed and already fired here on the unscoped path - `index.storeNotIndexed: true`, `scope.shape: "store_not_indexed"`, `sweep.storesWithoutIndex`, `no_index_frontier`, `degraded: true`, `freshness: "partial"`. A store the profile does NOT have is still refused, through the same pure `MailService.DescribeUnresolvedFolderStore` that closed G1, so the two tools refuse one mistake in one shape and the message names the stores that DO exist. Outlook unreachable is a third answer that says the two cases cannot be told apart, rather than picking one. NOT widened: `search`'s `store` filters the result set (unlike `thread`'s, which only speeds a lookup up), so widening would answer with another account's mail under a scope the caller chose. | `MailService.cs` `ResolveFolderScope` + `TryGetProfileStoreNames`; `Services/FolderScopeResolver.ForUnindexedStore`; sample SQL `IndexSearch/WsSqlBuilder.cs` (no ORDER BY) | `ArgumentException`: "Store 'X' was not found in the local index. Known stores: ..." (empty list on an all-PST profile) | ~~REPORTED (error), but misleading~~ -> **fixed** (it searches) **+ REPORTED** (`index.storeNotIndexed`, `scope.shape`) |
| A5 | The fact is machine-readable only in a different tool: `list_accounts` returns `inLocalIndex:false`, `locallySearchable:false`. `search` never reads or echoes it. **DOWNGRADED 2026-08-18** - `search` now echoes it both ways round: `sweep.storesWithoutIndex` names the stores an UNSCOPED search swept that the same probe reports as unindexed, and A4's fix adds `index.storeNotIndexed` on a store-SCOPED one, which was the case that could not report anything because it threw. Still not a per-store echo of the whole profile, which is what A3 is about. | `MailService.cs:3874-3893`, `ProbeStoreInIndex` `:4243-4277` | Correct field — in a tool the agent may never call | **REPORTED (wrong tool)** |

## 2. Freshness scope

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| E1 | An UNSCOPED search opens one sweep window from the PROFILE-wide frontier. A store whose own frontier lags by hours gets a window of minutes; its recent mail falls outside both tiers. **CLOSED 2026-08-18 by `79c1827`** - verified against the code and against T1 `SweepScopeAndWindowTests` on 2026-08-18, not re-fixed here. `MailService.ResolveSweepWindows` probes a frontier per catalog store and hands the sweep one window each (`SweepFoldersNewerThan`'s per-store map, matched case-insensitively, earlier spelling wins); a store-scoped search was already right and is untouched. The store the catalog does NOT know gets `EmptyIndexSweepWindow` rather than the profile frontier - the one store whose gap is unmeasured must not get the narrowest window on the machine - and the fact is reported through A1's machinery. `staleness.oldestStoreFrontierUtc` reports the worst store in scope, and `sweep.gapStartUtc` reports the widest window the sweep ACTUALLY opened rather than the planned one. Measured cost, maintainer-approved: 33-39 ms per unscoped search on a two-store catalog, against two frontiers 11 min 19 s apart - i.e. the single window was eleven minutes short for one store, every time, silently. | `MailService.cs:435-437` plus `StalenessScopeFor` `:607-609` (null when no store is named); window at `:880`; now `MailService.ResolveSweepWindows` + `OutlookComSession.NormalizeSweepWindows`/`WindowFor` | `staleness.newestIndexedUtc` = the profile value, `freshness:"live"`, `degraded` absent | ~~SILENT~~ (documented in `McpServer/README.md`, invisible in the payload) -> **fixed** (one window per store) **+ REPORTED** (`staleness.oldestStoreFrontierUtc`, `sweep.gapStartUtc`) |
| E2 | The default sweep set is four arrival-path folders only, and shallow (no subfolders). Mail a rule files elsewhere before indexing is in neither tier. | `Com/OutlookComSession.cs:1268-1271` (`DefaultSweepFolderKinds`), shallow `SweepFolder` call at `:1633-1636` | `sweep.scope:"default folders (Inbox, Sent Items, Deleted Items, Junk Email)"` — a human string, not a flag; `freshness:"live"`, `degraded` absent, no advice | **PROSE** |
| E3 | A cached sweep (up to 10 s old) can miss an arrival that happened inside the TTL. | `Services/SweepCache.cs:31`, use at `MailService.cs:951-960` | `sweep.cached:true`, `sweep.cacheAgeSeconds` — but `freshness:"live"`, not degraded | **REPORTED (weak)** |

## 3. Tier asymmetries (index vs freshness sweep vs exhaustive)

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| B1 | `from` matches the ADDRESS only in the index tier. `System.Message.FromName` is SELECTed and never used in a predicate; the sweep and exhaustive tiers both match name OR address. The tool description promises "address or name fragment". **CLOSED 2026-08-18** — the predicate is now `(CONTAINS(FromAddress,…) OR CONTAINS(FromName,…))`; `IndexQuery.FromAddressContains` renamed `SenderContains` so the field cannot re-acquire the old meaning. Measured on this index: FromName alone 18 ms vs FromAddress alone 42 ms for the same fragment, the OR pair -19 to +12 ms on agent-sized TOP 26 statements; recall `vleuterweide` 0 → 18 rows, `subsidiewijzer` 0 → 9. 218 of 419 distinct senders (52%) in a 3 000-row sample carry a display-name token absent from their address. | `IndexSearch/WsSqlBuilder.cs:192-196` (only `CONTAINS(System.Message.FromAddress...)`), `:47` (FromName in SELECT); sweep `MailService.cs:1027-1031`; exhaustive `MailService.cs:1122-1126`; description `OutlookTools.cs:133` | A full-looking result set built from the sweep window alone; `freshness:"live"`, `degraded` absent | ~~SILENT~~ → **fixed** (all three tiers agree) |
| B3 | Item-class admission differs per tier and is never counted. Index: message rows need `System.Kind` to contain `email` (meeting requests index as `calendar`, so they are dropped). Sweep: **no class filter at all**. Exhaustive: `PR_MESSAGE_CLASS like 'IPM.Note%'` AND `Class==43`, dropping meeting requests/responses, NDRs and read receipts (`REPORT.IPM.Note.*`), `IPM.Post`, `IPM.Sharing`. **CLOSED 2026-08-18** - unified UPWARDS, to the widest of the three: the sweep, which never filtered. The rule is one place, `Mapi/MailItemAdmission.cs`, and it is that **an item's class never excludes it** - written as a method that cannot return false, so a future narrowing has to delete a call site and a test that both say what is being given up. An allowlist of "mail-ish" classes was considered and rejected on a fact that decides it: the SystemIndex carries no message-class column, so an allowlist could only ever be enforced in the COM tiers and would have replaced one asymmetry with another. Per tier: the exhaustive scan's DASL class clause and its `Class == 43` gate are gone (`Com/ExhaustiveDaslFilter.cs`, `Com/OutlookComSession.cs`); the sweep is unchanged and is the tier the others were unified to; the index tier admits message-level rows whatever their kind (`KindFilter` renamed with the rule - `MessagesAndAttachments` / `MessagesOnly` / `AttachmentsOnly`, plus `MailKindOnly` which no search uses). **What no tier can reach, now reported rather than implicit:** the COM tiers only enter folders whose `DefaultItemType` is `olMailItem`, which is unchanged and is not a class filter; the index tier has no folder-type column, so its widening also admits the calendar and contact items of folders the COM tiers never open - over-return, visible per hit. **The one way that over-return could have COST mail, closed 2026-08-18 in the same week:** those calendar and contact rows carry no `System.Message.DateReceived`, and the statement is `SELECT TOP n ... ORDER BY System.Message.DateReceived DESC`, so the provider's NULL collation - never measured here - decided whether they filled the `n` and pushed real mail behind the cut, silently and with a full-looking result list. `IndexSearch/IndexOrderGuard.cs` makes that impossible without knowing the collation: rows the ordering cannot rank are trimmed last rather than first, and where the statement was truncated with such a row in it the same statement is re-run with a predicate admitting only rows that carry the ordering column, then unioned. A dated meeting request can still push an older mail off the end, which is the decision rather than a defect. | `IndexSearch/IndexRowFilter.cs`, `WsSqlBuilder.cs`; `Com/OutlookComSession.cs`; `Com/ExhaustiveDaslFilter.cs`; rule in `Mapi/MailItemAdmission.cs` | Nothing. `IndexSearchResult.RowsDropped` exists (`IndexSearch/IndexSearchService.cs:47`) but is never copied into `SearchOutcome` | ~~SILENT~~ -> **fixed** (one rule) **+ REPORTED** (per-hit `itemClass`, `index.rowsDropped`, and an advice sentence when an answer holds non-mail) |
| B2 | Attachment text is index-only. The refused attachment-ONLY case is properly flagged; the DEFAULT case is not — a term inside an attachment of just-arrived mail is invisible while the answer says `freshness:"live"`. | `Services/FreshMerge.cs:256-272`; default path `MailService.cs:464-505` | attachment-only: `sweep.error`, `freshness:"index-only"`, `degraded:true` (**REPORTED**). Default: nothing | **PROSE** (default case) |
| B4 | `search_in:"body"` means body **plus attachment content** in the index tier, `MailItem.Body` in the sweep, `urn:schemas:httpmail:textdescription` in exhaustive. | `WsSqlBuilder.cs:448-462`; `FreshMerge.cs:300-330`; `ExhaustiveDaslFilter.cs:44` | Nothing in the payload | **PROSE** (README only) |
| B5 | Whole-word (index) vs substring (sweep, and exhaustive's LIKE fallback). | `FreshMerge.cs:300-330`; `ExhaustiveDaslFilter.cs:113-130` | exhaustive: `exhaustive.engine`, `exhaustive.instantSearchEnabled` plus advice (**REPORTED**). The sweep's over-match is the safe direction | **REPORTED / benign** |
| B6 | The ordering the caller asked for did not survive the MERGE. `IndexOrder.SizeDescending` reached the provider correctly, but the combined list (index hits + sweep hits, or the exhaustive walk) was then re-sorted by `ReceivedUtc` unconditionally - and the `top` trim happens AFTER that sort, so a size-ordered search returned the newest `top` rather than the largest, and the biggest mail could be trimmed off the end. The same line stood in both tiers. Recorded 2026-08-18, alongside the COM-host boundary work; not reachable from the tool surface, since `search` exposes no order parameter and only a T2 test sets the flag - so no answer any caller has had was affected. It is written down anyway because the shape is the one this document is about: a correct query undone by a later line, with nothing in the payload to say the order was not the order asked for. **CLOSED 2026-08-18** - one `MailService.SortForOrder` decides both call sites; unknown keys sort last in both orders (an unmeasured size is not a zero-byte mail) and size ties fall back to date, because `List.Sort` is not stable and two equally large mails should not swap between runs of the same query. Pinned by T1 `MergedResultOrderTests`. | `Services/MailService.cs:612` (`Search`) and `:2082` (`RunExhaustive`); order chosen at `:485`; flag at `MailModels.cs:92` | Nothing - a correctly sized, full-looking list in the wrong order | ~~SILENT~~ -> **fixed** (one comparator, both tiers) |

## 4. `thread`

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| C1 | `thread` runs NO freshness sweep and carries NO freshness fields. If the index has one or more rows for the conversation the COM walk never runs, so replies newer than the frontier are omitted. **CLOSED 2026-08-18** — the COM conversation walk is no longer a fallback: it runs on every call that has an anchor `id`, and its members are merged and deduped with the index rows (`FreshMerge.SelectFreshOnly`). `thread` now carries `degraded`, `freshness`, `staleness`, a `live` block and `advice`, on the same three-value contract `search` has. Sweeping was chosen over reporting-only because a conversation is a scope Outlook enumerates directly — one `GetConversation` plus one table walk, no window, no folder set, no per-folder cap — bounded by `ThreadWalkBudgetMs` (= `SweepBudgetMs`, 30 s), plus one ~2 s `HitLocator` probe the first time a given hit id is used (cached after). | `MailService.cs:1495-1511`; model `Services/MailModels.cs:620-639` | `source:"index"`, `truncated:false`, and **no** `degraded`/`freshness`/`staleness` at all. Tool description: "Fetch the full conversation of a mail" (`OutlookTools.cs:175-180`) | ~~SILENT~~ → **REPORTED** (`degraded`, `freshness`, `live.coverageGaps`) |
| C2 | `thread`'s index path is `KindFilter.EmailOnly`, so a meeting-request member of the thread is dropped. **CLOSED 2026-08-18** — new `KindFilter.MessagesAnyClass`: message-level rows of every item class, no attachment rows. `search` keeps `EmailOnly` (there the caller asked for mail); a thread member Outlook puts in the conversation belongs in the conversation. **Half of that superseded by B3 (2026-08-18):** `search` no longer keeps the narrow shape - no tier filters by item class any more - and the enum members were renamed with the rule, so `MessagesAnyClass` is now `MessagesOnly` and `EmailOnly` is `MailKindOnly`, which only store-scope discovery uses. What C2 fixed still stands; only the names and the contrast with `search` have moved. | `MailService.cs:1489` | Nothing | ~~SILENT~~ → **fixed** (members no longer dropped) |
| C3 | An unresolvable `store` silently widens the conversation lookup to the whole profile. **CLOSED 2026-08-18** — the widening stays (over-return is the safe direction) and is now reported as `scopeWidened:true` plus an advice sentence naming the store. | `MailService.cs:1473-1483` (`catch (ArgumentException) { scope = null; }`) | Nothing (over-return: the safe direction) | ~~SILENT~~ → **REPORTED** (`scopeWidened`) |
| C4 | The COM fallback walks one item's `Conversation.GetTable()` — same-store members only. **DOWNGRADED 2026-08-18** — still same-store, but no longer silent: when the index rows for the conversation name a store the walk did not cover, `live.coverageGaps` raises `unwalked_store`, `freshness` is `"partial"` and advice says to pass an `id` from that account. Undetectable when the OTHER store has no index rows either, so this row stays open. | `Com/OutlookComSession.cs:1993-2060` | `source:"com"` only | ~~SILENT~~ → **REPORTED** when the index can see the other store; SILENT when it cannot |

## 5. `exhaustive:true`

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| F1 | `degraded` and `freshness` are never set on an exhaustive result, so a scan that timed out and skipped folders looks undegraded on the two fields the tool description teaches agents to branch on. **CLOSED 2026-08-18** — `FreshMerge.ClassifyExhaustiveFreshness` sets both from the scan's own counters. The scan IS the live check, so `"index-only"` is unreachable there by construction (it names the state the mode exists to avoid) and no fourth value was invented; `"live"` when it covered its scope, `"partial"` when it timed out, skipped folders **or** truncated. Truncation counts here and does not in the merged path: an indexed `truncated` caps a date-sorted complete match set and pages, while the exhaustive cap stops the folder walk in arbitrary order (F2) and cannot be paged past. | `MailService.cs:1198-1220` (the exhaustive `return new SearchOutcome` sets neither; contrast the indexed path at `:562-580`) | `exhaustive.timedOut/foldersSkipped/truncated` present (**REPORTED**), `degraded` and `freshness` **absent** | ~~PROSE / inconsistent contract~~ → **REPORTED** (same two flags as a merged search) |
| F2 | The scan never sorts the folder table, so the `maxItems` cap truncates in arbitrary order and there is no way to page past it (top is capped at 100). Contrast the sweep, which sorts newest-first. | `Com/OutlookComSession.cs:6873-6923` (no `t.Sort`) vs `:7045` | `truncated:true` plus "raise top" advice, which cannot help beyond 100 | **REPORTED but unactionable** |
| F3 | `from` / `unread_only` / `has_attachments` are applied AFTER the scan cap, so an exhaustive search can return 2 rows with `truncated:true` while thousands match. | `MailService.cs:1118-1140` | `truncated:true`; the advice does not say the filter ran post-cap | **PROSE** |
| F5 | Rows dropped inside the scan are never counted: `GetItemFromID` failure, `Class` read failure, non-`IPM.Note` class. **CLOSED 2026-08-18** - counted, and split by whether the drop was a FAILURE or a FILTER, because reporting them as one number would cry wolf on the mode working as designed. `exhaustive.rowsDropped` is every row not admitted, deliberately the same name and meaning as the index tier's `IndexSearchResult.RowsDropped` (one counter shape, not two); `exhaustive.rowsUnreadable` is the failure subset and is the one that raises `rows_unreadable`, `freshness:"partial"` and `degraded:true`. Their DIFFERENCE was exactly the item-class filter B3 is about, and measuring it was the point - **B3 then removed that filter on 2026-08-18, so on a real scan the difference is now zero by construction**, which is the machine-checkable statement that this tier admits every class. The two counters stay separate anyway, so a future non-failure drop cannot silently start degrading every scan that meets one. The exhaustive tier also gained `exhaustive.coverageGaps` in this work, on the same contract the sweep and the thread walk already carry - `time_budget` and `folders_skipped` reuse the sweep's tokens verbatim, `result_cap` gets its own because that cap loses whole unopened folders rather than truncating one folder's window - with `FreshMerge.DescribeExhaustiveCoverageGaps` as the classifier, `MailService.DescribeExhaustiveCoverage` as the single source of the prose (the three inline sentences moved there unchanged), and `ClassifyExhaustiveFreshness` recomputed FROM the codes so a hole cannot appear in one rendering and not the other. | `Com/OutlookComSession.cs:6890-6913` | Nothing | ~~SILENT~~ -> **REPORTED** (`exhaustive.rowsDropped`/`rowsUnreadable` + `coverageGaps`) |
| F4 | `ScanFolderTree` has no depth guard (unlike `SweepFolderTree` and `CollectFolders`) — a cyclic tree is an uncatchable StackOverflow. | `Com/OutlookComSession.cs:6763-6822` | n/a — robustness, not completeness | tangential |

## 6. Scope resolution that yields zero instead of an error

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| G1 | `list_folders(store:"typo")` returns an empty tree with no error. **CLOSED 2026-08-18** - a store scope that matched no folder is now resolved against the COM store list and refused by name: `ArgumentException` "Store 'typo' was not found in Outlook. Known stores: ... Use list_accounts for the full store list.", the same shape the index-tier resolver already threw for `search`. The list is consulted ONLY on the empty result, so an ordinary call pays nothing, and a store that IS mounted and simply has no folders below its root still answers an empty tree with no error - inventing a failure for it would be the same lie in the other direction. Classified by the pure `MailService.DescribeUnresolvedFolderStore`, pinned in T1 (unknown store / known-but-empty / case-insensitive / a profile reporting no stores at all). | `Com/OutlookComSession.cs:1186-1205`; `MailService.cs:3995-4004` | `stores: []`, `folderTotal: 0`, `truncated:false` | ~~SILENT~~ -> **REPORTED (error)** |
| G2 | A store whose `DisplayName` COM read fails is silently dropped from `list_folders`; in the sweep its four default folders count as skipped in the total but land in nobody's per-store bucket. | `OutlookComSession.cs:1197-1200`; sweep `:1580-1592` | list_folders: nothing. Sweep: unscoped searches see `foldersSkipped` (**REPORTED**); store-scoped ones cannot | **SILENT / partial** |
| G3 | `CollectFolders` / `CollectFolderPaths` truncate at `FolderWalkAbsoluteCap = 10 000` and depth 64 with no flag; `FoldersOutcome.truncated` is computed against the already-truncated list. | `OutlookComSession.cs:7611,7625,7669-7720`; cap at `MailService.cs:220` | `truncated:false` on a truncated tree | **SILENT** (low probability) |
| G4 | The delegate folder-NAME list comes from the same truncatable walk; a short list under-returns the delegate folder scope. | `MailService.cs:4373` into `OutlookComSession.ListFolderPaths:7451` | `scope.folderNamesMatched` (a count, not a completeness flag) | **SILENT** (low probability) |
| G5 | A folder path that resolves in COM but not in the index (localized/renamed folder, non-recursive `ItemFolderPathDisplay` mismatch) yields zero index rows. The C7 guard catches it — but only when the MERGED answer is completely empty, and only as advice. | `MailService.cs:697-720`, `Services/FolderScopeResolver.cs:296-306` | advice only; one swept item suppresses the guard entirely | **PROSE, conditional** |
| G6 | Index-tier candidate exhaustion (the post-filter ran out of over-fetched rows) is advice-only; `IndexSearchResult.CandidatesExhausted` is never copied into `SearchOutcome`. **CLOSED 2026-08-18** - it rides into the payload on the `index` block B3's work added, as `index.candidatesExhausted` (present only when true), beside `index.rowsScanned`/`index.rowsDropped`. `degraded` deliberately stays absent: exhaustion means the list MAY be short of matches, which is the same class of fact as `truncated`, not a freshness hole - and widening `degraded` to cover it would blunt the one flag the tool description tells agents to relay. The advice sentence is unchanged and still names the remedy. | `MailService.cs` (index block on the returned `SearchOutcome`) | an advice sentence; no field, and `degraded` stays absent | ~~PROSE~~ -> **REPORTED** (`index.candidatesExhausted`) |

## 7. Row- and item-level silent drops in the freshness sweep

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| H1 | A table row whose EntryID column is missing, or whose `GetItemFromID` fails, is skipped and does not count toward the per-folder cap. A folder where every row fails returns `SweepOutcome.Complete` with zero items. **CLOSED 2026-08-18** - such rows are counted (`SweepFolder`'s new `out int rowsUnreadable`), attributed to the store they were lost in (`ComStoreSweepCounters.RowsUnreadable`, so a cached all-stores sweep cannot degrade a different account's search) and reported as `sweep.rowsUnreadable` plus the `rows_unreadable` coverage code with its own advice sentence, making the search `freshness:"partial"`, `degraded:true`. The folder is still counted as swept, which is true - it was enumerated; the hole is one level in, at the row, which is exactly why no folder counter could express it. | `Com/OutlookComSession.cs:7060-7089` | `foldersSwept` counts that folder as fully covered | ~~SILENT~~ -> **REPORTED** (`sweep.rowsUnreadable` + `rows_unreadable`) |
| H2 | `t.Sort(...)` failure is swallowed; if the item cap then fires, the `item_cap` advice claims "reads newest-first, so the OLDEST ... is not covered", which is then false. | `OutlookComSession.cs:7045-7050`; advice `MailService.cs:801-807` | `item_cap` gap code (**REPORTED**) with a wrong explanation | **REPORTED, prose wrong** |
| I1 | `unread_only`, `has_attachments`, `after`, `before` all DROP a swept item when the underlying COM property could not be read (`IsRead`/`HasAttachments`/`ReceivedTime` null). **CLOSED 2026-08-18** - the DROP STANDS and is now reported. Dropping is the deliberate half of the decision: a filter the caller asked for has to be honoured, and admitting an item that cannot be shown to match would corrupt the answer in the opposite direction (an agent asking for unread mail relaying read mail as unread). The reporting is the other half: `sweep.itemsFilterUnreadable` counts them, `sweep.filtersUnevaluated` names the request parameters that could not be evaluated - so the remedy is literally "re-run without this one" - and `filter_unreadable` makes the search `partial`/`degraded` with its own advice sentence. The same drop in the exhaustive post-filter is instrumented identically (`exhaustive.itemsFilterUnreadable`, same token); only `unread_only`/`has_attachments` are reachable there, since that mode's date bounds go into the DASL filter. NOT SETTLED HERE: a null `ReceivedTime` is ambiguous - `TryGetDateTime` maps Outlook's 4501-01-01 "no value" to null exactly as it maps a failed read - so the advice says "no usable value", never "the read failed". Reporting it is what will settle it, since the code names the filter and counts the items every time it fires. | `MailService.cs:1033-1055` | Nothing | ~~SILENT~~ -> **REPORTED** (`sweep.itemsFilterUnreadable` + `filtersUnevaluated` + `filter_unreadable`) |

## 8. Caps that ARE properly reported (for map completeness)

| Cap | Value | Field |
|---|---|---|
| `top` | 1-100, default 25 | `truncated:true` (over-fetch-by-one, so definite) plus clamp advice (`MailService.cs:646-659`) |
| `SweepPerFolderCap` | 200 | `sweep.itemCappedFolders` plus `item_cap` in `sweep.coverageGaps` |
| Sweep rows that would not open | — | `sweep.rowsUnreadable` plus `rows_unreadable` in `sweep.coverageGaps` (H1) |
| Swept/scanned items an unevaluable filter dropped | — | `sweep.itemsFilterUnreadable` + `sweep.filtersUnevaluated` plus `filter_unreadable` (I1); same three on `exhaustive.*` |
| Exhaustive rows that would not open | — | `exhaustive.rowsUnreadable` (raises `rows_unreadable`) and `exhaustive.rowsDropped` (the total; since B3 removed the class filter the two are equal by construction) |
| Index rows the tier refused / ran out of | — | `index.rowsScanned`, `index.rowsDropped`, `index.candidatesExhausted` (B3, G6) |
| `UnindexedStoreListCap` | 12 | `sweep.storesWithoutIndexTruncated` + `sweep.storesWithoutIndexTotal`, and the `no_index_frontier` advice sentence names the cap (Q7b) |
| `NonMailClassAdviceCap` | 4 | distinct item classes the "not ordinary mail" advice names before ", ..."; the per-hit `itemClass` fields are the complete answer |
| `MaxScopedSweepFolders` / time budget / depth | 40 / 2000 ms / 64 | `sweep.folderCapReached` / `timeBudgetExceeded` / `depthLimitReached` plus gap codes |
| `SweptFolderListCap` | 12 | `sweep.folderListOmitted:true` |
| Sweep could not run | — | `freshness:"index-only"`, `degraded:true`, `sweep.error` - and `no_index_frontier` beside it where a store has no index tier either (A1), because the two are independent facts with different remedies |
| Sweep not needed | — | `sweep.notNeeded:true`, `freshness:"live"` - except over a scope with no index frontier, where "the index already covers this window" is false: then `freshness:"partial"`, `degraded:true` and `no_index_frontier` (A1) |
| Sweep refused (recipient filter, attachment-only) | - | `sweep.error`, `freshness:"index-only"`, `degraded:true`, plus `no_index_frontier` where it applies (A1) |
| Stores the index holds no mail for | - | `sweep.indexFrontierMissing`, `sweep.storesWithoutIndex` (+ the cap pair above), `no_index_frontier`; the same fact in `outlook_health` as `index.perStore[].inLocalIndex:false` (A3) and in `list_accounts` as `inLocalIndex`/`locallySearchable` (A5) |
| Delegate widening / leaf collision | 40 paths | `scope.shape`, `scope.widened`, `scope.folderNamesMatched` plus advice |
| `read` body/html/headers/recipients/attachments | 500k/100k/64k/100/100 | `bodyTruncated`+`bodyTotalChars`, `bodyHtmlTruncated`+`bodyHtmlTotalChars`, `headersTruncated`, `recipientsTruncated`+`recipientsTotal`, `attachmentsTruncated`+`attachmentsTotal` |
| `list_folders` page | 1000 | `truncated` plus `nextOffset` plus `folderTotal` |
| `thread` top | 1-200 | `truncated`; it caps the LIVE walk too, so a capped walk also raises `member_cap` in `live.coverageGaps` with `freshness:"partial"` (C1) |
| `thread` live walk could not run | — | `freshness:"index-only"`, `degraded:true`, `live.error` (`NoAnchorItem` / `UnknownAnchorId` / `AnchorNotLocatable` / COM token) plus advice (C1) |
| Exhaustive scan cap / time budget / skipped folders | `top` / 105 s / — | `exhaustive.truncated` / `timedOut` / `foldersSkipped`, and since 2026-08-18 `degraded:true` + `freshness:"partial"` (F1), plus `exhaustive.coverageGaps` codes `result_cap` / `time_budget` / `folders_skipped` (F5) |
| `move_mail` ids | 50 | error |
| `UnresolvedRecipientsCap` (drafts) | 20 | **no flag** — `Take(20)` at `MailService.cs:2374`, tangential to search |
| `snippet_chars` | clamped 0-1000 | silent clamp, cosmetic |

## Notes on the working tree

At audit start `git status` reported (contrary to the session snapshot):

    M  McpServer/OutlookAI.McpServer.Tests/T2/LiveStoreCountTripwire.cs   (+14 lines)
    ?? McpServer/OutlookAI.McpServer.Tests/T1/LiveOutlookPreflightTests.cs
    ?? McpServer/OutlookAI.McpServer.Tests/T2/LiveOutlookPreflight.cs

mtimes 08:43-08:52 local, i.e. minutes before this audit began; all three were already present in the first file listing this audit made. They account for the 1393 to 1416 test-count delta. Nothing here was written by this audit.
