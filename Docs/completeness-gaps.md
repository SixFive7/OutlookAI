# OutlookAI MCP - completeness gap map

**Why this file exists.** The product's worst possible failure is a search that quietly returns less
than it should - worse than being slow, worse than erroring, because nothing downstream can tell.
Several such defects were found on 2026-08-17/18 (date literals parsed in the wrong locale, a
freshness sweep anchored to the wrong account's frontier, coverage counters pooled across accounts),
so the whole surface was audited read-only for the same species.

**How to read the severity column.** **SILENT** means nothing in the payload says the answer is
partial - an agent cannot know. **PROSE** means only `advice` or the tool description mentions it, so
an agent that reads fields rather than sentences is not told. **REPORTED** means a field an agent can
branch on. The standing rule is that a partial answer must be reported in a form software can act on,
so PROSE is a defect too, merely a lesser one.

**Status.** This is the map as audited. Items get closed against it; a row is not removed when fixed,
it is marked, so the history of what was silent stays legible.

**How a closed row is marked.** The Gap cell keeps its original wording and gains a
`**CLOSED <date>** — <what the payload says now>` sentence; the severity cell becomes
`~~OLD~~ → **NEW**`. Nothing is rewritten, so the row still reads as the defect it was.

Closed so far: **A1**, **A2**, **A3**, **A4**, **B1**, **B2**, **B3**, **B6**, **C1**, **C2**, **C3**, **C4**, **E1**, **E2**, **F1**, **F3**,
**F4**, **F5**, **G1**, **G2**, **G3**, **G4**, **G5**, **G6**, **H1**, **H2**, **I1** (2026-08-18), **F2** (2026-08-19),
**J1**-**J9** (2026-08-20, section 7b - the write surface's own version of the same species), and **A5** downgraded
from SILENT to REPORTED by A4's work. **B6 was not in the original audit** - it came out of the COM-host boundary work,
and it is added as a row rather than fixed quietly, because the map is meant to record what
was silent and not only what was looked for. **C5 is new and OPEN**, found while closing C4 and added
for the same reason. **H3 is new and OPEN** (2026-08-19), found while building the measurement corpus rather than by
audit: the freshness sweep selects on two date properties, and mail carrying neither - imported, copied,
restored, or simply unsent - is invisible to the tier while the payload reports the folder as swept.

**`UnresolvedRecipientsCap` (section 8) was closed 2026-08-18** and it deserved better than its
"tangential to search" label. Two sites took the first 20 unresolvable addresses with no
truncation flag and no total, two lines below a resolved recipient list carrying exactly that
pair - so an agent told "these 20 did not resolve" out of 27 held a false-complete list on the
DRAFTING surface, where the remedy for an unresolved address is to ask the USER about it. The
seven past the cap were therefore never mentioned to anybody, and the draft carried a fault its
own report declared absent. One pure `MailService.CapUnresolvedRecipients` now cuts the list for
both tools and one pure `DescribeUnresolvedRecipientCap` renders the sentence from the same two
values; `unresolvedRecipientsTruncated` / `unresolvedRecipientsTotal` / `unresolvedRecipientsAdvice`
reach both draft payloads. Reproduced in T1 `UnresolvedRecipientReportingTests` (12 tests, of which
5 drive the real mapping methods; mutation-checked - dropping the payload pair fails 2, forcing the
sentence to null fails 2, and making the cap never report a cut fails 3).

**The freshness sweep's mail bodies were unbounded until 2026-08-18, and that was a cap-shaped hole with no cap in it.**
One `SweepFoldersNewerThan` answer is ONE frame with a hard 64 MB ceiling; `MailService` asks for it with
`includeBodies: true`; `SnapshotBrief` took `item.Body` WHOLE. The two constants that bound body size,
`BodyCharsDefault` and `BodyCharsCap`, live in `MailService` on the FAR side of that pipe, so they bounded what an
agent SEES and never what crosses. Nothing was silent about a truncation, because nothing truncated - the failure
mode was the opposite one: a frame too big to send, refused as a whole (and, before `0ee0cd0`, fatal to the COM
host), on the profile shape with nothing left to narrow. The maintainer chose to cap at the COM layer so an
unsendable frame cannot be built, rather than move `MaxFrameBytes` or chunk the result. The cap is the interesting
part for THIS map: these bodies are matched against and never shown, so unlike `read`'s windowing - which pages and
therefore loses nothing - a cut here can make a search MISS a real match. It is therefore reported like every other
cap in the server, and the reporting is gated on the intersection that can actually cost a hit (cut AND unmatched)
rather than on the cut, because a cut body on an item that matched anyway lost nothing and a code firing there would
blunt the ones that fire rarely. Row in section 8; T1 `SweepBodyCapTests` (18 tests) plus two wire round-trips in
`ComHostProtocolTests`, mutation-checked with 13 separate reverts, each disabling one decision.

**A2, A3 and E1 were closed by `79c1827` on 2026-08-18 and this map did not say so** for the
rest of that day, because that commit touched no row here. They were re-verified against the
code and against T1 before being marked, rather than taken on the commit message's word - and
the same verification is what turned up A1's residue, which the commit's own summary reads as
having covered. A map that lags the code invites exactly one wasted day per stale row, so the
lesson is recorded next to the rows: **a row is marked in the commit that closes it.**

**B3 closed 2026-08-18** by maintainer decision ("unify all three, and prefer returning EVERYTHING where
possible"): item class no longer excludes anything in any tier, and the two counters that were missing
now reach the payload. The earlier note here said an `index` block on `SearchOutcome` had been declined
to keep the `search` description inside its 2048-unit client budget; that turned out to be a cost that
does not exist - the budget is per STRING, a payload block needs no description text, and `search`
measures 1791 units before and after. `index.rowsDropped`, `index.rowsScanned` and
`index.candidatesExhausted` are the block.

Working tree was NOT clean at audit start (see notes at end). No file in the repo was modified by this audit.

Severity key:
- **SILENT** — nothing in the payload says the answer is partial.
- **PROSE** — only `advice` (or a tool description) mentions it.
- **REPORTED** — a field an agent can branch on.

All paths relative to `c:\Source\SixFive7\OutlookAI\McpServer\`.

**Cite by SYMBOL, not by line number.** Checked 2026-08-19: every one of the nine
`OutlookComSession.cs` line references this map carried had drifted off its subject - H3's by
about 100 lines, and the rest by between 60 and 900 - and several `MailService.cs` ones had too.
A stale line number is worse than none, because it reads as evidence and quietly points the next
reader at unrelated code. The `OutlookComSession.cs` column has been converted to method names
below; the remaining `MailService.cs` / `MailModels.cs` / `OutlookTools.cs` numbers are known
stale and are converted as each row is next touched (`TODO.md`).

## 1. Empty / partial index for a whole store (the flagged Hyper-V PST case)

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| A1 | No index rows in scope, so the sweep window silently becomes "last 7 days". Everything older in that store is reachable by neither tier. **CLOSED 2026-08-18**, in two commits, and the second half is the interesting one. `79c1827` gave the state a name: `sweep.indexFrontierMissing`, the `no_index_frontier` coverage code with its own advice sentence, `sweep.storesWithoutIndex` naming the stores, `freshness:"partial"` and `degraded:true` - plus `sweep.gapStartUtc`, which is the fallback boundary itself. That fired on the measured all-PST machine because the PROFILE-wide frontier probe came back empty there, setting the flag before anything else ran. **The residue it left, found and closed here:** on a MIXED profile - one indexed mailbox plus one unindexed data file, the ordinary archive-PST shape - the profile frontier measures fine and the per-store loop walks only the index's own CATALOG, which has never heard of that data file. The single thing that noticed it was a probe pass over the store names the SWEEP RESULT carried, which runs after the sweep, so all three paths where the sweep does not run stayed silent: the sweep was NOT NEEDED (a `before` bound older than the fallback span - which is how an agent searches for OLD mail, i.e. exactly what such a store holds - answering `freshness:"live"` with `degraded` absent, the very sentence `79c1827`'s own message claims to have retired); the sweep was REFUSED (`to` filter, attachment-only); or the sweep FAILED. Those three now source the profile's store list from Outlook instead of from the sweep they never got (`MailService.NoteProfileStoresWithoutIndex`), and the verdict is the pure `StoresMissingFromIndex` that closed A3, so "the index holds nothing for this store" is ONE rule in this server rather than two. Costs nothing on the store-scoped path (its own frontier probe already settled it) and nothing on the ordinary path (still the sweep's own counters); only a sweep that did not happen pays one 5-minute-cached COM store-list read. Reproduced first in T1 `UnindexedStoreReportingTests` - 4 of 8 failing before, all 8 after. | `OutlookAI.Core/Services/MailService.cs:880` (`?? DateTime.UtcNow - EmptyIndexSweepWindow`), constant at `:112`; now `MailService.NoteStoresWithoutIndex` / `NoteProfileStoresWithoutIndex` + `FreshMerge.GapNoIndexFrontier` | `freshness:"live"`, `degraded` **absent**, `sweep.performed:true`, `sweep.foldersSwept:4`, `sweep.coverageGaps` absent, `staleness.newestIndexedUtc` **absent** (nulls omitted, `OutlookTools.cs:54`), `staleness.ageMinutes` absent, no advice (`DescribeStaleIndex` returns null on a null age, `MailService.cs:627-631`) | ~~SILENT~~ -> **REPORTED** (`sweep.indexFrontierMissing`, `sweep.storesWithoutIndex`, `no_index_frontier`, `degraded`, `freshness`), on every path a sweep can take |
| A2 | `outlook_health` reports "Index is current; searches run at index speed." and `status:"ok"` when the index holds zero mail rows. **CLOSED 2026-08-18 by `79c1827`** - verified against the code and against T1 `IndexHonestyTests` on 2026-08-18, not re-fixed here. A null frontier and a null age were being read as "no lag", when they are also what an index holding no mail AND an index nobody could read both report. `MailService.ClassifyIndexCurrency` now tells the three apart in one pure place (`Measured` / `NoMailAtAll` / `Unavailable`): the "index is current" sentence needs `Measured` plus a real age before it may be said, and `NoMailAtAll` raises a PROBLEM naming the fallback window, the four folders it covers and the two things to check - so `status` reads `degraded`. Measured on the all-PST machine: `status:"degraded"` with two problem lines. | `MailService.cs:3768-3772` (fires when `advice.Count==0 && ageMinutes==null`); now `MailService.ClassifyIndexCurrency` + the `NoMailAtAll` problem in `Health()` | `index.newestIndexedUtc` absent, `index.perStore: []`, `status:"ok"`, advice says current | ~~SILENT (worse: actively wrong prose)~~ -> **REPORTED** (`status:"degraded"` + a problem sentence; the false claim is unreachable) |
| A3 | `index.perStore` is built from the index-derived catalog, so a mounted store with no index rows never appears; nothing compares it against `outlook.stores` (the COM list). **CLOSED 2026-08-18 by `79c1827`** - verified against the code and against T1 `IndexHonestyTests` on 2026-08-18, not re-fixed here. Both lists were already in hand and `AddStoresMissingFromIndex` now compares them: every store Outlook reports that the index holds nothing for gains an `index.perStore` row with `inLocalIndex:false`, and a problem names them all with the remedy. The comparison is by PROBE, never by name - absence from the 2000-row unordered catalog sample is not evidence, and a delegate mailbox is indexed under its owner's subtree - so a store the budget could not settle is reported neither way, and the comparison is skipped entirely when the per-store loop was cut short or Outlook gave no list. Measured on the all-PST machine: `index.perStore: [{"store":"Outlook Data File","inLocalIndex":false}]`. | `MailService.cs:3727-3747`, catalog at `:4403-4409`; now `MailService.StoresMissingFromIndex` + `AddStoresMissingFromIndex` | Two lists that disagree, with no field naming the disagreement | ~~SILENT~~ -> **REPORTED** (`index.perStore[].inLocalIndex:false` + a problem naming the stores) |
| A4 | Store-scoped search of a store absent from the index catalog throws instead of degrading to a COM-only answer. Also fires for a small-but-indexed store the unordered 2000-row discovery sample missed. **CLOSED 2026-08-18** - the scope is resolved against the profile OUTLOOK has, with the index used only to decide what the index can contribute. Reproduced first in T1 (`UnindexedStoreScopeTests`) against a stand-in index client whose store catalog is EMPTY, and a second that omits one store the profile has - the two fixtures no test had, which is why this shipped. A store the profile HAS now SEARCHES: the index tier is skipped rather than run unscoped, the freshness sweep is the only tier, and the answer reports that through machinery that already existed and already fired here on the unscoped path - `index.storeNotIndexed: true`, `scope.shape: "store_not_indexed"`, `sweep.storesWithoutIndex`, `no_index_frontier`, `degraded: true`, `freshness: "partial"`. A store the profile does NOT have is still refused, through the same pure `MailService.DescribeUnresolvedFolderStore` that closed G1, so the two tools refuse one mistake in one shape and the message names the stores that DO exist. Outlook unreachable is a third answer that says the two cases cannot be told apart, rather than picking one. NOT widened: `search`'s `store` filters the result set (unlike `thread`'s, which only speeds a lookup up), so widening would answer with another account's mail under a scope the caller chose. | `MailService.cs` `ResolveFolderScope` + `TryGetProfileStoreNames`; `Services/FolderScopeResolver.ForUnindexedStore`; sample SQL `IndexSearch/WsSqlBuilder.cs` (no ORDER BY) | `ArgumentException`: "Store 'X' was not found in the local index. Known stores: ..." (empty list on an all-PST profile) | ~~REPORTED (error), but misleading~~ -> **fixed** (it searches) **+ REPORTED** (`index.storeNotIndexed`, `scope.shape`) |
| A5 | The fact is machine-readable only in a different tool: `list_accounts` returns `inLocalIndex:false`, `locallySearchable:false`. `search` never reads or echoes it. **DOWNGRADED 2026-08-18** - `search` now echoes it both ways round: `sweep.storesWithoutIndex` names the stores an UNSCOPED search swept that the same probe reports as unindexed, and A4's fix adds `index.storeNotIndexed` on a store-SCOPED one, which was the case that could not report anything because it threw. Still not a per-store echo of the whole profile, which is what A3 is about. | `MailService.cs:3874-3893`, `ProbeStoreInIndex` `:4243-4277` | Correct field — in a tool the agent may never call | **REPORTED (wrong tool)** |

## 2. Freshness scope

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| E1 | An UNSCOPED search opens one sweep window from the PROFILE-wide frontier. A store whose own frontier lags by hours gets a window of minutes; its recent mail falls outside both tiers. **CLOSED 2026-08-18 by `79c1827`** - verified against the code and against T1 `SweepScopeAndWindowTests` on 2026-08-18, not re-fixed here. `MailService.ResolveSweepWindows` probes a frontier per catalog store and hands the sweep one window each (`SweepFoldersNewerThan`'s per-store map, matched case-insensitively, earlier spelling wins); a store-scoped search was already right and is untouched. The store the catalog does NOT know gets `EmptyIndexSweepWindow` rather than the profile frontier - the one store whose gap is unmeasured must not get the narrowest window on the machine - and the fact is reported through A1's machinery. `staleness.oldestStoreFrontierUtc` reports the worst store in scope, and `sweep.gapStartUtc` reports the widest window the sweep ACTUALLY opened rather than the planned one. Measured cost, maintainer-approved: 33-39 ms per unscoped search on a two-store catalog, against two frontiers 11 min 19 s apart - i.e. the single window was eleven minutes short for one store, every time, silently. | `MailService.cs:435-437` plus `StalenessScopeFor` `:607-609` (null when no store is named); window at `:880`; now `MailService.ResolveSweepWindows` + `OutlookComSession.NormalizeSweepWindows`/`WindowFor` | `staleness.newestIndexedUtc` = the profile value, `freshness:"live"`, `degraded` absent | ~~SILENT~~ (documented in `McpServer/README.md`, invisible in the payload) -> **fixed** (one window per store) **+ REPORTED** (`staleness.oldestStoreFrontierUtc`, `sweep.gapStartUtc`) |
| E2 | The default sweep set is four arrival-path folders only, and shallow (no subfolders). Mail a rule files elsewhere before indexing is in neither tier. **CLOSED 2026-08-18 as a FIELD, deliberately not as a coverage code.** The breadth is now `sweep.scopeShape` - `default_folders` / `folder` / `folder_only` - beside the sentence it has always carried, and both come out of one pure `MailService.ClassifySweepScope` + `DescribeSweepScope` pair, so the token and the prose cannot describe different breadths. **The question this row really asked was whether a field was needed at all**, and the answer is yes on one fact: `sweep.scope` was a SENTENCE, so the only way to branch on it was to compare against English, which is not a contract anyone can rely on - and this is the highest-probability hole left, because a rule filing mail into a subfolder is ordinary rather than exotic. **What was refused, and this is the substance of the decision: no coverage code and no `degraded`.** `ClassifyFreshness` derives `partial` from the code list, and `default_folders` is the shape of nearly every search anyone runs, so a code here would mark almost every answer this product gives as degraded and blunt the codes that fire rarely - the B2 arithmetic, applied one scope up. It is also not a hole in what the sweep was ASKED to do: it is the breadth of the tier, a fact to read rather than an alarm to raise, and the remedy (pass `folder`) is the caller's. The token is absent exactly when `sweep.scope` is - a refused or failed sweep planned no coverage to describe, which is the one way this differs from `attachmentTextCovered`, that one surviving every early return because it states a CAPABILITY. Reproduced in T1 `ScanDepthAndSweepScopeTests` (9 tests over this row; mutation-checked - dropping the field fails 3, a classifier that ignores the folder scope fails 3, changing the rendered sentence fails 2). | `Com/OutlookComSession.cs` `DefaultSweepFolderKinds`, and the shallow `SweepFolder` call in `SweepFoldersNewerThan`; now `MailService.ClassifySweepScope` / `DescribeSweepScope` | `sweep.scope:"default folders (Inbox, Sent Items, Deleted Items, Junk Email)"` — a human string, not a flag; `freshness:"live"`, `degraded` absent, no advice | ~~PROSE~~ -> **REPORTED** (`sweep.scopeShape`), with no gap code and no degradation, on purpose |
| E3 | A cached sweep (up to 10 s old) can miss an arrival that happened inside the TTL. | `Services/SweepCache.cs:31`, use at `MailService.cs:951-960` | `sweep.cached:true`, `sweep.cacheAgeSeconds` — but `freshness:"live"`, not degraded | **REPORTED (weak)** |

## 3. Tier asymmetries (index vs freshness sweep vs exhaustive)

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| B1 | `from` matches the ADDRESS only in the index tier. `System.Message.FromName` is SELECTed and never used in a predicate; the sweep and exhaustive tiers both match name OR address. The tool description promises "address or name fragment". **CLOSED 2026-08-18** — the predicate is now `(CONTAINS(FromAddress,…) OR CONTAINS(FromName,…))`; `IndexQuery.FromAddressContains` renamed `SenderContains` so the field cannot re-acquire the old meaning. Measured on this index: FromName alone 18 ms vs FromAddress alone 42 ms for the same fragment, the OR pair -19 to +12 ms on agent-sized TOP 26 statements; recall `vleuterweide` 0 → 18 rows, `subsidiewijzer` 0 → 9. 218 of 419 distinct senders (52%) in a 3 000-row sample carry a display-name token absent from their address. | `IndexSearch/WsSqlBuilder.cs:192-196` (only `CONTAINS(System.Message.FromAddress...)`), `:47` (FromName in SELECT); sweep `MailService.cs:1027-1031`; exhaustive `MailService.cs:1122-1126`; description `OutlookTools.cs:133` | A full-looking result set built from the sweep window alone; `freshness:"live"`, `degraded` absent | ~~SILENT~~ → **fixed** (all three tiers agree) |
| B3 | Item-class admission differs per tier and is never counted. Index: message rows need `System.Kind` to contain `email` (meeting requests index as `calendar`, so they are dropped). Sweep: **no class filter at all**. Exhaustive: `PR_MESSAGE_CLASS like 'IPM.Note%'` AND `Class==43`, dropping meeting requests/responses, NDRs and read receipts (`REPORT.IPM.Note.*`), `IPM.Post`, `IPM.Sharing`. **CLOSED 2026-08-18** - unified UPWARDS, to the widest of the three: the sweep, which never filtered. The rule is one place, `Mapi/MailItemAdmission.cs`, and it is that **an item's class never excludes it** - written as a method that cannot return false, so a future narrowing has to delete a call site and a test that both say what is being given up. An allowlist of "mail-ish" classes was considered and rejected on a fact that decides it: the SystemIndex carries no message-class column, so an allowlist could only ever be enforced in the COM tiers and would have replaced one asymmetry with another. Per tier: the exhaustive scan's DASL class clause and its `Class == 43` gate are gone (`Com/ExhaustiveDaslFilter.cs`, `Com/OutlookComSession.cs`); the sweep is unchanged and is the tier the others were unified to; the index tier admits message-level rows whatever their kind (`KindFilter` renamed with the rule - `MessagesAndAttachments` / `MessagesOnly` / `AttachmentsOnly`, plus `MailKindOnly` which no search uses). **What no tier can reach, now reported rather than implicit:** the COM tiers only enter folders whose `DefaultItemType` is `olMailItem`, which is unchanged and is not a class filter; the index tier has no folder-type column, so its widening also admits the calendar and contact items of folders the COM tiers never open - over-return, visible per hit. **The one way that over-return could have COST mail, closed 2026-08-18 in the same week:** those calendar and contact rows carry no `System.Message.DateReceived`, and the statement is `SELECT TOP n ... ORDER BY System.Message.DateReceived DESC`, so the provider's NULL collation - never measured here - decided whether they filled the `n` and pushed real mail behind the cut, silently and with a full-looking result list. `IndexSearch/IndexOrderGuard.cs` makes that impossible without knowing the collation: rows the ordering cannot rank are trimmed last rather than first, and where the statement was truncated with such a row in it the same statement is re-run with a predicate admitting only rows that carry the ordering column, then unioned. A dated meeting request can still push an older mail off the end, which is the decision rather than a defect. | `IndexSearch/IndexRowFilter.cs`, `WsSqlBuilder.cs`; `Com/OutlookComSession.cs`; `Com/ExhaustiveDaslFilter.cs`; rule in `Mapi/MailItemAdmission.cs` | Nothing. `IndexSearchResult.RowsDropped` exists (`IndexSearch/IndexSearchService.cs:47`) but is never copied into `SearchOutcome` | ~~SILENT~~ -> **fixed** (one rule) **+ REPORTED** (per-hit `itemClass`, `index.rowsDropped`, and an advice sentence when an answer holds non-mail) |
| B2 | Attachment text is index-only. The refused attachment-ONLY case is properly flagged; the DEFAULT case is not — a term inside an attachment of just-arrived mail is invisible while the answer says `freshness:"live"`. **CLOSED 2026-08-18.** `sweep.attachmentTextCovered: false` is present on every search whose terms could have matched inside an attachment (`FreshMerge.AttachmentTextMatchable`: terms present and `search_in` is not `subject`), and absent when the question does not arise. It is set before any early return, so it is equally true of a sweep that ran, one that was refused and one that was not needed. **Never `true`, and that is the design:** it states a fact about the TIER - the sweep reads `MailItem.Subject`/`Body` through COM and never opens an attachment - so there is no world in which it is true, and `=== false` is the branch. It depends on `search_in` and deliberately not on the attachment-hit flags, because all three of those shapes leave the sweep blind and only the attachment-ONLY one already said so. **It raises no coverage code and does not degrade the search, and that is arithmetic rather than taste:** `ClassifyFreshness` derives `partial` from the code list, and this condition holds for nearly every search anyone runs - a flag that fires always devalues the flags that fire rarely. The advice sentence is narrower (`MailService.DescribeAttachmentTextGap`, pure): the sweep must have RUN, and `sweep.itemsSeen` must be non-zero, because a freshness window with no mail in it cannot be hiding a mail whose match is in an attachment. That is the `foldersAbsent` reasoning, and it is emphatically not the G5 shape - it suppresses on provably nothing to report, not on the answer happening to look full. Reproduced in T1 `SearchCoverageClaimTests` (5 tests; mutation-checked - nulling the field fails 3). | `Services/FreshMerge.AttachmentTextMatchable`; `MailService.RunGapSweep` / `DescribeAttachmentTextGap` | attachment-only: `sweep.error`, `freshness:"index-only"`, `degraded:true` (**REPORTED**). Default: nothing | ~~PROSE (default case)~~ -> **REPORTED** (`sweep.attachmentTextCovered:false`, plus a sentence gated on the window holding anything) |
| B4 | `search_in:"body"` means body **plus attachment content** in the index tier, `MailItem.Body` in the sweep, `urn:schemas:httpmail:textdescription` in exhaustive. | `WsSqlBuilder.cs:448-462`; `FreshMerge.cs:300-330`; `ExhaustiveDaslFilter.cs:44` | Nothing in the payload | **PROSE** (README only) |
| B5 | Whole-word (index) vs substring (sweep, and exhaustive's LIKE fallback). | `FreshMerge.cs:300-330`; `ExhaustiveDaslFilter.cs:113-130` | exhaustive: `exhaustive.engine`, `exhaustive.instantSearchEnabled` plus advice (**REPORTED**). The sweep's over-match is the safe direction | **REPORTED / benign** |
| B6 | The ordering the caller asked for did not survive the MERGE. `IndexOrder.SizeDescending` reached the provider correctly, but the combined list (index hits + sweep hits, or the exhaustive walk) was then re-sorted by `ReceivedUtc` unconditionally - and the `top` trim happens AFTER that sort, so a size-ordered search returned the newest `top` rather than the largest, and the biggest mail could be trimmed off the end. The same line stood in both tiers. Recorded 2026-08-18, alongside the COM-host boundary work; not reachable from the tool surface, since `search` exposes no order parameter and only a T2 test sets the flag - so no answer any caller has had was affected. It is written down anyway because the shape is the one this document is about: a correct query undone by a later line, with nothing in the payload to say the order was not the order asked for. **CLOSED 2026-08-18** - one `MailService.SortForOrder` decides both call sites; unknown keys sort last in both orders (an unmeasured size is not a zero-byte mail) and size ties fall back to date, because `List.Sort` is not stable and two equally large mails should not swap between runs of the same query. Pinned by T1 `MergedResultOrderTests`. | `Services/MailService.cs:612` (`Search`) and `:2082` (`RunExhaustive`); order chosen at `:485`; flag at `MailModels.cs:92` | Nothing - a correctly sized, full-looking list in the wrong order | ~~SILENT~~ -> **fixed** (one comparator, both tiers) |

## 4. `thread`

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| C1 | `thread` runs NO freshness sweep and carries NO freshness fields. If the index has one or more rows for the conversation the COM walk never runs, so replies newer than the frontier are omitted. **CLOSED 2026-08-18** — the COM conversation walk is no longer a fallback: it runs on every call that has an anchor `id`, and its members are merged and deduped with the index rows (`FreshMerge.SelectFreshOnly`). `thread` now carries `degraded`, `freshness`, `staleness`, a `live` block and `advice`, on the same three-value contract `search` has. Sweeping was chosen over reporting-only because a conversation is a scope Outlook enumerates directly — one `GetConversation` plus one table walk, no window, no folder set, no per-folder cap — bounded by `ThreadWalkBudgetMs` (= `SweepBudgetMs`, 30 s), plus one ~2 s `HitLocator` probe the first time a given hit id is used (cached after). | `MailService.cs:1495-1511`; model `Services/MailModels.cs:620-639` | `source:"index"`, `truncated:false`, and **no** `degraded`/`freshness`/`staleness` at all. Tool description: "Fetch the full conversation of a mail" (`OutlookTools.cs:175-180`) | ~~SILENT~~ → **REPORTED** (`degraded`, `freshness`, `live.coverageGaps`) |
| C2 | `thread`'s index path is `KindFilter.EmailOnly`, so a meeting-request member of the thread is dropped. **CLOSED 2026-08-18** — new `KindFilter.MessagesAnyClass`: message-level rows of every item class, no attachment rows. `search` keeps `EmailOnly` (there the caller asked for mail); a thread member Outlook puts in the conversation belongs in the conversation. **Half of that superseded by B3 (2026-08-18):** `search` no longer keeps the narrow shape - no tier filters by item class any more - and the enum members were renamed with the rule, so `MessagesAnyClass` is now `MessagesOnly` and `EmailOnly` is `MailKindOnly`, which only store-scope discovery uses. What C2 fixed still stands; only the names and the contrast with `search` have moved. | `MailService.cs:1489` | Nothing | ~~SILENT~~ → **fixed** (members no longer dropped) |
| C3 | An unresolvable `store` silently widens the conversation lookup to the whole profile. **CLOSED 2026-08-18** — the widening stays (over-return is the safe direction) and is now reported as `scopeWidened:true` plus an advice sentence naming the store. | `MailService.cs:1473-1483` (`catch (ArgumentException) { scope = null; }`) | Nothing (over-return: the safe direction) | ~~SILENT~~ → **REPORTED** (`scopeWidened`) |
| C4 | The COM fallback walks one item's `Conversation.GetTable()` — same-store members only. **DOWNGRADED 2026-08-18** — still same-store, but no longer silent: when the index rows for the conversation name a store the walk did not cover, `live.coverageGaps` raises `unwalked_store`, `freshness` is `"partial"` and advice says to pass an `id` from that account. Undetectable when the OTHER store has no index rows either, so this row stays open. **CLOSED 2026-08-18.** That residue was the whole of it: `unwalked_store` is raised by comparing the conversation's INDEX ROWS against the walked store, so it can only ever fire for a store the index knows - and pointed at the unindexed-PST profile the A-group work is about it says nothing at all, with half a conversation missing under `freshness:"live"`. The fix asks OUTLOOK which stores exist instead of asking the index about itself, reusing the machinery `2d28957`/`3bd512f` built rather than inventing a parallel rule: `TryGetProfileStoreNames` for the list, the pure `StoresMissingFromIndex` for the verdict (only a probe's NO counts; a store that could not be settled is reported neither way). Stores the index holds nothing for that the walk did not cover reach the payload as `live.storesWithoutIndex` - capped at the same `UnindexedStoreListCap`, with the same truncated/total pair, through one shared `CapUnindexedStoreList` - and raise `unindexed_store` with `freshness:"partial"` and `degraded:true`. **A SECOND code rather than a widening of the first**, because the claims differ: `unwalked_store` says the conversation demonstrably has members elsewhere, this one says the question could not be asked, and the advice sentence says so in as many words so an agent cannot relay it as "we checked and there was nothing". **The anchor's own store is excluded**, and that exclusion is the rule (`FreshMerge.UnwalkedUnindexedStores`, pure): Outlook enumerated the conversation there member by member, so an unindexed store that happens to be the walked one is not a hole - without it the code would fire on every conversation on a single-PST profile, which is the cry-wolf failure that makes a completeness flag worthless. Nothing is claimed when the walk did not run, returned no members, or could not name its store, on the same reasoning `unwalked_store` already uses. COST: one store-list read and one probe per store, both from the five-minute caches `search` already fills, under `StoreIndexProbeBudgetMs`, only on a walk that ran and returned members. Reproduced in T1 `ThreadUnindexedStoreTests` (16 tests; mutation-checked - dropping the anchor exclusion fails 3, removing the code fails 3, removing the profile pass fails 2). | `Com/OutlookComSession.cs` `TryGetConversationItems`; now `MailService.NoteThreadStoresWithoutIndex` + `FreshMerge.UnwalkedUnindexedStores` / `ThreadGapUnindexedStore` | `source:"com"` only | ~~SILENT~~ → REPORTED when the index can see the other store; SILENT when it cannot → **REPORTED** either way (`live.storesWithoutIndex`, `unindexed_store`, `degraded`) |
| C5 | **NEW, and OPEN.** A `store` on `thread` scopes the INDEX query, so it also narrows the evidence `unwalked_store` is computed from - and the store is auto-derived from the referenced hit when the caller passes `id` without `conversation_id` (`MailService.Thread`, the `_hits.TryGetValue` branch). On those call shapes the index rows can only ever name the one store, so `unwalked_store` cannot fire at all and a member in a second INDEXED account is both absent and unreported. Found while closing C4 and recorded rather than fixed quietly, the way B6 was. C4's fix covers the half where the other store is UNINDEXED (that pass reads Outlook's store list, not the index rows); this is the half where it is indexed and simply scoped out. Note the asymmetry it creates with C3, which reports the OPPOSITE event (`scopeWidened`) on the same parameter. Candidate fixes, none free: report `scope.applied` on the thread payload so the narrowing is at least visible; run the conversation query unscoped and use `store` only to order the results (`thread`'s `store` is documented as a speed hint, not a filter - C3's own reasoning - which makes this the coherent one); or leave it and widen `unwalked_store`'s evidence to a profile-wide `ConversationID` probe. | `MailService.cs` `Thread` (scope resolution + `IndexQuery.Scope`), evidence at `FreshMerge.DescribeThreadCoverageGaps` | nothing - the narrowing is not echoed anywhere, and the code that would have caught the consequence is silenced by it | **SILENT** |

## 5. `exhaustive:true`

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| F1 | `degraded` and `freshness` are never set on an exhaustive result, so a scan that timed out and skipped folders looks undegraded on the two fields the tool description teaches agents to branch on. **CLOSED 2026-08-18** — `FreshMerge.ClassifyExhaustiveFreshness` sets both from the scan's own counters. The scan IS the live check, so `"index-only"` is unreachable there by construction (it names the state the mode exists to avoid) and no fourth value was invented; `"live"` when it covered its scope, `"partial"` when it timed out, skipped folders **or** truncated. Truncation counts here and does not in the merged path: an indexed `truncated` caps a date-sorted complete match set and pages, while the exhaustive cap stops the folder walk in arbitrary order (F2) and cannot be paged past. | `MailService.cs:1198-1220` (the exhaustive `return new SearchOutcome` sets neither; contrast the indexed path at `:562-580`) | `exhaustive.timedOut/foldersSkipped/truncated` present (**REPORTED**), `degraded` and `freshness` **absent** | ~~PROSE / inconsistent contract~~ → **REPORTED** (same two flags as a merged search) |
| F2 | The scan never sorts the folder table, so the `maxItems` cap truncates in arbitrary order and there is no way to page past it (top is capped at 100). Contrast the sweep, which sorts newest-first. **CLOSED 2026-08-19 - made RESUMABLE, which is the maintainer's decision (c), and (a) came with it as its prerequisite.** The walk now enumerates its scope in a stated order first - the SAME sibling comparator `list_folders` uses, shared rather than copied (`OutlookComSession.CompareSiblings`; the store-ordering leg was a third copy and now calls it too) - so a stop is reproducible and `foldersTotal` is honest even when the walk covered four of thirty-two. A scan that stops early returns `exhaustive.nextToken`, a `scan-<32 hex>` handle over walk state held in the SERVER PARENT (`ExhaustiveScanCursors`, TTL 30 min, 4 concurrent chains): never in the COM child, because the event this exists for - a scan past its deadline - ends with the supervisor killing that child. Per folder the ladder is date cursor (the table sorted: resumption is a narrower DASL restriction, `datereceived <= cursor` plus a tie set, so nothing depends on row order), then validated ordinal (`GetArray(k)` plus a watermark EntryID, since MAPI documents no row order for an unsorted table - a mismatch REPORTS rather than resumes), then folder restart with per-folder EntryID suppression. `position.resumeTier` says which rung paid. Tree motion between pages is DETECTED rather than assumed: the completed-folder set (EntryID, which survives rename and within-store move) is reconciled against a fresh enumeration each page, so a folder that appeared before the cursor is scanned and one that vanished is named. **What did NOT change, deliberately:** `top` stays at 100 and counts PER PAGE (`itemsReturnedTotal` is the chain total), `result_cap` and `time_budget` keep firing on a resumable stop, and `degraded`/`freshness: "partial"` stay set - the token is a remedy, not a repair, and a page reached by one raises `scan_resumed` so the LAST page of a chain cannot report itself as complete. Reproduced in T1 `ResumableScanTests` (23 tests) plus `ComHostProtocolTests` and `BudgetCompositionTests`; T2 `LiveResumableScanTests` holds the acceptance T1 cannot reach - a paged scan's EntryID set must EQUAL an unpaged one's, with no duplicates. | `Com/OutlookComSession.cs` `ExhaustiveScan` / `EnumerateScanFolders` / `ScanEnumeratedFolders` / `ScanSingleFolder`; `Services/ExhaustiveScanCursors.cs`; `MailService.RunExhaustive` | `exhaustive.stopReason`, `exhaustive.nextToken`, `exhaustive.position`, `exhaustive.itemsReturnedTotal`, and the `scan_resumed` / `tree_changed` / `resumed_unsorted` / `resume_position_lost` / `dedup_capacity` codes | ~~REPORTED but unactionable~~ -> **REPORTED and ACTIONABLE** |
| F3 | `from` / `unread_only` / `has_attachments` are applied AFTER the scan cap, so an exhaustive search can return 2 rows with `truncated:true` while thousands match. **CLOSED 2026-08-18** - reported, not re-plumbed. `exhaustive.postCapFilters` names the filters that ran after the cap in request order (`FreshMerge.PostCapFilters`, pure), `exhaustive.itemsFilteredOut` counts what they discarded, and the pairing with `truncated` raises `post_cap_filter` with its own sentence: the cap counted CANDIDATES rather than results, N of them were thrown away afterwards, and the remedy is a tighter folder or `after` bound rather than a filter that runs after the cap. **Raised only alongside `result_cap`**, which is what makes it free of false alarms and free of new degradation: with no cap the filter saw the whole matched set and removed exactly what was asked, so this code explains a hole the scan had already declared rather than declaring one. `before`/`after` are deliberately absent from the list - they go into the DASL filter and bound the scan itself. **What was NOT done, and why:** the cap was not raised to over-fetch past `top`. `maxItems` is what stops the folder walk, so over-fetching means opening more of the tree under a 105 s budget, and the reachable ceiling is still 100 - it would trade a bounded honest answer for an unbounded one that is only sometimes better. The remedy the sentence gives - narrow the scan's own bounds - is the one that actually works, and F2 is why "raise top" cannot be it. Reproduced in T1 `SearchCoverageClaimTests` (4 tests; mutation-checked - removing the code branch fails 2, one of them the "no code may ship without prose" guard). | `MailService.RunExhaustive`; `FreshMerge.ScanGapPostCapFilter` / `PostCapFilters` | `truncated:true`; the advice does not say the filter ran post-cap | ~~PROSE~~ -> **REPORTED** (`exhaustive.postCapFilters`, `exhaustive.itemsFilteredOut`, `post_cap_filter`) |
| F5 | Rows dropped inside the scan are never counted: `GetItemFromID` failure, `Class` read failure, non-`IPM.Note` class. **CLOSED 2026-08-18** - counted, and split by whether the drop was a FAILURE or a FILTER, because reporting them as one number would cry wolf on the mode working as designed. `exhaustive.rowsDropped` is every row not admitted, deliberately the same name and meaning as the index tier's `IndexSearchResult.RowsDropped` (one counter shape, not two); `exhaustive.rowsUnreadable` is the failure subset and is the one that raises `rows_unreadable`, `freshness:"partial"` and `degraded:true`. Their DIFFERENCE was exactly the item-class filter B3 is about, and measuring it was the point - **B3 then removed that filter on 2026-08-18, so on a real scan the difference is now zero by construction**, which is the machine-checkable statement that this tier admits every class. The two counters stay separate anyway, so a future non-failure drop cannot silently start degrading every scan that meets one. The exhaustive tier also gained `exhaustive.coverageGaps` in this work, on the same contract the sweep and the thread walk already carry - `time_budget` and `folders_skipped` reuse the sweep's tokens verbatim, `result_cap` gets its own because that cap loses whole unopened folders rather than truncating one folder's window - with `FreshMerge.DescribeExhaustiveCoverageGaps` as the classifier, `MailService.DescribeExhaustiveCoverage` as the single source of the prose (the three inline sentences moved there unchanged), and `ClassifyExhaustiveFreshness` recomputed FROM the codes so a hole cannot appear in one rendering and not the other. | `Com/OutlookComSession.cs` `ScanFolderTree` | Nothing | ~~SILENT~~ -> **REPORTED** (`exhaustive.rowsDropped`/`rowsUnreadable` + `coverageGaps`) |
| F4 | `ScanFolderTree` has no depth guard (unlike `SweepFolderTree` and `CollectFolders`) — a cyclic tree is an uncatchable StackOverflow. **CLOSED 2026-08-18, and it was two fixes rather than one.** The crash half is the guard the other two walks already make - a `depth` parameter and `if (depth > FolderWalkDepthGuard) return;` - and it matters more than "robustness" suggests, because a `StackOverflowException` cannot be caught: it ends the COM HOST process, the parent reports it to the caller as Outlook having gone away, and two of those open the breaker for 30 s. The completeness half is why it is ten lines and not one: adding the guard alone would have replaced a crash with a SILENT truncation, in the one mode a caller picks BECAUSE completeness matters. So the stop latches on `ExhaustiveScanState.DepthLimitReached`, rides out on `ComExhaustiveResult` (pinned across the process boundary in `ComHostProtocolTests` - the flag is raised in the child), and reaches the payload as `exhaustive.depthLimitReached` plus `depth_limit` in `exhaustive.coverageGaps`, with `freshness:"partial"` and `degraded:true`. **The token is the sweep walk's own** (`ScanGapDepthLimit = GapDepthLimit`), because it is literally the same guard at the same number and a third spelling would make an agent learn a vocabulary per tier. It bounds only the offending subtree - siblings are still scanned - since a walk that abandoned the remaining accounts over one bad folder would lose far more than it saved, and the advice sentence quotes `FolderWalkDepthGuard` rather than restating 64 and points at `list_folders` to find the damaged subtree. Reproduced in T1 `ScanDepthAndSweepScopeTests` (6 tests over this row plus one wire round-trip; mutation-checked - removing the code fails 3, breaking the COM-to-payload mapping fails 1, dropping the flag on the process hop fails 2). **What T1 cannot reach: the guard itself**, which needs a folder tree deeper than 64 levels; this tier pins that once the walk says it stopped, every rendering says so. | `Com/OutlookComSession.cs` `ScanFolderTree`; now `ScanFolderTree(..., int depth)` + `ExhaustiveScanState.DepthLimitReached` + `FreshMerge.ScanGapDepthLimit` | n/a — robustness, not completeness | ~~tangential~~ -> **REPORTED** (`exhaustive.depthLimitReached` + `depth_limit`), and the crash is gone |

## 6. Scope resolution that yields zero instead of an error

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| G1 | `list_folders(store:"typo")` returns an empty tree with no error. **CLOSED 2026-08-18** - a store scope that matched no folder is now resolved against the COM store list and refused by name: `ArgumentException` "Store 'typo' was not found in Outlook. Known stores: ... Use list_accounts for the full store list.", the same shape the index-tier resolver already threw for `search`. The list is consulted ONLY on the empty result, so an ordinary call pays nothing, and a store that IS mounted and simply has no folders below its root still answers an empty tree with no error - inventing a failure for it would be the same lie in the other direction. Classified by the pure `MailService.DescribeUnresolvedFolderStore`, pinned in T1 (unknown store / known-but-empty / case-insensitive / a profile reporting no stores at all). | `Com/OutlookComSession.cs` `ListFolders`; `MailService.cs:3995-4004` | `stores: []`, `folderTotal: 0`, `truncated:false` | ~~SILENT~~ -> **REPORTED (error)** |
| G2 | A store whose `DisplayName` COM read fails is silently dropped from `list_folders`; in the sweep its four default folders count as skipped in the total but land in nobody's per-store bucket. **CLOSED 2026-08-18.** The question this row really asks is what such a store can be CALLED, since every scope, bucket, label and refusal in this server is keyed by a display name - and the answer decided here is a LABEL rather than a guess: `Com/StoreNaming.cs` derives `(unnamed store N)` from the store's 1-based position in `Namespace.Stores`, the only thing about it that can still be read. A guess would have been worse than the drop it replaces - the root folder's name or the PST path would LOOK like a name, could collide with a real store's sweep bucket, and still could not round-trip, because a store scope is matched against the `DisplayName` that failed. So the label is bracketed, it is flagged wherever it appears (`nameUnreadable: true`), and passing it back as `store` is refused by what it IS (`DescribeUnresolvedFolderStore`) rather than as a typo. **What stopped being lost:** `list_folders` lists the store's whole tree instead of omitting it; the three COM reads that used to share one `try` are separate, so an unreadable StoreID no longer drops a store that named itself (the id falls back to empty, which the one-argument `GetItemFromID` overload already handles); the sweep SWEEPS the store under its label and gives it a per-store bucket, so its fresh mail reaches unscoped searches for the first time and those four folders stop being unattributable skips. A store-SCOPED walk or sweep still leaves it out - it can be neither matched against the requested name nor ruled out of it - and says so (`storesUnnamedExcluded`, and the sweep counter deliberately stays null on a scoped read so a cached all-stores sweep cannot leak another store's naming failure). **Upstream half:** G1's refusal used to assert "Store 'X' was not found in Outlook" from an enumeration that could not see such a store; it now names the count that could not be ruled out. No coverage code and no `degraded`: the store is swept, so nothing is missing from the answer - what is lost is the ability to ASK about it again, which is a fact about the next call. | `OutlookComSession.cs` `GetStoreDetails` / `ListFolders` / `SweepFoldersNewerThan`; `Com/StoreNaming.cs`; `MailService.PageFolders` / `DescribeUnresolvedFolderStore` / `DescribeUnnamedStores` / `ApplySweepCounters` | list_folders: nothing. Sweep: unscoped searches see `foldersSkipped` (**REPORTED**); store-scoped ones cannot | ~~SILENT / partial~~ -> **fixed** (it is listed, and it is swept) **+ REPORTED** (`stores[].nameUnreadable`, `storesUnnamed`, `storesUnnamedExcluded`, `sweep.storesUnnamed`, plus advice on both tools) |
| G3 | `CollectFolders` / `CollectFolderPaths` truncate at `FolderWalkAbsoluteCap = 10 000` and depth 64 with no flag; `FoldersOutcome.truncated` is computed against the already-truncated list. **CLOSED 2026-08-18.** Both walks now carry a `FolderWalkBounds` down the recursion and hand it back on `ComFolderTree` / `ComFolderPathList` - a mutable carrier rather than a return value because the cap can fire at any depth, and threading a bool up through every frame is how the flag went missing to begin with. `truncated` means what it says: not the whole tree, from EITHER cause. `nextOffset` is present only for the pageable cause, because paging cannot get past a walk cap - the next call re-walks and stops in the same place - so an offset there would be an instruction that cannot work; the remedy (`store`) is in `advice` instead. `walkCapReached` / `depthLimitReached` tell the two apart, `folderTotal` is documented as a lower bound when either is set, and the store loop sets `walkCapReached` when the cap falls BETWEEN stores, which is the shape that loses whole trees rather than trailing folders. `FolderWalkDepthGuard` became public and is pinned in T1 beside `FolderWalkAbsoluteCap`, because the advice sentence quotes it and a cap named in prose next to a constant nobody compares it with is how the two drift. | `OutlookComSession.cs` `CollectFolders` / `CollectFolderPaths`; cap at `MailService.cs:220` | `truncated:false` on a truncated tree | ~~SILENT~~ (low probability) -> **REPORTED** (`truncated`, `walkCapReached`, `depthLimitReached`, `advice`) |
| G4 | The delegate folder-NAME list comes from the same truncatable walk; a short list under-returns the delegate folder scope. **CLOSED 2026-08-18** on G3's back: the walk's bounds ride out with the paths (`ComFolderPathList.Incomplete`), are cached WITH them so the next caller inside the TTL hears the same thing, and reach the payload as `scope.folderNamesTruncated`. It sets `degraded: true` - which has always meant "this answer covers less than it was asked to" - while `freshness` stays what the tiers measured, because this is a scope hole rather than a lag and folding it into `partial` would give that word two holes with two remedies. The advice sentence names both remedies (a single folder with `include_subfolders:false`, or `exhaustive:true`). **The predicates are untouched**, deliberately: narrowing further would drop mail and widening would answer with the whole delegate mailbox under a folder scope the caller chose, so only the report differs - which is what makes the fix free of behaviour risk. This one matters more than its probability suggests, because the delegate path is the weakest in the product: the delegate index namespace is FLAT, so a delegate folder scope can only be an OR of folder NAMES, and a name the walk never reached is not a missing row in a listing but a folder no tier looks in. | `MailService.cs:4373` into `OutlookComSession.ListFolderPaths:7451` | `scope.folderNamesMatched` (a count, not a completeness flag) | ~~SILENT~~ (low probability) -> **REPORTED** (`scope.folderNamesTruncated` + `degraded`) |
| G5 | A folder path that resolves in COM but not in the index (localized/renamed folder, non-recursive `ItemFolderPathDisplay` mismatch) yields zero index rows. The C7 guard catches it — but only when the MERGED answer is completely empty, and only as advice. **CLOSED 2026-08-18**, and the answer to "is that condition defensible at all" was no. The guard's question is whether the INDEX can address the folder; the merged answer includes swept items, which come from COM, where the folder resolved perfectly well - so one just-arrived mail silenced a guard whose subject it says nothing about. The gate is now the index tier's own row count, which is exactly the question the two TOP-1 probes go on to ask, and the state that used to be hidden is the ordinary one for a renamed folder: the sweep answers for the last few minutes, the index answers for nothing, and the whole thing reads as a thin result set. The payload half is `index.folderNotIndexed: true`, the folder-level sibling of `index.storeNotIndexed`, set from the same branch that emits the sentence so the two are one decision. A third case is now separated out: with no indexed rows for the STORE either the field stays null, because that state has its own reporting (`storeNotIndexed`, `storesWithoutIndex`, `no_index_frontier`) and blaming the path for the store's gap would be a guess. **`degraded` deliberately stays absent**, on the G6 precedent: the probes cannot tell "the path does not resolve" from "the folder was created this morning and everything in it is still only in the sweep window", and degrading the second is a false alarm on every new folder - the cry-wolf trap `foldersAbsent` exists to avoid, one scope down. COST: two TOP-1 statements, on folder-scoped searches whose index tier returned nothing. That is more often than "the whole answer was empty" and still only where the index did not answer. Reproduced in T1 `SearchCoverageClaimTests` (4 tests; mutation-checked - restoring the merged-count gate fails the one that matters). | `MailService.DescribeFolderMissingFromIndex` (was `AddUnresolvedFolderAdvice`), `Services/FolderScopeResolver.DescribeUnresolvedFolder` | advice only; one swept item suppresses the guard entirely | ~~PROSE, conditional~~ -> **REPORTED** (`index.folderNotIndexed`, judged on the index tier's own row count) |
| G6 | Index-tier candidate exhaustion (the post-filter ran out of over-fetched rows) is advice-only; `IndexSearchResult.CandidatesExhausted` is never copied into `SearchOutcome`. **CLOSED 2026-08-18** - it rides into the payload on the `index` block B3's work added, as `index.candidatesExhausted` (present only when true), beside `index.rowsScanned`/`index.rowsDropped`. `degraded` deliberately stays absent: exhaustion means the list MAY be short of matches, which is the same class of fact as `truncated`, not a freshness hole - and widening `degraded` to cover it would blunt the one flag the tool description tells agents to relay. The advice sentence is unchanged and still names the remedy. | `MailService.cs` (index block on the returned `SearchOutcome`) | an advice sentence; no field, and `degraded` stays absent | ~~PROSE~~ -> **REPORTED** (`index.candidatesExhausted`) |

## 7. Row- and item-level silent drops in the freshness sweep

| # | Gap | Code path | What the caller sees | Severity |
|---|---|---|---|---|
| H1 | A table row whose EntryID column is missing, or whose `GetItemFromID` fails, is skipped and does not count toward the per-folder cap. A folder where every row fails returns `SweepOutcome.Complete` with zero items. **CLOSED 2026-08-18** - such rows are counted (`SweepFolder`'s new `out int rowsUnreadable`), attributed to the store they were lost in (`ComStoreSweepCounters.RowsUnreadable`, so a cached all-stores sweep cannot degrade a different account's search) and reported as `sweep.rowsUnreadable` plus the `rows_unreadable` coverage code with its own advice sentence, making the search `freshness:"partial"`, `degraded:true`. The folder is still counted as swept, which is true - it was enumerated; the hole is one level in, at the row, which is exactly why no folder counter could express it. | `Com/OutlookComSession.cs` `SweepFolder` | `foldersSwept` counts that folder as fully covered | ~~SILENT~~ -> **REPORTED** (`sweep.rowsUnreadable` + `rows_unreadable`) |
| H2 | `t.Sort(...)` failure is swallowed; if the item cap then fires, the `item_cap` advice claims "reads newest-first, so the OLDEST ... is not covered", which is then false. **REOPENED IN PART 2026-08-19 - the reporting is right and its PREMISE is now in doubt.** Microsoft's `Table.Sort` reference says a sort property may be referenced "by their explicit string names only; cannot reference properties by their namespaces", and `SweepFolder` passes `urn:schemas:httpmail:datereceived`, which is a namespace. If that holds for this call then the sort has never applied on ANY store for ANY user, the cap has always cut arbitrarily, and the tier whose purpose is recent mail has been returning an arbitrary 200 rather than the newest 200 - which would make this session's reading of `item_cap_unsorted` ("the sort genuinely does not apply on that store") wrong in the direction that matters. Two things were done about it and a third was deliberately not: the shipped `catch` that wrapped `Columns.Add` and `Sort` TOGETHER is split, so `sortApplied: false` can now say which failed, and the folders where the column WAS added and `Sort` still threw are counted into `sweep.sortRefusedFolders` - equal to the folders swept on every store means the property name is the cause, varying by store means it is not. The sort call itself was NOT changed, because changing it before the question is settled would destroy the evidence. The probe is T2 `LiveTableSortProbeTests`, read-only, one table per store, both spellings, printing a per-store verdict. **CLOSED 2026-08-18** - the same species as A2, and closed the same way: the fact is measured where it happens and both renderings are computed from it, so the payload and the prose cannot disagree. `SweepFolder` returns `sortApplied` (a `Sort` that does not throw is the only evidence COM offers that a table is ordered), the folders where the cap fired over an UNSORTED table ride up as `ComSweepResult.ItemCappedFoldersUnsorted` and reach the payload as `sweep.itemCappedFoldersUnsorted`, and `FreshMerge.SortedItemCappedFolders` is the ONE split that both the gap codes and the advice sentences read. `item_cap` is now raised only for the folders whose table really was sorted and its sentence names only those; the rest raise `item_cap_unsorted`, whose own sentence says the cap kept an ARBITRARY 200 of the window, that which mail is missing is unknown, and not to assume it is the oldest. **Why its own code rather than a qualifier:** the two lead somewhere different. `item_cap` tells an agent which mail to expect to be absent, so `after` can page past it and a human can be told what to look for; `item_cap_unsorted` withdraws exactly that, leaving "shrink the window until the folder fits under the cap, or read it with `exhaustive:true`". A single code carrying both would have to say the weaker thing always, which would give up coverage the sorted case really does deliver. The unsorted list is a SUBSET rather than a whole-sweep flag because one folder can refuse the sort while its siblings sort fine. Reproduced in T1 `SearchCoverageClaimTests` (7 tests over this row; mutation-checked - reverting the classifier split fails 5, reverting the sentence's folder list fails 1). **What T1 cannot reach: the COM half.** `Table.Sort` throwing has never been observed on any machine here - it was found by reading the `catch` - so the `sortApplied` read itself is pinned only by the process-boundary round trip (`ComHostProtocolTests`). See TODO. | `OutlookComSession.SweepFolder` (`out bool sortApplied`) + `SweepTally.ItemCappedUnsorted`; `FreshMerge.GapItemCapUnsorted` / `SortedItemCappedFolders`; advice `MailService.DescribeSweepCoverage` | `item_cap` gap code (**REPORTED**) with a wrong explanation | ~~REPORTED, prose wrong~~ -> **REPORTED** (`sweep.itemCappedFoldersUnsorted` + `item_cap_unsorted`, each sentence true of every folder it names) |
| I1 | `unread_only`, `has_attachments`, `after`, `before` all DROP a swept item when the underlying COM property could not be read (`IsRead`/`HasAttachments`/`ReceivedTime` null). **CLOSED 2026-08-18** - the DROP STANDS and is now reported. Dropping is the deliberate half of the decision: a filter the caller asked for has to be honoured, and admitting an item that cannot be shown to match would corrupt the answer in the opposite direction (an agent asking for unread mail relaying read mail as unread). The reporting is the other half: `sweep.itemsFilterUnreadable` counts them, `sweep.filtersUnevaluated` names the request parameters that could not be evaluated - so the remedy is literally "re-run without this one" - and `filter_unreadable` makes the search `partial`/`degraded` with its own advice sentence. The same drop in the exhaustive post-filter is instrumented identically (`exhaustive.itemsFilterUnreadable`, same token); only `unread_only`/`has_attachments` are reachable there, since that mode's date bounds go into the DASL filter. NOT SETTLED HERE: a null `ReceivedTime` is ambiguous - `TryGetDateTime` maps Outlook's 4501-01-01 "no value" to null exactly as it maps a failed read - so the advice says "no usable value", never "the read failed". Reporting it is what will settle it, since the code names the filter and counts the items every time it fires. | `MailService.cs:1033-1055` | Nothing | ~~SILENT~~ -> **REPORTED** (`sweep.itemsFilterUnreadable` + `filtersUnevaluated` + `filter_unreadable`) |
| H3 | An item the folder table carries NO usable delivery time for is selected by no sweep window at all. The sweep restricts with `("urn:schemas:httpmail:datereceived" >= X) OR ("urn:schemas:httpmail:date" >= X)` (the DASL restriction `SweepFolder` builds), so an item exposing neither property matches no window - not a narrow one, not a 7-day one, not a 60-day one. Such an item is **absent** from the freshness tier rather than mis-dated, and it is absent no matter how wide the window is opened. The folder is still counted in `sweep.foldersSwept`, the search still reports `freshness: "live"`, and no coverage code, counter or advice sentence exists for the case. **Real-world shape:** mail that reaches a PST without transport - imported from another client, copied or dragged between stores, restored from a backup, or recovered by a repair tool - plus any item still in an UNSENT state, which is what `Items.Add` + `Save` produces. A user who imports an archive and searches it gets index coverage where the index reaches and nothing at all from the sweep, with the payload asserting the sweep ran. **Evidence, and its limits.** The filter is code, read directly, and is not in doubt. The supporting observation is: a 40 000 item corpus was built into a Hyper-V PST on 2026-08-19 and a sweep over it selected **6 items in 234-367 ms**. That observation is CONFOUNDED and must not be quoted as proof of this row - those items were unsent, so Outlook had filed all 40 000 into Drafts, which the sweep does not cover, and the missing folder alone explains the result. The corpus tool's own date probe hit the same confound: it reported `readBack` correct with `daslIn=False`, which reads as "the date does not drive selection" but is equally explained by "the item is in another folder". **What would settle it:** the corpus tool now settles placement before dates (`RemediationTools/CorpusPlacement.cs`), so a re-run with items provably in the Inbox and provably in that folder's table isolates the date question. If `daslIn` is still false with placement verified, this row is confirmed against a real store. | `Com/OutlookComSession.cs` `SweepFolder` (its DASL restriction); merge and reporting at `Services/MailService.cs` (`sweep.*`) | `sweep.foldersSwept` counts the folder, `freshness: "live"`, `degraded` unset, no coverage code, no counter - nothing distinguishes "the folder held nothing recent" from "the folder held mail this filter cannot see" | **SILENT** |

## 7b. Claims of non-effect - the same species, on the WRITE surface

**Why this section is in a search-completeness map.** The defect this file was built for is an
answer that quietly asserts more coverage than it has. A failure message that says "Nothing was
changed" when the code cannot know that is the same defect with the sign flipped: an assertion
about the mailbox that nothing checked, in a payload that reads as authoritative. Enumerated
2026-08-19 across all 31 such claims in the product; 16 were wrong or misleading, and the
severity vocabulary transfers unchanged - PROSE means only a sentence said it, REPORTED means a
field an agent can branch on.

| # | Gap | Code path | What the caller saw | Severity |
|---|---|---|---|---|
| J1 | The tool layer attached ONE advice string - "Nothing was changed or deleted." - to every draft refusal reason, `com_failure` included. So an interrupted `update_draft` returned a single payload whose `message` said WHETHER IT WAS APPLIED IS UNKNOWN and whose `advice` said nothing had changed, undoing `db34923` one layer up on the way to the wire. The T1 test that pinned that fix asserted on `refusal.Message` only. **CLOSED 2026-08-20** - `OutlookTools.DraftRefusalAdvice` branches on the reason, `outcome` carries the same answer as a field, and T1 `AtomicityClaimsTests` asserts on the advice as well as the message. | `OutlookTools.GuardAsync`, the `catch (DraftRefusedException)` arm | contradictory advice beside a correct message | ~~PROSE, wrong~~ -> **REPORTED** (`outcome`) |
| J2 | `MoveItemView.Ok` is a boolean over a three-valued outcome - moved, not moved, unknown - and `Error` was documented as "nothing was moved for this item". The product contradicted that in its own code twice: the audit-failure message says "The item WAS moved ..." and the per-item timeout message says the outcome is UNKNOWN, and both travelled through that field. **CLOSED 2026-08-20** - `MoveItemView.Outcome` carries `unchanged` / `applied` / `unknown`, set from `MailService.MoveFailureOutcome` for a refusal, `applied` for a move whose audit line could not be written, `unknown` for a per-item timeout. No wording could have fixed this one. | `MailModels.MoveItemView`, `MailService.CompleteMove` / `MoveOne` / `ArchiveOne` | `Ok:false` for an item that had moved | ~~PROSE, wrong~~ -> **REPORTED** (`items[].outcome`) |
| J3 | A failed `new_draft` could leave a draft in the mailbox that `discard_draft` was structurally unable to reach. `Save()` commits the item and four COM steps follow it inside the same `try`; a failure in any of them returned null, so the caller never learned the EntryID, `_draftRegistry.Register` was never reached, and the registry gate is exactly what `discard_draft` enforces. **CLOSED 2026-08-20, behaviourally** - the COM layer reports `savedDraftEntryId` on the failure path (re-read after the relocate, since a Move mints a new id), `MailService` registers it BEFORE judging the call, and the message names it. `TryCreateDerivedDraft` has the same shape and the same fix. | `OutlookComSession.TryCreateNewDraft` / `TryCreateDerivedDraft`; `MailService.NewDraft` / `CreateDerived` | "The draft could not be created", with the draft in Drafts | ~~SILENT~~ -> **REPORTED** (the id, in the message; `outcome: unknown`) |
| J4 | `move_mail` with `create_folder: true` creates folders BEFORE the target guard runs, so a move into `Deleted Items/foo` created `foo` inside Deleted Items and then answered "Refused: moving to Deleted Items ... is deletion semantics" with `createdFolders` absent from the outcome - the paths died with a local when the method returned null. A multi-segment path whose second `Add` throws has the same shape. **CLOSED 2026-08-20** - `TryMoveItemToPath` reports `createdFolderPaths` on every path, `MoveOne` merges them into `createdFolders` whether the item moved or not, and `DescribeMoveFailure` names them in the per-item error with the reminder that this server cannot delete folders. | `OutlookComSession.TryMoveItemToPath`; `MailService.MoveOne` / `DescribeMoveFailure` | a refusal, and a folder nobody mentioned | ~~SILENT~~ -> **REPORTED** (`createdFolders` + the per-item error) |
| J5 | Nine tools had NO unknown-outcome message on the common way out of a killed COM host. `send`, `update_draft` and the move batch each catch `TimeoutException` and write their own correct message; `new_draft`, `reply_draft`, `replyall_draft`, `forward_draft`, `discard_draft`, `save_attachment`, `open_in_outlook`, `goto_folder` and `show_search_results` did not, so they got the generic "Outlook did not answer ... so the next call starts clean" - a statement about the HOST, read as a statement about the mail. **CLOSED 2026-08-20** - the `ComHostTimeoutException` arm now opens with `MutationOutcome.DescribeInterrupted(ex.Operation)` and keeps its own remedy, the same shape `ComHostSupervisor.DescribeInterruption` already proved for the other exit. | `OutlookTools.GuardAsync`, the `catch (ComHostTimeoutException)` arm | "the next call starts clean" | ~~PROSE, wrong~~ -> **REPORTED** (`outcome`) |
| J6 | A partial file survives a failed attachment save, at a path the caller was never told - the name is chosen COM-side and gets a numeric suffix on a clash. Separately, the size read that followed the save sat inside the same `try`, so an `IOException` from `FileInfo.Length` reported "could not be saved" over a file that was saved in full. **CLOSED 2026-08-20** - `attemptedPath` is reported on the failure path and named in the message; the size read is best-effort, because measuring a file is reporting and must not be able to fail a completed save. | `OutlookComSession.TrySaveAttachment`; `MailService.SaveAttachment` | "Attachment could not be saved" | ~~SILENT~~ -> **REPORTED** (the path, in the message; `outcome`) |
| J7 | `open_in_outlook`, `goto_folder` and `show_search_results` reported a failure as though the user's screen were untouched. `Display()` is the LAST call in its sequence, so a failure over it can follow the window opening and the mail being marked read; `BuildNavigationError`'s catch-all sits after an Explorer may have been created and shown, after `CurrentFolder` was set, and after `Explorer.Search` was issued. **CLOSED 2026-08-20.** `BuildNavigationError` is the one function the audit deprioritised and left unread; reading it put it in the same class as the rest. | `MailService.OpenInOutlook` / `BuildNavigationError` | "Item could not be displayed" / "could not show the requested view" | ~~PROSE, wrong~~ -> **REPORTED** (`outcome`) |
| J8 | The framing refusal for an answer too large to return said "The work itself succeeded and nothing was changed", unconditionally - the only claim in the product that asserted a mutation both happened and did not, since `Invoke` runs to completion before anything is framed. The tool-layer restatement added "retrying the SAME request will refuse again", which over a mutation is an invitation to perform it twice. **CLOSED 2026-08-20** - both halves branch on `ComSessionOperations.IsRetryable`; reads keep the old sentence, mutations are told the work SUCCEEDED and not to repeat it. Still true that no reachable 64 MB mutating answer could be constructed by reading: the verdict is false by construction, not observed. | `ComHostServer.TooLarge`; `OutlookTools`' `catch (ComHostResponseTooLargeException)` | a self-contradicting sentence | ~~PROSE, wrong~~ -> **REPORTED** (`outcome: applied`) |
| J9 | `discard_draft`'s `com_failure` said "Nothing was changed" over a sequence whose one mutating call is `MailItem.Delete()`. **CLOSED 2026-08-20**, and the RECORDED REASON was wrong as well as the message: `TODO.md` said the post-delete re-locate reaches the catch-all, but `TryFindDiscardedCopy` has its own `catch when (IsComCallFailure(ex))` covering all six COM-failure types, so that route is closed. The open route is `Delete()` itself, bare in the outer `try`, whose disconnect family includes `RPC_S_CALL_FAILED` - documented as MAY OR MAY NOT have executed. One mutating call with that semantic is enough. | `OutlookComSession.TryDiscardDraft`; `MailService.BuildDraftRefusal` | "Nothing was changed" | ~~PROSE, wrong~~ -> **REPORTED** (`outcome: unknown`) |

**Fourteen of the thirty-one were true and are unchanged**, and the reason is worth keeping as
the test for a new one: each sits before every write. `WritingRulesGate.RetryMessage` (no COM
host is even started), `BuildOverlongSubjectMessage` (pure validation), the five "Nothing was
sent" refusals (all decided before `Send()`), the named draft refusals (all decided by
`CheckEditableDraft` or the registry gate), and the add-in's eight registration messages (the
write is `File.Replace` via a sibling temp file). If a sentence is not written before the first
mutating call, it may not assert the negative.

## 8. Caps that ARE properly reported (for map completeness)

| Cap | Value | Field |
|---|---|---|
| `top` | 1-100, default 25 | `truncated:true` (over-fetch-by-one, so definite) plus clamp advice (`MailService.cs:646-659`) |
| `SweepPerFolderCap` | 200 | `sweep.itemCappedFolders` plus `item_cap` in `sweep.coverageGaps`; where Outlook would not sort the folder's table the cut is arbitrary rather than newest-first, and those folders are named in `sweep.itemCappedFoldersUnsorted` and raise `item_cap_unsorted` instead, whose sentence withdraws the "the oldest is what is missing" claim (H2) |
| Sweep rows that would not open | — | `sweep.rowsUnreadable` plus `rows_unreadable` in `sweep.coverageGaps` (H1) |
| Swept/scanned items an unevaluable filter dropped | — | `sweep.itemsFilterUnreadable` + `sweep.filtersUnevaluated` plus `filter_unreadable` (I1); same three on `exhaustive.*` |
| Exhaustive rows that would not open | — | `exhaustive.rowsUnreadable` (raises `rows_unreadable`) and `exhaustive.rowsDropped` (the total; since B3 removed the class filter the two are equal by construction) |
| Index rows the tier refused / ran out of | — | `index.rowsScanned`, `index.rowsDropped`, `index.candidatesExhausted` (B3, G6) |
| `UnindexedStoreListCap` | 12 | `sweep.storesWithoutIndexTruncated` + `sweep.storesWithoutIndexTotal`, and the `no_index_frontier` advice sentence names the cap (Q7b) |
| `NonMailClassAdviceCap` | 4 | distinct item classes the "not ordinary mail" advice names before ", ..."; the per-hit `itemClass` fields are the complete answer |
| `MaxScopedSweepFolders` / time budget / depth | 40 / 2000 ms / 64 | `sweep.folderCapReached` / `timeBudgetExceeded` / `depthLimitReached` plus gap codes |
| `SweptFolderListCap` | 12 | `sweep.folderListOmitted:true` |
| Sweep could not run | — | `freshness:"index-only"`, `degraded:true`, `sweep.error` - and `no_index_frontier` beside it where a store has no index tier either (A1), because the two are independent facts with different remedies |
| Sweep not needed | — | `sweep.notNeeded:true`, `freshness:"live"` - except over a scope with no index frontier, where "the index already covers this window" is false: then `freshness:"partial"`, `degraded:true` and `no_index_frontier` (A1) |
| Sweep refused (recipient filter, attachment-only) | - | `sweep.error`, `freshness:"index-only"`, `degraded:true`, plus `no_index_frontier` where it applies (A1) |
| Stores the index holds no mail for | - | `sweep.indexFrontierMissing`, `sweep.storesWithoutIndex` (+ the cap pair above), `no_index_frontier`; the same fact in `outlook_health` as `index.perStore[].inLocalIndex:false` (A3) and in `list_accounts` as `inLocalIndex`/`locallySearchable` (A5) |
| Delegate widening / leaf collision | 40 paths | `scope.shape`, `scope.widened`, `scope.folderNamesMatched` plus advice |
| `read` body/html/headers/recipients/attachments | 500k/100k/64k/100/100 | `bodyTruncated`+`bodyTotalChars`, `bodyHtmlTruncated`+`bodyHtmlTotalChars`, `headersTruncated`, `recipientsTruncated`+`recipientsTotal`, `attachmentsTruncated`+`attachmentsTotal` |
| `list_folders` page | 1000 | `truncated` plus `nextOffset` plus `folderTotal` |
| `FolderWalkAbsoluteCap` / `FolderWalkDepthGuard` (the walk itself) | 10 000 / 64 | `truncated` **without** `nextOffset`, plus `walkCapReached` / `depthLimitReached` and an advice sentence (G3); the same two bounds reach a delegate scope as `scope.folderNamesTruncated` + `degraded` (G4) |
| Stores Outlook will not name | - | listed under `(unnamed store N)` with `stores[].nameUnreadable`, `storesUnnamed` / `storesUnnamedExcluded` and advice; swept and counted as `sweep.storesUnnamed`; refused by name if passed back as a scope (G2) |
| `thread` top | 1-200 | `truncated`; it caps the LIVE walk too, so a capped walk also raises `member_cap` in `live.coverageGaps` with `freshness:"partial"` (C1) |
| `thread` live walk could not run | — | `freshness:"index-only"`, `degraded:true`, `live.error` (`NoAnchorItem` / `UnknownAnchorId` / `AnchorNotLocatable` / COM token) plus advice (C1) |
| Exhaustive scan cap / time budget / skipped folders | `top` / 600 s / — | `exhaustive.truncated` / `timedOut` / `foldersSkipped`, and since 2026-08-18 `degraded:true` + `freshness:"partial"` (F1), plus `exhaustive.coverageGaps` codes `result_cap` / `time_budget` / `folders_skipped` (F5). The cap counts items the DASL filter matched, BEFORE `from`/`unread_only`/`has_attachments`, so a truncated scan carrying one of those also raises `post_cap_filter` with `exhaustive.postCapFilters` + `exhaustive.itemsFilteredOut` (F3) |
| `move_mail` ids | 50 | error |
| `UnresolvedRecipientsCap` (drafts) | 20 | ~~**no flag** — `Take(20)`, tangential to search~~ -> `unresolvedRecipientsTruncated` + `unresolvedRecipientsTotal` + `unresolvedRecipientsAdvice` on both draft payloads (`MailService.CapUnresolvedRecipients` / `DescribeUnresolvedRecipientCap`, 2026-08-18). The "tangential" label was wrong: the resolved list can survive being short - the draft holds the recipients either way and every operation reads the full COM-side list - while an unresolved address is a defect the agent must ASK THE USER about, so a short list read as complete is one the user never hears the rest of |
| Exhaustive folder walk depth | 64 (`FolderWalkDepthGuard`) | `exhaustive.depthLimitReached` + `depth_limit` in `exhaustive.coverageGaps`, `freshness:"partial"`, `degraded:true`, and an advice sentence quoting the constant (F4). Before 2026-08-18 there was no bound to report: the walk recursed until the stack ran out and ended the COM host |
| Sweep breadth (which folders the tier looks in at all) | 4 arrival-path folders per store, shallow | `sweep.scopeShape` (`default_folders` / `folder` / `folder_only`) beside the `sweep.scope` sentence, both from one classifier (E2). Deliberately no gap code and no `degraded`: it holds for nearly every search, and `ClassifyFreshness` would turn it into a permanent `partial` - the same reasoning as `sweep.attachmentTextCovered` |
| `UnindexedStoreListCap` on `thread` | 12 (shared constant) | `live.storesWithoutIndexTruncated` + `live.storesWithoutIndexTotal`, and the `unindexed_store` advice sentence names the cap and the remainder (C4). One `MailService.CapUnindexedStoreList` cuts both this list and the sweep's |
| Sweep body, per item and per sweep | 500 000 chars / 32 MiB (`OutlookComSession.SweepBodyCharsCap` / `SweepBodyBytesBudget`) | `sweep.itemsBodyCapped` counts the swept items whose body was cut, `sweep.itemsBodyCappedUnmatched` the subset that then failed the term match - the only ones a cut can have cost a hit - and that subset alone raises `body_cap` in `sweep.coverageGaps`, with `freshness: "partial"`, `degraded: true` and one advice sentence built from the same two numbers. `sweep.bodyBudgetExhausted` says WHICH bound cut, because the remedies differ (read one enormous mail, versus sweep fewer of them). The counts are taken inside the store filter, so a cached all-stores sweep cannot report another account's cuts; the flag rides per item on `ComMailBrief.BodyTruncated`. A cut body never reaches the read-path body cache, which is fed only from `ComItemDetail` - pinned behaviourally, since a truncated body cached as whole would be worse than the hole being closed |
| `snippet_chars` | clamped 0-1000 | silent clamp, cosmetic |
| Attachment text inside the freshness window | - | `sweep.attachmentTextCovered:false`, plus one advice sentence when the window held anything (B2). Deliberately not a coverage code: it holds for nearly every search, and `ClassifyFreshness` would turn it into a permanent `partial` |
| A folder the index cannot address | - | `index.folderNotIndexed:true` plus the unresolved-folder advice sentence (G5); no `degraded`, because a genuinely new folder is indistinguishable from a path that does not resolve |

## Notes on the working tree

At audit start `git status` reported (contrary to the session snapshot):

    M  McpServer/OutlookAI.McpServer.Tests/T2/LiveStoreCountTripwire.cs   (+14 lines)
    ?? McpServer/OutlookAI.McpServer.Tests/T1/LiveOutlookPreflightTests.cs
    ?? McpServer/OutlookAI.McpServer.Tests/T2/LiveOutlookPreflight.cs

mtimes 08:43-08:52 local, i.e. minutes before this audit began; all three were already present in the first file listing this audit made. They account for the 1393 to 1416 test-count delta. Nothing here was written by this audit.
